-- ══════════════════════════════════════════════════════════════════
-- Ryuu's HUB V2
-- by ZivX ◈ Upgraded: Better Prediction + Ping Comp + New Intro
--           + Premium Unlock + Manual Spam + HUD + Ability ESP
-- ══════════════════════════════════════════════════════════════════

-- ── Services ─────────────────────────────────────────────────────
local Players       = game:GetService("Players")
local RunService    = game:GetService("RunService")
local VIM           = game:GetService("VirtualInputManager")
local UIS           = game:GetService("UserInputService")
local TweenService  = game:GetService("TweenService")
local CoreGui       = game:GetService("CoreGui")
local Lighting      = game:GetService("Lighting")
local Stats         = game:GetService("Stats")
local RS            = game:GetService("ReplicatedStorage")
local Plr           = Players.LocalPlayer
local Cam           = workspace.CurrentCamera

-- ── Destroy old instances ─────────────────────────────────────────
pcall(function()
    for _, g in ipairs(CoreGui:GetChildren()) do
        if g.Name:find("Ryuu") then pcall(function() g:Destroy() end) end
    end
end)

-- ══════════════════════════════════════════════════════════════════
-- RAYFIELD STUB (fallback if CDN fails)
-- ══════════════════════════════════════════════════════════════════
local Rayfield
local function makeStub()
    local function makeTab()
        return {
            CreateToggle   = function(_, t) if t and t.Callback then t.Callback(t.CurrentValue or false) end end,
            CreateSlider   = function(_, t) if t and t.Callback then t.Callback(t.CurrentValue or 0) end end,
            CreateLabel    = function() return {Text=""} end,
            CreateSection  = function() end,
            CreateDivider  = function() end,
            CreateButton   = function() end,
            CreateKeybind  = function() end,
            CreateInput    = function() end,
            CreateDropdown = function(_, t) if t and t.Callback then t.Callback({t.CurrentOption and t.CurrentOption[1] or ""}) end end,
        }
    end
    return {
        CreateWindow = function() return {CreateTab=function() return makeTab() end} end,
        Notify       = function() end,
        Flags        = {},
    }
end

-- ══════════════════════════════════════════════════════════════════
-- KEY SYSTEM
-- ══════════════════════════════════════════════════════════════════
local VALID_KEYS = {
    "RYUU-KEY1434s9df9sdfsdf",
    "RYUU-KEY1a0da90sdacsdaasdx",
    "RYUU-KEY2o3i4j2o3232sxasdv",
    "09153552478",
    "x31THEGOAT",
    "RYUU-KEYS0CF94TEDFGVasd4swdf",
    "RYUU-KEY1csdfsf444sdccasdaaasdf",
    "RYUU-KEY0aioscd8asdcasvvawqxsac",
    "RYUU-KEY9x98K998y98om9N6na778Xzsaddf",
    "RYUU-KEY8IA6SDXAOM30z68BKOsadoawdxx",
    "RYUU-KEY8mu90m89898989M9M09m6m8Kaqq",
    "RYUU-KEY7H5Bnq5Hzuy7ksdO9B42ASnv5RT",
}
local DEVICE_FILE = "ryuu_device.txt"
local LOCK_FILE   = "ryuu_keys.txt"

local function GetDeviceID()
    local ok, stored = pcall(function() return readfile(DEVICE_FILE) end)
    if ok and stored and #stored >= 16 then
        return stored:match("^%s*(.-)%s*$")
    end
    local parts = {}
    pcall(function()
        local id = tostring(game:GetService("RbxAnalyticsService"):GetClientId())
        if id and #id > 4 then table.insert(parts, id) end
    end)
    pcall(function() table.insert(parts, tostring(Plr.UserId)) end)
    table.insert(parts, tostring(math.random(100000,999999)))
    table.insert(parts, tostring(os.time()))
    local raw  = table.concat(parts,"-")
    local hash = 0
    for i=1,#raw do hash=(hash*31+string.byte(raw,i))%0xFFFFFFFF end
    local deviceID = string.format("DEV-%08X-%08X-%06d",
        hash, (hash*1000003)%0xFFFFFFFF, math.random(100000,999999))
    pcall(function() writefile(DEVICE_FILE, deviceID) end)
    return deviceID
end

local function LoadLocks()
    local ok, data = pcall(function() return readfile(LOCK_FILE) end)
    if not ok or not data or data=="" then return {} end
    local locks = {}
    for line in data:gmatch("[^\n]+") do
        local k,d = line:match("^(.+)=(.+)$")
        if k and d then locks[k]=d end
    end
    return locks
end

local function SaveLocks(locks)
    local lines={}
    for k,d in pairs(locks) do table.insert(lines,k.."="..d) end
    pcall(function() writefile(LOCK_FILE,table.concat(lines,"\n")) end)
end

local function ValidateKey(key)
    if not key or #key<4 then return "invalid" end
    local k = key:match("^%s*(.-)%s*$")
    local keyExists = false
    for _,v in ipairs(VALID_KEYS) do
        if v==k then keyExists=true; break end
    end
    if not keyExists then return "invalid" end
    local myDevice = GetDeviceID()
    local locks    = LoadLocks()
    if locks[k]==nil then
        locks[k]=myDevice; SaveLocks(locks); return "ok"
    elseif locks[k]==myDevice then
        return "ok"
    else
        return "locked_other"
    end
end

-- ══════════════════════════════════════════════════════════════════
-- INTRO  ◈  Holographic Boot Sequence (no key gate — free)
-- ══════════════════════════════════════════════════════════════════
do
    local IntroSG = Instance.new("ScreenGui")
    IntroSG.Name="RyuuIntro"; IntroSG.ResetOnSpawn=false
    IntroSG.IgnoreGuiInset=true; IntroSG.DisplayOrder=99999
    pcall(function() IntroSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not IntroSG.Parent then pcall(function() IntroSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    -- Sound helper
    local function playSound(id, vol, pitch)
        pcall(function()
            local s = Instance.new("Sound", workspace)
            s.SoundId = "rbxassetid://"..id; s.Volume = vol or 0.6
            s.PlaybackSpeed = pitch or 1; s.RollOffMaxDistance = 0
            s:Play(); game:GetService("Debris"):AddItem(s, 4)
        end)
    end

    local scanRunning = true

    -- ── PHASE 1: White flash ──────────────────────────────────────
    local Flash = Instance.new("Frame", IntroSG)
    Flash.Size=UDim2.new(1,0,1,0); Flash.BackgroundColor3=Color3.fromRGB(255,255,255)
    Flash.BackgroundTransparency=0; Flash.BorderSizePixel=0; Flash.ZIndex=100
    playSound(6042053626, 0.8, 1.4)
    TweenService:Create(Flash, TweenInfo.new(0.18, Enum.EasingStyle.Quad),
        {BackgroundTransparency=1}):Play()
    task.wait(0.22)

    -- ── DARK BASE ────────────────────────────────────────────────
    local BG = Instance.new("Frame", IntroSG)
    BG.Size=UDim2.new(1,0,1,0); BG.BackgroundColor3=Color3.fromRGB(2,1,6)
    BG.BackgroundTransparency=0; BG.BorderSizePixel=0; BG.ZIndex=1

    -- Radial center glow
    local Glow = Instance.new("Frame", BG)
    Glow.AnchorPoint=Vector2.new(0.5,0.5); Glow.Position=UDim2.new(0.5,0,0.46,0)
    Glow.Size=UDim2.new(0,600,0,300); Glow.BackgroundColor3=Color3.fromRGB(55,10,140)
    Glow.BackgroundTransparency=0.94; Glow.BorderSizePixel=0; Glow.ZIndex=1
    Instance.new("UICorner",Glow).CornerRadius=UDim.new(1,0)

    -- ── DENSE SCAN LINE GRID ─────────────────────────────────────
    local ScanContainer = Instance.new("Frame", IntroSG)
    ScanContainer.Size=UDim2.new(1,0,1,0); ScanContainer.BackgroundTransparency=1
    ScanContainer.BorderSizePixel=0; ScanContainer.ZIndex=2; ScanContainer.ClipsDescendants=true

    local lineCount = 45
    local scanLines = {}
    for i = 1, lineCount do
        local line = Instance.new("Frame", ScanContainer)
        line.Size = UDim2.new(1,0,0,1)
        line.Position = UDim2.new(0,0,(i-1)/lineCount,0)
        line.BackgroundColor3 = Color3.fromRGB(80,220,255)
        line.BackgroundTransparency = 0.72; line.BorderSizePixel=0; line.ZIndex=2
        table.insert(scanLines, line)
    end

    -- Animated scan-line sweep (lines drift downward)
    task.spawn(function()
        local offset = 0
        while scanRunning do
            offset = (offset + 0.016) % 1
            for i, line in ipairs(scanLines) do
                local base = (i-1)/lineCount
                local y = (base + offset) % 1
                local brightness = math.abs(math.sin(y * math.pi * 3))
                pcall(function()
                    line.BackgroundTransparency = 0.55 + brightness * 0.38
                    line.Position = UDim2.new(0,0,y,0)
                end)
            end
            task.wait(0.018)
        end
    end)

    -- Vertical grid lines (holographic matrix)
    local GridContainer = Instance.new("Frame", IntroSG)
    GridContainer.Size=UDim2.new(1,0,1,0); GridContainer.BackgroundTransparency=1
    GridContainer.BorderSizePixel=0; GridContainer.ZIndex=3
    for i = 1, 16 do
        local vl = Instance.new("Frame", GridContainer)
        vl.Size=UDim2.new(0,1,1,0); vl.Position=UDim2.new(i/16,0,0,0)
        vl.BackgroundColor3=Color3.fromRGB(0,160,220); vl.BackgroundTransparency=0.88
        vl.BorderSizePixel=0; vl.ZIndex=3
    end
    for i = 1, 9 do
        local hl = Instance.new("Frame", GridContainer)
        hl.Size=UDim2.new(1,0,0,1); hl.Position=UDim2.new(0,0,i/9,0)
        hl.BackgroundColor3=Color3.fromRGB(0,160,220); hl.BackgroundTransparency=0.88
        hl.BorderSizePixel=0; hl.ZIndex=3
    end

    -- ── VERTICAL SWEEP BEAM (L→R) ────────────────────────────────
    local VSweep = Instance.new("Frame", IntroSG)
    VSweep.Size=UDim2.new(0,3,1,0); VSweep.Position=UDim2.new(-0.05,0,0,0)
    VSweep.BackgroundColor3=Color3.fromRGB(120,240,255); VSweep.BackgroundTransparency=0.3
    VSweep.BorderSizePixel=0; VSweep.ZIndex=8
    local VSweepGlow = Instance.new("UIGradient", VSweep)
    VSweepGlow.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(0,0,0)),
        ColorSequenceKeypoint.new(0.5,Color3.fromRGB(180,255,255)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(0,0,0)),
    }; VSweepGlow.Rotation=90
    local VBloom = Instance.new("Frame", IntroSG)
    VBloom.Size=UDim2.new(0,50,1,0); VBloom.Position=UDim2.new(-0.1,0,0,0)
    VBloom.BackgroundColor3=Color3.fromRGB(60,180,255); VBloom.BackgroundTransparency=0.82
    VBloom.BorderSizePixel=0; VBloom.ZIndex=7
    local BloomGrad = Instance.new("UIGradient", VBloom)
    BloomGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(0,0,0)),
        ColorSequenceKeypoint.new(0.5,Color3.fromRGB(60,180,255)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(0,0,0)),
    }

    playSound(9119713951, 0.5, 1.0)
    TweenService:Create(VSweep, TweenInfo.new(0.55, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {Position=UDim2.new(1.05,0,0,0)}):Play()
    TweenService:Create(VBloom, TweenInfo.new(0.55, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {Position=UDim2.new(1.0,0,0,0)}):Play()
    task.wait(0.3)

    -- ── CENTER HOLOGRAPHIC CARD ───────────────────────────────────
    local Card = Instance.new("Frame", IntroSG)
    Card.AnchorPoint=Vector2.new(0.5,0.5); Card.Position=UDim2.new(0.5,0,0.5,0)
    Card.Size=UDim2.new(0,0,0,0); Card.BackgroundColor3=Color3.fromRGB(4,14,22)
    Card.BackgroundTransparency=0; Card.BorderSizePixel=0; Card.ZIndex=10
    Instance.new("UICorner",Card).CornerRadius=UDim.new(0,16)
    local CardStroke = Instance.new("UIStroke", Card)
    CardStroke.Thickness=1.8; CardStroke.Color=Color3.fromRGB(0,210,255); CardStroke.Transparency=0.1
    local CardGrad = Instance.new("UIGradient", Card)
    CardGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(5,20,35)),
        ColorSequenceKeypoint.new(0.5,Color3.fromRGB(3,10,18)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(5,20,35)),
    }; CardGrad.Rotation=135

    -- Card top accent
    local Accent = Instance.new("Frame", Card)
    Accent.Size=UDim2.new(1,0,0,2); Accent.Position=UDim2.new(0,0,0,0)
    Accent.BackgroundColor3=Color3.fromRGB(0,210,255); Accent.BorderSizePixel=0; Accent.ZIndex=11
    Instance.new("UICorner",Accent).CornerRadius=UDim.new(0,16)

    -- PREMIUM badge (top-right)
    local Badge = Instance.new("TextLabel", Card)
    Badge.AnchorPoint=Vector2.new(1,0); Badge.Position=UDim2.new(1,-10,0,8)
    Badge.Size=UDim2.new(0,72,0,18); Badge.BackgroundColor3=Color3.fromRGB(0,50,80)
    Badge.BorderSizePixel=0; Badge.Text="FREE"; Badge.Font=Enum.Font.GothamBlack
    Badge.TextSize=9; Badge.TextColor3=Color3.fromRGB(0,220,255); Badge.TextTransparency=1; Badge.ZIndex=12
    Instance.new("UICorner",Badge).CornerRadius=UDim.new(0,5)
    local BadgeStroke=Instance.new("UIStroke",Badge); BadgeStroke.Thickness=1; BadgeStroke.Color=Color3.fromRGB(0,180,255)

    -- ◈ icon
    local IconLbl = Instance.new("TextLabel", Card)
    IconLbl.AnchorPoint=Vector2.new(0,0.5); IconLbl.Position=UDim2.new(0,18,0,62)
    IconLbl.Size=UDim2.new(0,44,0,44); IconLbl.BackgroundTransparency=1; IconLbl.Text="◈"
    IconLbl.Font=Enum.Font.GothamBlack; IconLbl.TextSize=36
    IconLbl.TextColor3=Color3.fromRGB(0,210,255); IconLbl.TextTransparency=1; IconLbl.ZIndex=12

    -- Main headline
    local Headline = Instance.new("TextLabel", Card)
    Headline.AnchorPoint=Vector2.new(0,0.5); Headline.Position=UDim2.new(0,70,0,52)
    Headline.Size=UDim2.new(1,-80,0,30); Headline.BackgroundTransparency=1
    Headline.Text="RYUU  HUB"; Headline.Font=Enum.Font.GothamBlack; Headline.TextSize=28
    Headline.TextTransparency=1; Headline.TextXAlignment=Enum.TextXAlignment.Left; Headline.ZIndex=12
    local HLGrad = Instance.new("UIGradient", Headline)
    HLGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(0,200,255)),
        ColorSequenceKeypoint.new(0.5,Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(0,200,255)),
    }

    -- Sub line
    local SubLbl = Instance.new("TextLabel", Card)
    SubLbl.AnchorPoint=Vector2.new(0,0.5); SubLbl.Position=UDim2.new(0,70,0,82)
    SubLbl.Size=UDim2.new(1,-80,0,16); SubLbl.BackgroundTransparency=1
    SubLbl.Text="V 2   ·   by ZivX   ·   Loading..."; SubLbl.Font=Enum.Font.Gotham; SubLbl.TextSize=10
    SubLbl.TextColor3=Color3.fromRGB(0,160,200); SubLbl.TextTransparency=1
    SubLbl.TextXAlignment=Enum.TextXAlignment.Left; SubLbl.ZIndex=12

    -- Divider inside card
    local CDivider = Instance.new("Frame", Card)
    CDivider.Size=UDim2.new(0,0,0,1); CDivider.Position=UDim2.new(0,18,0,104)
    CDivider.BackgroundColor3=Color3.fromRGB(0,160,210); CDivider.BorderSizePixel=0; CDivider.ZIndex=11

    -- Progress bar inside card
    local CBarBG = Instance.new("Frame", Card)
    CBarBG.Size=UDim2.new(1,-36,0,4); CBarBG.Position=UDim2.new(0,18,0,116)
    CBarBG.BackgroundColor3=Color3.fromRGB(0,20,40); CBarBG.BorderSizePixel=0; CBarBG.ZIndex=11
    Instance.new("UICorner",CBarBG).CornerRadius=UDim.new(1,0)
    local CBar = Instance.new("Frame", CBarBG)
    CBar.Size=UDim2.new(0,0,1,0); CBar.BackgroundColor3=Color3.fromRGB(0,200,255)
    CBar.BorderSizePixel=0; CBar.ZIndex=12
    Instance.new("UICorner",CBar).CornerRadius=UDim.new(1,0)
    local CBarGrad = Instance.new("UIGradient", CBar)
    CBarGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(0,140,220)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(80,255,255)),
    }
    local CBarShimmer = Instance.new("Frame", CBar)
    CBarShimmer.Size=UDim2.new(0,30,1,0); CBarShimmer.Position=UDim2.new(-0.2,0,0,0)
    CBarShimmer.BackgroundColor3=Color3.fromRGB(255,255,255); CBarShimmer.BackgroundTransparency=0.7
    CBarShimmer.BorderSizePixel=0; CBarShimmer.ZIndex=13
    Instance.new("UICorner",CBarShimmer).CornerRadius=UDim.new(1,0)

    -- Status label below card
    local StatusLbl = Instance.new("TextLabel", IntroSG)
    StatusLbl.AnchorPoint=Vector2.new(0.5,0.5); StatusLbl.Position=UDim2.new(0.5,0,0.72,0)
    StatusLbl.Size=UDim2.new(0,340,0,14); StatusLbl.BackgroundTransparency=1
    StatusLbl.Text="Initializing..."; StatusLbl.Font=Enum.Font.Gotham; StatusLbl.TextSize=10
    StatusLbl.TextColor3=Color3.fromRGB(0,160,200); StatusLbl.ZIndex=8

    -- ── ANIMATE SEQUENCE ─────────────────────────────────────────
    local loadDone = false

    task.spawn(function()
        -- Pop card open
        playSound(4590662766, 0.65, 1.1)
        TweenService:Create(Card,
            TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            {Size=UDim2.new(0,360,0,140)}):Play()
        task.wait(0.22)

        -- Fade in card elements
        TweenService:Create(Badge, TweenInfo.new(0.18), {TextTransparency=0}):Play()
        TweenService:Create(IconLbl, TweenInfo.new(0.2), {TextTransparency=0}):Play()
        task.wait(0.1)
        TweenService:Create(Headline, TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            {TextTransparency=0}):Play()
        task.wait(0.12)
        TweenService:Create(SubLbl, TweenInfo.new(0.2), {TextTransparency=0}):Play()
        task.wait(0.18)
        TweenService:Create(CDivider, TweenInfo.new(0.3, Enum.EasingStyle.Quad),
            {Size=UDim2.new(1,-36,0,1)}):Play()
        task.wait(0.1)

        -- Loading steps with bar animation
        local STEPS = {
            "Initializing engine...",
            "Loading Rayfield...",
            "Configuring parry core...",
            "Connecting to services...",
            "Starting systems...",
        }

        -- Shimmer loop
        task.spawn(function()
            while not loadDone do
                pcall(function()
                    CBarShimmer.Position=UDim2.new(-0.15,0,0,0)
                    TweenService:Create(CBarShimmer,
                        TweenInfo.new(0.65,Enum.EasingStyle.Quad,Enum.EasingDirection.InOut),
                        {Position=UDim2.new(1.15,0,0,0)}):Play()
                end)
                task.wait(1.0)
            end
        end)

        local stepCount = #STEPS
        for i, stepText in ipairs(STEPS) do
            if loadDone then break end
            local pct = i / stepCount
            StatusLbl.Text = stepText
            TweenService:Create(CBar,
                TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                {Size=UDim2.new(pct * 0.85,0,1,0)}):Play()
            task.wait(0.38)
        end

        -- Wait for Rayfield
        local w = 0
        while not loadDone and w < 8 do task.wait(0.1); w=w+0.1 end

        -- Snap to 100% green
        SubLbl.Text = "V 2   ·   by ZivX   ·   Ready!"
        StatusLbl.Text = "Ready"
        StatusLbl.TextColor3 = Color3.fromRGB(50,220,110)
        TweenService:Create(CBar, TweenInfo.new(0.18),
            {Size=UDim2.new(1,0,1,0), BackgroundColor3=Color3.fromRGB(35,200,90)}):Play()
        TweenService:Create(CBarBG, TweenInfo.new(0.18),
            {BackgroundColor3=Color3.fromRGB(8,35,18)}):Play()
        TweenService:Create(CDivider, TweenInfo.new(0.18),
            {BackgroundColor3=Color3.fromRGB(35,200,90)}):Play()
        TweenService:Create(CardStroke, TweenInfo.new(0.2),
            {Color=Color3.fromRGB(35,220,100)}):Play()
        TweenService:Create(Accent, TweenInfo.new(0.2),
            {BackgroundColor3=Color3.fromRGB(35,220,100)}):Play()

        -- Second confirm sweep (right → left)
        task.wait(0.25)
        playSound(6042053626, 0.5, 0.85)
        local VSweep2 = Instance.new("Frame", IntroSG)
        VSweep2.Size=UDim2.new(0,2,1,0); VSweep2.Position=UDim2.new(1.05,0,0,0)
        VSweep2.BackgroundColor3=Color3.fromRGB(80,255,200); VSweep2.BackgroundTransparency=0.4
        VSweep2.BorderSizePixel=0; VSweep2.ZIndex=9
        TweenService:Create(VSweep2, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
            {Position=UDim2.new(-0.05,0,0,0)}):Play()
        task.wait(0.4)

        -- Border glow pulse → white flash → fade
        TweenService:Create(CardStroke, TweenInfo.new(0.1),
            {Color=Color3.fromRGB(255,255,255), Thickness=3.5}):Play()
        task.wait(0.12)
        TweenService:Create(CardStroke, TweenInfo.new(0.4),
            {Color=Color3.fromRGB(35,220,100), Thickness=1.8}):Play()
        task.wait(0.5)

        scanRunning = false

        -- Fade scan lines
        for _, line in ipairs(scanLines) do
            TweenService:Create(line, TweenInfo.new(0.3), {BackgroundTransparency=1}):Play()
        end
        TweenService:Create(GridContainer, TweenInfo.new(0.4), {BackgroundTransparency=1}):Play()
        task.wait(0.5)

        -- Final black fade-out
        local ft = TweenInfo.new(0.38)
        TweenService:Create(BG,       ft, {BackgroundTransparency=1}):Play()
        TweenService:Create(Card,     ft, {BackgroundTransparency=1}):Play()
        TweenService:Create(CardStroke, ft, {Transparency=1}):Play()
        TweenService:Create(Headline, ft, {TextTransparency=1}):Play()
        TweenService:Create(SubLbl,   ft, {TextTransparency=1}):Play()
        TweenService:Create(IconLbl,  ft, {TextTransparency=1}):Play()
        TweenService:Create(Badge,    ft, {TextTransparency=1}):Play()
        TweenService:Create(StatusLbl, ft, {TextTransparency=1}):Play()
        task.wait(0.42)
        pcall(function() IntroSG:Destroy() end)
    end)

    -- ── Load Rayfield (parallel) ──────────────────────────────────
    local RF_URLS = {
        "https://sirius.menu/rayfield",
        "https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua",
    }
    for _, url in ipairs(RF_URLS) do
        local ok, res = pcall(function()
            return loadstring(game:HttpGet(url, true))()
        end)
        if ok and res then Rayfield = res; break end
    end
    if not Rayfield then
        warn("[Ryuu V2] Rayfield load failed — using stub")
        Rayfield = makeStub()
    end

    loadDone = true
end


-- ══════════════════════════════════════════════════════════════════
-- ALWAYS-ON HUD  ◈  Upgraded layout
-- ══════════════════════════════════════════════════════════════════
do
    local HudSG = Instance.new("ScreenGui")
    HudSG.Name           = "RyuuHUD"
    HudSG.ResetOnSpawn   = false
    HudSG.IgnoreGuiInset = true
    HudSG.DisplayOrder   = 9998
    pcall(function()
        HudSG.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui
    end)
    if not HudSG.Parent then
        pcall(function() HudSG.Parent = Plr:WaitForChild("PlayerGui") end)
    end

    local HudFrame = Instance.new("Frame", HudSG)
    HudFrame.AnchorPoint       = Vector2.new(1,0)
    HudFrame.Position          = UDim2.new(1,-14,0,14)
    HudFrame.Size              = UDim2.new(0,190,0,96)
    HudFrame.BackgroundColor3  = Color3.fromRGB(6,3,14)
    HudFrame.BackgroundTransparency = 0.18
    HudFrame.BorderSizePixel   = 0
    Instance.new("UICorner", HudFrame).CornerRadius = UDim.new(0,10)
    local HudStroke = Instance.new("UIStroke", HudFrame)
    HudStroke.Thickness  = 1.2
    HudStroke.Color      = Color3.fromRGB(80,35,170)
    HudStroke.Transparency = 0.35

    -- Subtle top gradient
    local HudGrad = Instance.new("UIGradient", HudFrame)
    HudGrad.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(18,8,40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(6,3,14)),
    }
    HudGrad.Rotation = 90

    -- Title badge
    local HudTitle = Instance.new("TextLabel", HudFrame)
    HudTitle.Size              = UDim2.new(1,-10,0,14)
    HudTitle.Position          = UDim2.new(0,8,0,5)
    HudTitle.BackgroundTransparency = 1
    HudTitle.Text              = "◈  RYUU V2"
    HudTitle.Font              = Enum.Font.GothamBlack
    HudTitle.TextSize          = 10
    HudTitle.TextColor3        = Color3.fromRGB(140,60,255)
    HudTitle.TextXAlignment    = Enum.TextXAlignment.Left

    local divider = Instance.new("Frame", HudFrame)
    divider.Size               = UDim2.new(1,-16,0,1)
    divider.Position           = UDim2.new(0,8,0,21)
    divider.BackgroundColor3   = Color3.fromRGB(60,25,120)
    divider.BorderSizePixel    = 0

    local HudPing = Instance.new("TextLabel", HudFrame)
    HudPing.Size               = UDim2.new(1,-12,0,22)
    HudPing.Position           = UDim2.new(0,8,0,26)
    HudPing.BackgroundTransparency = 1
    HudPing.Text               = "📶  Ping  —  --ms"
    HudPing.Font               = Enum.Font.GothamBold
    HudPing.TextSize           = 12
    HudPing.TextColor3         = Color3.fromRGB(200,200,200)
    HudPing.TextXAlignment     = Enum.TextXAlignment.Left

    local HudFPS = Instance.new("TextLabel", HudFrame)
    HudFPS.Size                = UDim2.new(1,-12,0,20)
    HudFPS.Position            = UDim2.new(0,8,0,46)
    HudFPS.BackgroundTransparency = 1
    HudFPS.Text                = "🖥  FPS  —  --"
    HudFPS.Font                = Enum.Font.GothamBold
    HudFPS.TextSize            = 11
    HudFPS.TextColor3          = Color3.fromRGB(200,200,200)
    HudFPS.TextXAlignment      = Enum.TextXAlignment.Left

    local HudParry = Instance.new("TextLabel", HudFrame)
    HudParry.Size              = UDim2.new(1,-12,0,20)
    HudParry.Position          = UDim2.new(0,8,0,68)
    HudParry.BackgroundTransparency = 1
    HudParry.Text              = "⚔  Hit Rate  —  --%"
    HudParry.Font              = Enum.Font.GothamBold
    HudParry.TextSize          = 11
    HudParry.TextColor3        = Color3.fromRGB(200,200,200)
    HudParry.TextXAlignment    = Enum.TextXAlignment.Left

    local fpsCount, fpsTimer, fpsNow = 0, 0, 0
    RunService.Heartbeat:Connect(function(dt)
        fpsCount = fpsCount + 1
        fpsTimer = fpsTimer + dt
        if fpsTimer >= 0.5 then
            fpsNow   = math.floor(fpsCount / fpsTimer)
            fpsCount = 0
            fpsTimer = 0
            local col = fpsNow >= 55 and Color3.fromRGB(34,197,110)
                     or fpsNow >= 30 and Color3.fromRGB(255,200,50)
                     or Color3.fromRGB(255,80,80)
            pcall(function()
                HudFPS.Text      = ("🖥  FPS  —  %d"):format(fpsNow)
                HudFPS.TextColor3= col
            end)
        end
        pcall(function()
            -- HUD shows PingRaw (real current ms), not the min-sampled value
            local displayMs = math.floor(PingRaw > 0 and PingRaw or PingMs)
            local col = displayMs < 60  and Color3.fromRGB(34,197,110)
                     or displayMs < 120 and Color3.fromRGB(255,200,50)
                     or Color3.fromRGB(255,80,80)
            HudPing.Text      = ("📶  %dms"):format(displayMs)
            HudPing.TextColor3= col
        end)
        pcall(function()
            local rate = ParrySuccessRate()
            local pcol = rate >= 80 and Color3.fromRGB(34,197,110)
                      or rate >= 50 and Color3.fromRGB(255,200,50)
                      or Color3.fromRGB(255,80,80)
            HudParry.Text       = ("⚔  Hit Rate  —  %d%%"):format(rate)
            HudParry.TextColor3 = pcol
        end)
    end)
end

-- ══════════════════════════════════════════════════════════════════
-- STATE
-- ══════════════════════════════════════════════════════════════════
local S = {
    AutoParry       = false,
    AutoFace        = false,
    BallESP         = false,
    AbilityESP      = false,
    Triggerbot      = false,
    Fullbright      = false,
    Noclip          = false,
    InfJump         = false,
    ParryCount      = 0,
    WalkSpeed       = 16,
    JumpPower       = 50,
    LastParry       = 0,
    ParryCooldown   = 0.30,
    DidParry        = false,
    EmergencyParry  = true,
    EmergencyDist   = 6,
    ParryAccuracy   = 100,
    AntiCurve       = true,
    ParryReset      = 0.16,
}

-- Premium state
local P = {
    AutoParry       = false,
    GodEmergency    = false,
    CurveCrusher    = true,
    AutoFace        = false,
    HitboxTarget    = 4.0,
    SpeedBoostDetect= true,
    QuadCurvePredict= true,
}

local PDet = { conn=nil, cd=0.4, cds={} }
local PremiumUnlocked = false
local PremiumActive   = false
local EnteredKey      = ""
local Conn            = {}



-- ══════════════════════════════════════════════════════════════════
-- BALL / PLAYER HELPERS
-- ══════════════════════════════════════════════════════════════════
local function GetBall()
    local folder = workspace:FindFirstChild("Balls")
    if folder then
        for _,v in ipairs(folder:GetChildren()) do
            if v:IsA("BasePart") and v:GetAttribute("realBall") then return v end
        end
        for _,v in ipairs(folder:GetChildren()) do
            if v:IsA("BasePart") then return v end
        end
    end
    for _,v in ipairs(workspace:GetChildren()) do
        if v:IsA("BasePart") and v.Name:lower():find("ball") then return v end
    end
    return nil
end

local function GetHRP()
    local c=Plr.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end

local function GetVel(ball)
    local best = Vector3.zero
    local ok1,v1 = pcall(function() return ball.AssemblyLinearVelocity end)
    if ok1 and v1.Magnitude>best.Magnitude then best=v1 end
    pcall(function()
        for _,d in ipairs(ball:GetChildren()) do
            if (d:IsA("BodyVelocity") or d:IsA("LinearVelocity"))
                and d.VectorVelocity.Magnitude>best.Magnitude then
                best=d.VectorVelocity
            end
        end
    end)
    return best
end

-- ══════════════════════════════════════════════════════════════════
-- PING SAMPLER
--   PingMs  = lowest sample in window  → used for parry compensation
--   PingRaw = exact latest reading     → shown on HUD (real ms)
-- ══════════════════════════════════════════════════════════════════
local PingMs      = 1    -- lowest (for parry math)
local PingRaw     = 0    -- real current ms (for HUD display)
local PingBonus   = 0
local PingScale   = 1.0
local PingSamples = {}
local PingJitter  = 0

task.spawn(function()
    while true do
        local ok, raw = pcall(function()
            return Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
        end)
        if ok and type(raw)=="number" and raw>0 then
            PingRaw = raw  -- always store the unmodified reading for HUD

            table.insert(PingSamples, raw)
            if #PingSamples>12 then table.remove(PingSamples,1) end

            -- PingMs = lowest sample in window (keeps parry compensation tight)
            local lowest = math.huge
            for _,v in ipairs(PingSamples) do
                if v < lowest then lowest = v end
            end
            PingMs = lowest

            -- Jitter = mean absolute deviation of last 4 samples
            if #PingSamples>=4 then
                local n = #PingSamples
                local recent = {PingSamples[n], PingSamples[n-1], PingSamples[n-2], PingSamples[n-3]}
                local jSum=0; for _,v in ipairs(recent) do jSum=jSum+v end
                local jMean=jSum/4
                local jDev=0; for _,v in ipairs(recent) do jDev=jDev+math.abs(v-jMean) end
                PingJitter = jDev/4
            end

            -- PingBonus uses lowest ping for fastest possible compensation
            PingBonus = (PingMs/1000) * PingScale
        end
        task.wait(0.5)
    end
end)

local PingLabel = nil
task.spawn(function()
    while true do
        task.wait(1)
        if PingLabel then
            pcall(function()
                local col = PingMs<60 and Color3.fromRGB(34,197,110)
                         or PingMs<120 and Color3.fromRGB(255,200,50)
                         or Color3.fromRGB(255,80,80)
                local realMs = math.floor(PingRaw > 0 and PingRaw or PingMs)
                local col2 = realMs<60 and Color3.fromRGB(34,197,110)
                          or realMs<120 and Color3.fromRGB(255,200,50)
                          or Color3.fromRGB(255,80,80)
                PingLabel.Text      = ("Real: %dms  |  Comp: +%.3fs"):format(realMs, PingBonus)
                PingLabel.TextColor3= col2
            end)
        end
    end
end)

-- ══════════════════════════════════════════════════════════════════
-- PARRY STATS  ◈  Success rate tracker + HUD integration
-- ══════════════════════════════════════════════════════════════════
local ParryStats = {
    total          = 0,
    hits           = 0,
    lastBallTarget = "",
    lastFireTime   = 0,
}

local function TrackParryHit(ballTarget)
    -- Ball stopped targeting us within 0.4s of a parry = confirmed hit
    if ballTarget ~= Plr.Name and ParryStats.lastBallTarget == Plr.Name then
        if os.clock() - ParryStats.lastFireTime < 0.4 then
            ParryStats.hits = ParryStats.hits + 1
        end
    end
    ParryStats.lastBallTarget = ballTarget or ""
end

local function ParrySuccessRate()
    if ParryStats.total == 0 then return 100 end
    return math.floor((ParryStats.hits / ParryStats.total) * 100)
end

-- ══════════════════════════════════════════════════════════════════
-- PARRY FIRE HELPERS  ◈  Anti-stagger + accurate hit tracking
-- ══════════════════════════════════════════════════════════════════

-- Anti-stagger: track the exact ball instance last parried — never double-fire same ball
local FreeLastParriedBall = nil
local PremLastParriedBall = nil

-- Anti-spam: tracks last fire time and suppresses rapid duplicate fires
-- Prevents double-click spam and engine looping on the same close-range ball
local FREE_ANTI_SPAM_CD = 0.08  -- minimum 80ms between any two free fires

local function FireParry(ball)
    if S.DidParry then return end
    if os.clock() - S.LastParry < S.ParryCooldown then return end
    if os.clock() - S.LastParry < FREE_ANTI_SPAM_CD then return end  -- anti-spam gate
    if ball and ball == FreeLastParriedBall then return end  -- anti-stagger

    VIM:SendMouseButtonEvent(0,0,0,true,  game, 0)
    S.LastParry             = os.clock()
    S.DidParry              = true
    S.ParryCount            = S.ParryCount + 1
    ParryStats.total        = ParryStats.total + 1
    ParryStats.lastFireTime = os.clock()
    FreeLastParriedBall     = ball

    task.spawn(function()
        task.wait(0.015)
        VIM:SendMouseButtonEvent(0,0,0,false, game, 0)
    end)
    task.delay(S.ParryReset or 0.16, function()
        S.DidParry          = false
        FreeLastParriedBall = nil
    end)
end

-- Premium adaptive cooldown — shrinks when ball is fast, grows when slow
local PremCooldown      = 0.15
local PremCooldownBase  = 0.15   -- floor
local PremCooldownMax   = 0.22   -- ceiling
local PremLastParry     = 0
local PremDidParry      = false

local PremConn = {}  -- connection table for premium engine (shared scope)
local PremSpeedWarnThreshold = 160
local PremLastSpeedWarn      = 0

local function AdaptPremCooldown(speed)
    -- Fast ball = tighter cooldown, slow ball = looser
    if     speed > 200 then PremCooldown = PremCooldownBase
    elseif speed > 140 then PremCooldown = PremCooldownBase + 0.02
    elseif speed >  80 then PremCooldown = PremCooldownBase + 0.04
    else                    PremCooldown = PremCooldownMax
    end
end

local function CheckBallSpeedWarning(speed)
    if not PremiumUnlocked then return end
    local now = os.clock()
    if speed >= PremSpeedWarnThreshold and now - PremLastSpeedWarn > 2.5 then
        PremLastSpeedWarn = now
        -- notification removed (caused frame drops)
    end
end

local function PremiumFire(ball)
    local now = os.clock()
    if PremDidParry then return end
    if now - PremLastParry < PremCooldown then return end
    if ball and ball == PremLastParriedBall then return end  -- anti-stagger

    VIM:SendMouseButtonEvent(0,0,0,true,  game, 0)
    PremLastParry           = now
    PremDidParry            = true
    S.ParryCount            = S.ParryCount + 1
    ParryStats.total        = ParryStats.total + 1
    ParryStats.lastFireTime = now
    PremLastParriedBall     = ball

    task.spawn(function()
        task.wait(0.015)
        VIM:SendMouseButtonEvent(0,0,0,false, game, 0)
    end)
    task.delay(PremCooldown * 0.70, function()
        PremDidParry        = false
        PremLastParriedBall = nil
    end)
end

-- ══════════════════════════════════════════════════════════════════
-- VISUAL PARRY TIMING INDICATOR  ◈  Shared HUD bar
-- ══════════════════════════════════════════════════════════════════
local ParryIndicatorGui = nil
local ParryIndicatorBar = nil
local ParryIndicatorLbl = nil

local function BuildParryIndicator()
    if ParryIndicatorGui then pcall(function() ParryIndicatorGui:Destroy() end) end
    local sg = Instance.new("ScreenGui")
    sg.Name="RyuuParryInd"; sg.ResetOnSpawn=false; sg.IgnoreGuiInset=true; sg.DisplayOrder=9996
    pcall(function() sg.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not sg.Parent then pcall(function() sg.Parent=Plr:WaitForChild("PlayerGui") end) end
    ParryIndicatorGui = sg

    -- Outer container — bottom center
    local Container = Instance.new("Frame", sg)
    Container.AnchorPoint = Vector2.new(0.5,1)
    Container.Position    = UDim2.new(0.5,0,1,-18)
    Container.Size        = UDim2.new(0,220,0,36)
    Container.BackgroundColor3 = Color3.fromRGB(5,3,12)
    Container.BackgroundTransparency = 0.2
    Container.BorderSizePixel = 0
    Instance.new("UICorner",Container).CornerRadius = UDim.new(0,10)
    local CStroke = Instance.new("UIStroke",Container)
    CStroke.Thickness=1.2; CStroke.Color=Color3.fromRGB(60,25,120); CStroke.Transparency=0.3

    -- Label
    local Lbl = Instance.new("TextLabel",Container)
    Lbl.Size=UDim2.new(1,-8,0,14); Lbl.Position=UDim2.new(0,6,0,3)
    Lbl.BackgroundTransparency=1; Lbl.Text="PARRY TIMING"
    Lbl.Font=Enum.Font.GothamBold; Lbl.TextSize=9
    Lbl.TextColor3=Color3.fromRGB(100,60,200); Lbl.TextXAlignment=Enum.TextXAlignment.Left
    ParryIndicatorLbl = Lbl

    -- Bar background
    local BarBG = Instance.new("Frame",Container)
    BarBG.Size=UDim2.new(1,-12,0,8); BarBG.Position=UDim2.new(0,6,0,20)
    BarBG.BackgroundColor3=Color3.fromRGB(15,8,35); BarBG.BorderSizePixel=0
    Instance.new("UICorner",BarBG).CornerRadius=UDim.new(1,0)

    -- Fill bar
    local Fill = Instance.new("Frame",BarBG)
    Fill.Size=UDim2.new(0,0,1,0); Fill.BackgroundColor3=Color3.fromRGB(130,50,255)
    Fill.BorderSizePixel=0; Fill.ZIndex=2
    Instance.new("UICorner",Fill).CornerRadius=UDim.new(1,0)
    local FillGrad=Instance.new("UIGradient",Fill)
    FillGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(80,20,200)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(200,100,255)),
    }
    ParryIndicatorBar = Fill

    -- Fire flash frame (flashes green on parry)
    local Flash = Instance.new("Frame",Container)
    Flash.Name="Flash"; Flash.Size=UDim2.new(1,0,1,0); Flash.Position=UDim2.new(0,0,0,0)
    Flash.BackgroundColor3=Color3.fromRGB(50,255,120); Flash.BackgroundTransparency=1
    Flash.BorderSizePixel=0; Flash.ZIndex=5
    Instance.new("UICorner",Flash).CornerRadius=UDim.new(0,10)
end

-- Update indicator each frame — call from inside engine loop
local function UpdateParryIndicator(dist, fireDist, didJustFire)
    if not ParryIndicatorBar then return end
    pcall(function()
        local ratio = math.clamp(1-(dist-fireDist)/math.max(fireDist,1), 0, 1)
        local col = ratio > 0.85 and Color3.fromRGB(50,255,120)
                 or ratio > 0.55 and Color3.fromRGB(255,200,50)
                 or Color3.fromRGB(130,50,255)
        TweenService:Create(ParryIndicatorBar,
            TweenInfo.new(0.06, Enum.EasingStyle.Linear),
            {Size=UDim2.new(ratio,0,1,0), BackgroundColor3=col}):Play()

        if didJustFire then
            local flash = ParryIndicatorGui and ParryIndicatorGui:FindFirstChild("Frame") and
                ParryIndicatorGui.Frame:FindFirstChild("Flash")
            if flash then
                flash.BackgroundTransparency = 0.55
                TweenService:Create(flash, TweenInfo.new(0.18),
                    {BackgroundTransparency=1}):Play()
            end
            if ParryIndicatorLbl then
                ParryIndicatorLbl.Text = "✓ PARRY FIRED  —  " .. ParrySuccessRate() .. "% hit rate"
                ParryIndicatorLbl.TextColor3 = Color3.fromRGB(50,220,110)
                task.delay(0.6, function()
                    pcall(function()
                        ParryIndicatorLbl.Text = "PARRY TIMING"
                        ParryIndicatorLbl.TextColor3 = Color3.fromRGB(100,60,200)
                    end)
                end)
            end
        end
    end)
end

-- ══════════════════════════════════════════════════════════════════
-- FREE PARRY ENGINE (Upgraded - Improved for Reliability)
local FREE_PING_LEAD = 0.95  -- Slightly increased lead for better ping compensation
local FREE_HITBOX = 6.5     -- Slightly larger base hitbox for good performance
local PrevVel = nil
local SpeedHistory = {}     -- Added basic speed history for minor accel awareness
local function StartEngine()
    if Conn.Parry then
        Conn.Parry:Disconnect()
    end
    BuildParryIndicator()
    Conn.Parry = RunService.PreSimulation:Connect(function()
        if not S.AutoParry then return end
        local Ball = GetBall()
        if not Ball then return end
        local HRP = GetHRP()
        if not HRP then return end
        local BPos = Ball.Position
        local HPos = HRP.Position
        local Dist = (HPos - BPos).Magnitude
        local tgt = Ball:GetAttribute("target") or ""
        local isTgt = tgt == Plr.Name
        if not isTgt then
            UpdateParryIndicator(99,1,false)
            return
        end
        local Vel = GetVel(Ball)
        local Speed = Vel.Magnitude
        -- Basic speed history for minor improvements
        table.insert(SpeedHistory, Speed)
        if #SpeedHistory > 3 then  -- Shorter history for free version
            table.remove(SpeedHistory, 1)
        end
        local accel = 0
        if #SpeedHistory >= 3 then
            accel = SpeedHistory[#SpeedHistory] - SpeedHistory[#SpeedHistory-2]
        end
        local predictedSpeed = Speed + math.max(accel * 0.5, 0)  -- Toned down accel impact
        local toPlayer = HPos - BPos
        local dotVal = Vel:Dot(toPlayer)
        -- Allow slight sideways movement, but tightened for better fake detection
        local isClosing = dotVal > -(Speed * 0.20)
        -- Basic curve detection (minimal for free)
        local curveBonus = 1
        if PrevVel then
            local angle = Vel.Unit:Dot(PrevVel.Unit)
            if angle < 0.96 then
                curveBonus = 1.08  -- Small bonus
            end
        end
        PrevVel = Vel
        -- Dynamic hitbox
        local dynamicHitbox =
            FREE_HITBOX +
            math.clamp(predictedSpeed / 180, 0, 1) * 2.5  -- Slightly improved scaling
        dynamicHitbox = dynamicHitbox * curveBonus
        local pingLead =
            (PingRaw / 1000) *
            predictedSpeed *
            FREE_PING_LEAD
        pingLead = math.min(pingLead, 3.2)  -- Slightly higher cap
        local fireDist = dynamicHitbox + pingLead
        UpdateParryIndicator(Dist, fireDist, false)
        if isClosing and Dist <= fireDist then
            FireParry(Ball)
            UpdateParryIndicator(Dist, fireDist, true)
            return
        end
        -- Basic lookahead with minor improvements
        if Speed > 75 then  -- Lower threshold for activation
            local dt = 1/60
            for step=1,3 do  -- Increased to 3 steps for better coverage
                local nextPos = BPos + Vel * (dt * step)
                if (HPos - nextPos).Magnitude <= fireDist then
                    FireParry(Ball)
                    UpdateParryIndicator(Dist, fireDist, true)
                    return
                end
            end
        end
        -- Basic panic for very close balls (added for reliability)
        local panicDist = math.clamp(2.5 + Speed * 0.003, 2.5, 4)
        if Dist <= panicDist then
            FireParry(Ball)
            UpdateParryIndicator(Dist, panicDist, true)
            return
        end
    end)
end
-- ══════════════════════════════════════════════════════════════════
-- PREMIUM PARRY ENGINE (Advanced - Enhanced Accuracy, Sensitivity, Precision, and Detection)
local PREM_PING_LEAD = 0.85  -- Higher lead for more precise ping compensation
local PrevVel = nil
local SpeedHistory = {}
local AngleHistory = {}     -- Added for better curve tracking
local TargetHistory = {}    -- Added for fake target detection
local function StartPremiumEngine()
    if PremConn.Parry then
        PremConn.Parry:Disconnect()
    end
    BuildParryIndicator()
    PremConn.Parry = RunService.PreSimulation:Connect(function()
        if not P.AutoParry then return end
        local Ball = GetBall()
        if not Ball then return end
        local HRP = GetHRP()
        if not HRP then return end
        local BPos = Ball.Position
        local HPos = HRP.Position
        local Dist = (HPos - BPos).Magnitude
        local tgt = Ball:GetAttribute("target") or ""
        local isTgt = tgt == Plr.Name
        -- Target history for fake detection
        table.insert(TargetHistory, tgt)
        if #TargetHistory > 10 then
            table.remove(TargetHistory, 1)
        end
        local fakeTargetSuspect = false
        if #TargetHistory >= 3 then
            -- Detect recent target changes as potential fakes
            if TargetHistory[#TargetHistory] == Plr.Name and TargetHistory[#TargetHistory-1] ~= Plr.Name then
                fakeTargetSuspect = true
            end
        end
        if not isTgt and not fakeTargetSuspect then
            UpdateParryIndicator(99,1,false)
            return
        end
        local Vel = GetVel(Ball)
        local Speed = Vel.Magnitude
        -- Store speed history
        table.insert(SpeedHistory, Speed)
        if #SpeedHistory > 10 then  -- Longer history for better accel
            table.remove(SpeedHistory, 1)
        end
        -- Acceleration prediction with smoothing
        local accel = 0
        if #SpeedHistory >= 4 then
            local recentAccel1 = SpeedHistory[#SpeedHistory] - SpeedHistory[#SpeedHistory-1]
            local recentAccel2 = SpeedHistory[#SpeedHistory-1] - SpeedHistory[#SpeedHistory-2]
            accel = (recentAccel1 + recentAccel2) / 2  -- Averaged for precision
        end
        local predictedSpeed = Speed + math.max(accel, 0) * 1.2  -- Amplified accel for sensitivity
        local toPlayer = HPos - BPos
        local dotVal = Vel:Dot(toPlayer)
        local isClosing = dotVal > -(Speed * 0.18)  -- More sensitive closing threshold
        -- Advanced curve detection with history
        local curveBonus = 1
        if PrevVel then
            local angle = Vel.Unit:Dot(PrevVel.Unit)
            table.insert(AngleHistory, angle)
            if #AngleHistory > 5 then
                table.remove(AngleHistory, 1)
            end
            local avgAngle = 0
            for _, a in ipairs(AngleHistory) do
                avgAngle = avgAngle + a
            end
            avgAngle = avgAngle / #AngleHistory
            if avgAngle < 0.92 then
                curveBonus = 1.25  -- Higher bonus for strong curves
            elseif avgAngle < 0.95 then
                curveBonus = 1.15  -- Medium bonus
            elseif avgAngle < 0.98 then
                curveBonus = 1.08  -- Low bonus
            end
        end
        PrevVel = Vel
        -- Dynamic hitbox with more factors
        local baseHitbox = P.HitboxTarget or 4
        local dynamicHitbox =
            baseHitbox +
            math.clamp(predictedSpeed / 200, 0, 1) * 3.5  -- Faster scaling for precision
        dynamicHitbox = dynamicHitbox * curveBonus
        if fakeTargetSuspect then
            dynamicHitbox = dynamicHitbox * 1.1  -- Slight increase for suspected fakes
        end
        -- Ping lead with more precision
        local pingLead =
            (PingRaw / 1000) *
            predictedSpeed *
            PREM_PING_LEAD
        pingLead = math.min(pingLead, 3)  -- Balanced cap
        local fireDist = dynamicHitbox + pingLead
        UpdateParryIndicator(Dist, fireDist, false)
        -- Enhanced panic protection with fake detection
        local panicDist = math.clamp(2 + Speed * 0.005, 2, 4)  -- More sensitive to speed
        if fakeTargetSuspect then
            panicDist = panicDist * 1.2  -- Larger panic for fakes
        end
        if Dist <= panicDist then
            PremiumFire(Ball)
            UpdateParryIndicator(Dist, panicDist, true)
            return
        end
        -- Normal fire with increased sensitivity
        if isClosing and Dist <= fireDist then
            PremiumFire(Ball)
            UpdateParryIndicator(Dist, fireDist, true)
            return
        end
        -- Advanced prediction with more frames and curve adjustment
        if Speed > 60 then  -- Lower activation for more detection
            local dt = 1/60
            local maxFrames =
                Speed > 280 and 8
                or Speed > 200 and 7
                or Speed > 150 and 6
                or Speed > 100 and 5
                or 4  -- More frames overall for precision
            for step=1,maxFrames do
                -- Basic curve adjustment in prediction (assume slight bend)
                local curveAdjust = (1 - curveBonus) * step * 0.05  -- Minor position tweak
                local nextPos = BPos + Vel * (dt * step) + Vector3.new(curveAdjust, 0, curveAdjust)  -- Example adjustment
                if (HPos - nextPos).Magnitude <= fireDist then
                    PremiumFire(Ball)
                    UpdateParryIndicator(Dist, fireDist, true)
                    return
                end
            end
        end
    end)
end
-- ══════════════════════════════════════════════════════════════════
-- ABILITY ESP  ◈  Upgraded: cleaner cards, warn flash, tier badge
-- ══════════════════════════════════════════════════════════════════
local AbilityBBs    = {}
local AbilityLastSeen={}

local BB_ABILITIES = {
    ["Infinity"]           ={tier="S",col=Color3.fromRGB(255,60,220), warn=true},
    ["Time Hole"]          ={tier="S",col=Color3.fromRGB(255,60,220), warn=true},
    ["Slash of Duality"]   ={tier="S",col=Color3.fromRGB(255,60,220), warn=true},
    ["Dribble"]            ={tier="S",col=Color3.fromRGB(255,60,220), warn=true},
    ["Dragon Spirit"]      ={tier="S",col=Color3.fromRGB(255,60,220), warn=true},
    ["Pull"]               ={tier="A",col=Color3.fromRGB(255,130,30), warn=true},
    ["Guardian Angel"]     ={tier="A",col=Color3.fromRGB(255,130,30), warn=false},
    ["Reaper"]             ={tier="A",col=Color3.fromRGB(255,130,30), warn=false},
    ["Slashes of Fury"]    ={tier="A",col=Color3.fromRGB(255,130,30), warn=true},
    ["Forcefield"]         ={tier="A",col=Color3.fromRGB(255,130,30), warn=false},
    ["Bunny Leap"]         ={tier="A",col=Color3.fromRGB(255,130,30), warn=false},
    ["Singularity"]        ={tier="A",col=Color3.fromRGB(255,130,30), warn=true},
    ["Invisibility"]       ={tier="B",col=Color3.fromRGB(80,220,120), warn=false},
    ["Titan Blade"]        ={tier="B",col=Color3.fromRGB(80,220,120), warn=false},
    ["Scopophobia"]        ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Blade Trap"]         ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Death Slash"]        ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Phantom"]            ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Swap"]               ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Hell Hook"]          ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Blink"]              ={tier="B",col=Color3.fromRGB(80,220,120), warn=false},
    ["Qi-Charge"]          ={tier="B",col=Color3.fromRGB(80,220,120), warn=false},
    ["Telekinesis"]        ={tier="B",col=Color3.fromRGB(80,220,120), warn=true},
    ["Thunder Dash"]       ={tier="B",col=Color3.fromRGB(80,220,120), warn=false},
    ["Flash Counter"]      ={tier="C",col=Color3.fromRGB(255,220,40), warn=false},
    ["Pulse"]              ={tier="C",col=Color3.fromRGB(255,220,40), warn=false},
    ["Raging Deflection"]  ={tier="C",col=Color3.fromRGB(255,220,40), warn=true},
    ["Freeze"]             ={tier="C",col=Color3.fromRGB(255,220,40), warn=true},
    ["Quantum Arena"]      ={tier="C",col=Color3.fromRGB(255,220,40), warn=true},
    ["Dash"]               ={tier="C",col=Color3.fromRGB(255,220,40), warn=false},
    ["Quasar"]             ={tier="C",col=Color3.fromRGB(255,220,40), warn=false},
    ["Freeze Trap"]        ={tier="D",col=Color3.fromRGB(160,160,160),warn=false},
    ["Martyrdom"]          ={tier="D",col=Color3.fromRGB(160,160,160),warn=true},
    ["Super Jump"]         ={tier="D",col=Color3.fromRGB(160,160,160),warn=false},
}

local TIER_COL = {
    S=Color3.fromRGB(255,60,220),
    A=Color3.fromRGB(255,130,30),
    B=Color3.fromRGB(80,220,120),
    C=Color3.fromRGB(255,220,40),
    D=Color3.fromRGB(160,160,160),
}

local function ScanAbility(plr)
    if not plr then return nil end
    local char=plr.Character
    local function match(s)
        if not s or #s<2 then return nil end
        if BB_ABILITIES[s] then return s end
        local sl=s:lower()
        for k in pairs(BB_ABILITIES) do
            if k:lower()==sl then return k end
        end
        return nil
    end
    for _,n in ipairs({"Ability","ability","AbilityName","EquippedAbility","Skill","CurrentAbility","equipped"}) do
        local ok,v=pcall(function() return plr:GetAttribute(n) end)
        if ok and type(v)=="string" then local r=match(v); if r then return r end end
    end
    if char then
        for _,n in ipairs({"Ability","ability","AbilityName","EquippedAbility","Skill","CurrentAbility"}) do
            local ok,v=pcall(function() return char:GetAttribute(n) end)
            if ok and type(v)=="string" then local r=match(v); if r then return r end end
        end
    end
    for _,v in ipairs(plr:GetDescendants()) do
        if v:IsA("StringValue") then
            local r=match(v.Value); if r then return r end
            local nl=v.Name:lower()
            if (nl:find("ability") or nl:find("skill") or nl:find("equip")) and #v.Value>0 then return v.Value end
        end
    end
    if char then
        for _,v in ipairs(char:GetDescendants()) do
            if v:IsA("StringValue") then
                local r=match(v.Value); if r then return r end
            end
        end
        for _,v in ipairs(char:GetChildren()) do
            if v:IsA("Tool") then return match(v.Name) or v.Name end
        end
    end
    return nil
end

local function BuildAbilityBB(plr)
    if not (plr and plr.Character) then return end
    local head=plr.Character:FindFirstChild("Head"); if not head then return end
    if AbilityBBs[plr] and AbilityBBs[plr].Parent then
        pcall(function() AbilityBBs[plr]:Destroy() end)
    end
    AbilityLastSeen[plr]=nil

    local bb=Instance.new("BillboardGui")
    bb.Name="RyuuAbilESP"; bb.AlwaysOnTop=true
    bb.Size=UDim2.new(0,152,0,42); bb.StudsOffset=Vector3.new(0,3.2,0)
    bb.MaxDistance=100; bb.ClipsDescendants=false; bb.Parent=head

    -- Card background
    local bg=Instance.new("Frame",bb)
    bg.Size=UDim2.new(1,0,1,0)
    bg.BackgroundColor3=Color3.fromRGB(7,4,16)
    bg.BackgroundTransparency=0.12; bg.BorderSizePixel=0
    Instance.new("UICorner",bg).CornerRadius=UDim.new(0,10)

    -- Top color accent bar
    local accent=Instance.new("Frame",bg)
    accent.Name="Accent"
    accent.Size=UDim2.new(1,0,0,2); accent.Position=UDim2.new(0,0,0,0)
    accent.BackgroundColor3=Color3.fromRGB(120,40,240); accent.BorderSizePixel=0
    Instance.new("UICorner",accent).CornerRadius=UDim.new(0,10)

    local stroke=Instance.new("UIStroke",bg)
    stroke.Name="S"; stroke.Thickness=1.2
    stroke.Color=Color3.fromRGB(55,28,100); stroke.Transparency=0

    -- Player name
    local nLbl=Instance.new("TextLabel",bg); nLbl.Name="PN"
    nLbl.Size=UDim2.new(1,-36,0,14); nLbl.Position=UDim2.new(0,5,0,4)
    nLbl.BackgroundTransparency=1; nLbl.Text=plr.Name
    nLbl.TextColor3=Color3.fromRGB(140,130,175); nLbl.Font=Enum.Font.GothamBold
    nLbl.TextSize=10; nLbl.TextXAlignment=Enum.TextXAlignment.Left
    nLbl.TextTruncate=Enum.TextTruncate.AtEnd

    -- Tier badge
    local badge=Instance.new("TextLabel",bg); badge.Name="TB"
    badge.Size=UDim2.new(0,26,0,14); badge.Position=UDim2.new(1,-30,0,4)
    badge.BackgroundColor3=Color3.fromRGB(25,12,50); badge.BorderSizePixel=0
    badge.Text="?"; badge.TextColor3=Color3.fromRGB(170,90,255)
    badge.Font=Enum.Font.GothamBlack; badge.TextSize=10
    Instance.new("UICorner",badge).CornerRadius=UDim.new(0,5)

    -- Ability name
    local aLbl=Instance.new("TextLabel",bg); aLbl.Name="AN"
    aLbl.Size=UDim2.new(1,-10,0,20); aLbl.Position=UDim2.new(0,5,0,18)
    aLbl.BackgroundTransparency=1; aLbl.Text="Scanning..."
    aLbl.TextColor3=Color3.fromRGB(200,200,200); aLbl.Font=Enum.Font.GothamBold
    aLbl.TextSize=13; aLbl.TextXAlignment=Enum.TextXAlignment.Left
    aLbl.TextTruncate=Enum.TextTruncate.AtEnd

    AbilityBBs[plr]=bb
end

local warnFlashConn = {}

local function RefreshBB(plr)
    if not (plr and plr.Character) then return end
    local bb=AbilityBBs[plr]
    if not (bb and bb.Parent) then BuildAbilityBB(plr); bb=AbilityBBs[plr] end
    if not bb then return end
    local bg=bb:FindFirstChildOfClass("Frame"); if not bg then return end
    local aLbl=bg:FindFirstChild("AN"); if not aLbl then return end
    local badge=bg:FindFirstChild("TB")
    local stk=bg:FindFirstChild("S")
    local accent=bg:FindFirstChild("Accent")

    local name=ScanAbility(plr)
    local cacheKey=name or "__none__"
    if AbilityLastSeen[plr]==cacheKey then return end
    AbilityLastSeen[plr]=cacheKey

    if name then
        local data=BB_ABILITIES[name]
        if data then
            local col=data.warn and Color3.fromRGB(255,65,65) or data.col
            aLbl.Text=(data.warn and "⚠  " or "")..name
            aLbl.TextColor3=col
            if badge  then badge.Text=data.tier; badge.TextColor3=TIER_COL[data.tier] or Color3.fromRGB(170,90,255) end
            if stk    then stk.Color=data.warn and Color3.fromRGB(190,40,40) or data.col end
            if accent then accent.BackgroundColor3=col end

            -- Flash animation for warn abilities
            if data.warn then
                if warnFlashConn[plr] then warnFlashConn[plr]:Disconnect() end
                local flashOn=true
                warnFlashConn[plr]=RunService.Heartbeat:Connect(function()
                    if not (bg and bg.Parent) then
                        warnFlashConn[plr]:Disconnect(); warnFlashConn[plr]=nil; return
                    end
                    pcall(function()
                        bg.BackgroundTransparency=flashOn and 0.08 or 0.35
                        flashOn=not flashOn
                    end)
                    task.wait(0.4)
                end)
            else
                if warnFlashConn[plr] then
                    warnFlashConn[plr]:Disconnect(); warnFlashConn[plr]=nil
                end
                pcall(function() bg.BackgroundTransparency=0.12 end)
            end
        else
            aLbl.Text=name; aLbl.TextColor3=Color3.fromRGB(200,200,200)
            if badge  then badge.Text="?" end
            if stk    then stk.Color=Color3.fromRGB(55,28,100) end
            if accent then accent.BackgroundColor3=Color3.fromRGB(80,40,180) end
        end
    else
        aLbl.Text="No Ability"; aLbl.TextColor3=Color3.fromRGB(80,75,100)
        if badge  then badge.Text="-" end
        if stk    then stk.Color=Color3.fromRGB(30,15,50) end
        if accent then accent.BackgroundColor3=Color3.fromRGB(30,15,50) end
        if warnFlashConn[plr] then warnFlashConn[plr]:Disconnect(); warnFlashConn[plr]=nil end
        pcall(function() bg.BackgroundTransparency=0.12 end)
    end
end

local function CleanAbilityESP()
    for _,bb in pairs(AbilityBBs) do
        pcall(function() if bb and bb.Parent then bb:Destroy() end end)
    end
    for p,c in pairs(warnFlashConn) do
        pcall(function() c:Disconnect() end)
        warnFlashConn[p]=nil
    end
    AbilityBBs={}; AbilityLastSeen={}
end

local function StartAbilityESP()
    CleanAbilityESP()
    for _,p in ipairs(Players:GetPlayers()) do
        if p~=Plr then BuildAbilityBB(p); RefreshBB(p) end
    end
    task.spawn(function()
        while S.AbilityESP do
            for _,p in ipairs(Players:GetPlayers()) do
                if p~=Plr and p.Character then
                    local head=p.Character:FindFirstChild("Head")
                    if head then
                        local bb=AbilityBBs[p]
                        if not bb or not bb.Parent or bb.Parent~=head then BuildAbilityBB(p) end
                        RefreshBB(p)
                    end
                end
            end
            for p,bb in pairs(AbilityBBs) do
                if not p.Parent then
                    pcall(function() if bb and bb.Parent then bb:Destroy() end end)
                    AbilityBBs[p]=nil
                end
            end
            task.wait(0.75)
        end
    end)
    for _,p in ipairs(Players:GetPlayers()) do
        if p~=Plr then
            p.CharacterAdded:Connect(function()
                task.wait(0.5)
                if S.AbilityESP then BuildAbilityBB(p); RefreshBB(p) end
            end)
        end
    end
    Players.PlayerAdded:Connect(function(p)
        if not S.AbilityESP then return end
        p.CharacterAdded:Connect(function()
            task.wait(0.5)
            if S.AbilityESP then BuildAbilityBB(p); RefreshBB(p) end
        end)
    end)
end

-- ══════════════════════════════════════════════════════════════════
-- ABILITY DETECTOR  ◈  Free + Premium, throttled to 20 Hz
-- ══════════════════════════════════════════════════════════════════
local Det={conn=nil,pconn=nil,cds={},cd=0.4}

local FREE_SIGS={
    {name="Slashes of Fury",check=function(char,plr)
        if not char then return false end
        local hum=char:FindFirstChildOfClass("Humanoid")
        if hum then
            local anim=hum:FindFirstChildOfClass("Animator")
            if anim then
                for _,t in ipairs(anim:GetPlayingAnimationTracks()) do
                    local n=t.Name:lower()
                    if n:find("fury") or n:find("slash") or n:find("barrage") then return true end
                end
            end
        end
        for _,v in ipairs(char:GetDescendants()) do
            if v:IsA("ParticleEmitter") then
                local n=v.Name:lower()
                if n:find("fury") or n:find("slash") then return true end
            end
        end
        return false
    end},
    {name="Pull",check=function(char,plr)
        if not char then return false end
        local aa=plr:GetAttribute("AbilityActive")
        if aa and tostring(aa):lower():find("pull") then return true end
        for _,v in ipairs(char:GetDescendants()) do
            if v:IsA("BasePart") and v.Name:lower():find("pull") then return true end
        end
        return false
    end},
    {name="Infinity",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            if (v:IsA("ParticleEmitter") or v:IsA("SelectionBox") or v:IsA("BasePart"))
                and v.Name:lower():find("infinity") then return true end
        end
        return false
    end},
}

local PREM_SIGS={
    {name="Slashes of Fury",check=function(char,plr)
        if not char then return false end
        local hum=char:FindFirstChildOfClass("Humanoid")
        if hum then
            local anim=hum:FindFirstChildOfClass("Animator")
            if anim then
                for _,t in ipairs(anim:GetPlayingAnimationTracks()) do
                    local n=t.Name:lower()
                    if n:find("fury") or n:find("slash") or n:find("barrage") or n:find("multi") then return true end
                end
            end
        end
        for _,v in ipairs(char:GetDescendants()) do
            local n=v.Name:lower()
            if v:IsA("ParticleEmitter") and (n:find("fury") or n:find("slash")) then return true end
            if v:IsA("BasePart") and (n:find("slash") or n:find("fury") or n:find("hit")) then return true end
        end
        local aa=plr:GetAttribute("AbilityActive")
        if aa and tostring(aa):lower():find("fury") then return true end
        return false
    end},
    {name="Time Hole",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            local n=v.Name:lower()
            if (n:find("hole") or n:find("time") or n:find("vortex"))
                and (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
        end
        return false
    end},
    {name="Slash of Duality",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            local n=v.Name:lower()
            if (n:find("dual") or n:find("duality"))
                and (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
        end
        return false
    end},
    {name="Dragon Spirit",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            local n=v.Name:lower()
            if (n:find("dragon") or n:find("spirit") or n:find("charge"))
                and (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
        end
        return false
    end},
    {name="Pull",check=function(char,plr)
        if not char then return false end
        local aa=plr:GetAttribute("AbilityActive")
        if aa and tostring(aa):lower():find("pull") then return true end
        for _,v in ipairs(char:GetDescendants()) do
            if v:IsA("BasePart") and v.Name:lower():find("pull") then return true end
        end
        return false
    end},
    {name="Infinity",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            if (v:IsA("ParticleEmitter") or v:IsA("BasePart"))
                and v.Name:lower():find("infinity") then return true end
        end
        return false
    end},
    {name="Phantom",check=function(char,plr)
        if not char then return false end
        local hrp=char:FindFirstChild("HumanoidRootPart")
        return hrp and hrp.Transparency>0.4 or false
    end},
    {name="Hell Hook",check=function(char,plr)
        if not char then return false end
        local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return false end
        for _,v in ipairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") then
                local n=v.Name:lower()
                if (n:find("hook") or n:find("hell"))
                    and (v.Position-hrp.Position).Magnitude<25 then return true end
            end
        end
        return false
    end},
    {name="Thunder Dash",check=function(char,plr)
        if not char then return false end
        for _,v in ipairs(char:GetDescendants()) do
            local n=v.Name:lower()
            if v:IsA("ParticleEmitter")
                and (n:find("thunder") or n:find("dash") or n:find("lightning")) then return true end
        end
        return false
    end},
}

local function RunDetector(sigList, fireFn)
    local lastTick=0
    local conn
    conn=RunService.Heartbeat:Connect(function()
        local now=os.clock()
        if now-lastTick<0.05 then return end  -- throttle to 20 Hz
        lastTick=now
        pcall(function()
            local ball=GetBall(); if not ball then return end
            local tgt=ball:GetAttribute("target"); if not tgt then return end
            local myHRP=GetHRP(); if not myHRP then return end
            for _,p in ipairs(Players:GetPlayers()) do
                if p==Plr then continue end
                local char=p.Character; if not char then continue end
                local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then continue end
                if (myHRP.Position-hrp.Position).Magnitude>100 then continue end
                local ballNearEnemy=(hrp.Position-ball.Position).Magnitude<20
                local ballTargetsMe=tgt==Plr.Name
                if not (ballNearEnemy or ballTargetsMe) then continue end
                local cdKey=p.Name
                if Det.cds[cdKey] and now-Det.cds[cdKey]<Det.cd then continue end
                for _,sig in ipairs(sigList) do
                    if sig.check(char,p) then
                        Det.cds[cdKey]=now
                        fireFn()  -- no notification — caused frame drops
                        break
                    end
                end
            end
        end)
    end)
    return conn
end

local function StartAbilDetect()
    if Det.conn then Det.conn:Disconnect() end
    Det.cds={}
    Det.conn=RunDetector(FREE_SIGS, FireParry)
end
local function StopAbilDetect()
    if Det.conn then Det.conn:Disconnect(); Det.conn=nil end
end
local function StartPremAbilDetect()
    if PDet.conn then PDet.conn:Disconnect() end
    Det.cds={}
    PDet.conn=RunDetector(PREM_SIGS, PremiumFire)
end
local function StopPremAbilDetect()
    if PDet.conn then PDet.conn:Disconnect(); PDet.conn=nil end
end

-- ══════════════════════════════════════════════════════════════════
-- MISC  (Fullbright, Noclip, Inf Jump, Ball ESP, Sword)
-- ══════════════════════════════════════════════════════════════════
local function SetFB(on)
    if on then
        Lighting.Brightness=2; Lighting.GlobalShadows=false
        Lighting.FogEnd=9e9; Lighting.EnvironmentDiffuseScale=1
    else
        Lighting.Brightness=1; Lighting.GlobalShadows=true
        Lighting.FogEnd=100000; Lighting.EnvironmentDiffuseScale=1
    end
end

local function StartNoclip()
    RunService.Stepped:Connect(function()
        if not S.Noclip then return end
        local c=Plr.Character
        if c then for _,p in ipairs(c:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=false end
        end end
    end)
end

local function StartIJ()
    UIS.JumpRequest:Connect(function()
        if not S.InfJump then return end
        local c=Plr.Character
        local h=c and c:FindFirstChildOfClass("Humanoid")
        if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
    end)
end

local BallESPGui=nil
local function StartESP()
    if BallESPGui then pcall(function() BallESPGui:Destroy() end) end
    local sg=Instance.new("ScreenGui"); sg.Name="RyuuBallESP"
    sg.ResetOnSpawn=false; sg.IgnoreGuiInset=true; sg.DisplayOrder=9997
    pcall(function() sg.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not sg.Parent then pcall(function() sg.Parent=Plr:WaitForChild("PlayerGui") end) end
    BallESPGui=sg
    RunService.Heartbeat:Connect(function()
        if not S.BallESP then return end
        local ball=GetBall(); if not ball then return end
        local isTgt=ball:GetAttribute("target")==Plr.Name
        local pos,onScreen=Cam:WorldToViewportPoint(ball.Position)
        if not onScreen then return end
        local dot=sg:FindFirstChild("BallDot")
        if not dot then
            dot=Instance.new("Frame",sg); dot.Name="BallDot"
            dot.Size=UDim2.new(0,14,0,14); dot.AnchorPoint=Vector2.new(0.5,0.5)
            dot.BorderSizePixel=0; dot.ZIndex=5
            Instance.new("UICorner",dot).CornerRadius=UDim.new(1,0)
        end
        dot.Position=UDim2.new(0,pos.X,0,pos.Y)
        dot.BackgroundColor3=isTgt and Color3.fromRGB(255,50,50) or Color3.fromRGB(50,220,80)
    end)
end
local function CleanESP()
    if BallESPGui then pcall(function() BallESPGui:Destroy() end); BallESPGui=nil end
end

-- Sword changer
local SWORD_COLORS={
    Default=nil, Gold=Color3.fromRGB(255,200,40),
    ["Shadow Black"]=Color3.fromRGB(15,10,20), ["Crystal Blue"]=Color3.fromRGB(40,140,255),
    Crimson=Color3.fromRGB(200,30,30), ["Ghost White"]=Color3.fromRGB(230,230,255),
    ["Neon Purple"]=Color3.fromRGB(180,40,255), Emerald=Color3.fromRGB(20,200,80),
    ["Rose Gold"]=Color3.fromRGB(220,140,120), Obsidian=Color3.fromRGB(30,28,35),
    Cyan=Color3.fromRGB(0,220,220), Orange=Color3.fromRGB(255,120,20), Rainbow=nil,
}
local SWORD_MATS={
    Default=Enum.Material.SmoothPlastic, ["Neon Purple"]=Enum.Material.Neon,
    Emerald=Enum.Material.Neon, Cyan=Enum.Material.Neon, Rainbow=Enum.Material.Neon,
}
local CurrentSwordPreset="Default"
local SwordOrigColors={}
local RainbowHue=0

local function GetSwordParts()
    local char=Plr.Character; if not char then return nil,{} end
    local tool=nil
    for _,v in ipairs(char:GetChildren()) do if v:IsA("Tool") then tool=v; break end end
    if not tool then return nil,{} end
    local parts={}
    for _,v in ipairs(tool:GetDescendants()) do
        if v:IsA("BasePart") or v:IsA("UnionOperation") then parts[#parts+1]=v end
    end
    local h=tool:FindFirstChild("Handle"); if h then parts[#parts+1]=h end
    return tool,parts
end

local function ApplySwordPreset(name)
    CurrentSwordPreset=name
    local _,parts=GetSwordParts(); if #parts==0 then return end
    for _,p in ipairs(parts) do
        if not SwordOrigColors[p] then SwordOrigColors[p]={color=p.Color,mat=p.Material} end
    end
    if name=="Default" then
        for _,p in ipairs(parts) do
            local o=SwordOrigColors[p]
            if o then pcall(function() p.Color=o.color; p.Material=o.mat end) end
        end
        return
    end
    if name=="Rainbow" then
        RainbowHue=(RainbowHue+0.08)%1
        local col=Color3.fromHSV(RainbowHue,1,1)
        for _,p in ipairs(parts) do pcall(function() p.Color=col; p.Material=Enum.Material.Neon end) end
        return
    end
    local col=SWORD_COLORS[name]; if not col then return end
    local mat=SWORD_MATS[name] or Enum.Material.SmoothPlastic
    for _,p in ipairs(parts) do pcall(function() p.Color=col; p.Material=mat end) end
end

local function HookSwordApply(char)
    char.ChildAdded:Connect(function(v)
        if v:IsA("Tool") then task.wait(0.1); ApplySwordPreset(CurrentSwordPreset) end
    end)
end
Plr.CharacterAdded:Connect(function(char) task.wait(0.5); HookSwordApply(char) end)
if Plr.Character then HookSwordApply(Plr.Character) end


-- ══════════════════════════════════════════════════════════════════
-- PREMIUM FEATURES  ◈  State + All Functions
-- ══════════════════════════════════════════════════════════════════

-- ── PREMIUM STATE TABLE ───────────────────────────────────────────
local PF = {
    -- Parry extras
    ParrySound     = false,
    AntiBlock      = false,
    TimingRandom   = false,
    ComboTracker   = false,
    ComboCount     = 0,
    ComboLastParry = 0,
    AutoChat       = false,
    -- Prediction
    TrajectoryESP  = false,
    BallSpeedMeter = false,
    TimeToImpact   = false,
    TargetAlert    = false,
    -- ESP / Visual
    DistESP        = false,
    Chams          = false,
    BallHighlight  = false,
    RainbowTrail   = false,
    -- Movement
    AutoDodge      = false,
    SpeedOnParry   = false,
    SpeedOnParryAmt= 40,
    GhostMode      = false,
    -- HUD / UI
    PremFPS        = false,
    PremFPSCap     = 0,
    BallSpeedMeter = false,
    KillTracker    = false,
    KillCount      = 0,
    PremWatermark  = false,
    SafeZoneWarn   = false,
}

-- ── AUTO CHAT ──────────────────────────────────────────────────────
local AUTO_CHAT_MSGS = {
    "nice try 😏",
    "too slow ⚡",
    "skill issue 💯",
    "not even close 🎯",
    "touch grass 🌿",
    "L + ratio 😴",
}
local AutoChatIdx = 1
local AutoChatLastTime = 0

local function DoAutoChat()
    if not PF.AutoChat or not PremiumUnlocked then return end
    local now = os.clock()
    if now - AutoChatLastTime < 3 then return end  -- cooldown
    AutoChatLastTime = now
    local msg = AUTO_CHAT_MSGS[AutoChatIdx]
    AutoChatIdx = (AutoChatIdx % #AUTO_CHAT_MSGS) + 1
    pcall(function()
        local ts = game:GetService("TextService")
        local chatSvc = game:GetService("Chat")
        chatSvc:Chat(Plr.Character and Plr.Character:FindFirstChild("Head"), msg, Enum.ChatColor.White)
    end)
    -- Fallback: use StarterGui chat
    pcall(function()
        game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
            :FindFirstChild("SayMessageRequest"):FireServer(msg, "All")
    end)
end

-- ── KILL TRACKER WIDGET ────────────────────────────────────────────
local KillSG = nil
local KillLbl = nil

local function BuildKillWidget()
    if KillSG then pcall(function() KillSG:Destroy() end) end
    KillSG = Instance.new("ScreenGui")
    KillSG.Name = "RyuuKills"; KillSG.ResetOnSpawn = false
    KillSG.IgnoreGuiInset = true; KillSG.DisplayOrder = 9992
    pcall(function() KillSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not KillSG.Parent then pcall(function() KillSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    local KFrame = Instance.new("Frame", KillSG)
    KFrame.AnchorPoint = Vector2.new(1,0)
    KFrame.Position = UDim2.new(1,-14,0,130)
    KFrame.Size = UDim2.new(0,150,0,46)
    KFrame.BackgroundColor3 = Color3.fromRGB(5,12,20)
    KFrame.BackgroundTransparency = 0.2; KFrame.BorderSizePixel = 0
    Instance.new("UICorner",KFrame).CornerRadius = UDim.new(0,10)
    local KStr = Instance.new("UIStroke",KFrame)
    KStr.Thickness = 1.2; KStr.Color = Color3.fromRGB(255,60,60); KStr.Transparency = 0.3

    local KAccent = Instance.new("Frame",KFrame)
    KAccent.Size = UDim2.new(1,0,0,2); KAccent.BackgroundColor3 = Color3.fromRGB(255,60,60)
    KAccent.BorderSizePixel = 0
    Instance.new("UICorner",KAccent).CornerRadius = UDim.new(0,10)

    local KTitle = Instance.new("TextLabel",KFrame)
    KTitle.Size = UDim2.new(1,-8,0,13); KTitle.Position = UDim2.new(0,6,0,6)
    KTitle.BackgroundTransparency = 1; KTitle.Text = "☠  KILLS"
    KTitle.Font = Enum.Font.GothamBlack; KTitle.TextSize = 9
    KTitle.TextColor3 = Color3.fromRGB(255,80,80); KTitle.TextXAlignment = Enum.TextXAlignment.Left

    KillLbl = Instance.new("TextLabel",KFrame)
    KillLbl.Size = UDim2.new(1,-8,0,22); KillLbl.Position = UDim2.new(0,6,0,20)
    KillLbl.BackgroundTransparency = 1; KillLbl.Text = "0"
    KillLbl.Font = Enum.Font.GothamBlack; KillLbl.TextSize = 20
    KillLbl.TextColor3 = Color3.fromRGB(255,90,90); KillLbl.TextXAlignment = Enum.TextXAlignment.Left
    local KGrad = Instance.new("UIGradient",KillLbl)
    KGrad.Color = ColorSequence.new{ColorSequenceKeypoint.new(0,Color3.fromRGB(255,60,60)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(255,160,80))}
end

local function IncrementKill()
    if not PF.KillTracker or not PremiumUnlocked then return end
    PF.KillCount = PF.KillCount + 1
    pcall(function()
        if KillLbl then KillLbl.Text = tostring(PF.KillCount) end
    end)
    Rayfield:Notify({Title="☠ Kill Tracked",
        Content="Offensive parries: "..PF.KillCount,Duration=2,Image=4483362458})
end

-- ── TRAJECTORY ESP ─────────────────────────────────────────────────
local TrajSG = nil
local TrajConnActive = false

local function BuildTrajectoryESP()
    if TrajSG then pcall(function() TrajSG:Destroy() end) end
    TrajSG = Instance.new("ScreenGui")
    TrajSG.Name = "RyuuTraj"; TrajSG.ResetOnSpawn = false
    TrajSG.IgnoreGuiInset = true; TrajSG.DisplayOrder = 9989
    pcall(function() TrajSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not TrajSG.Parent then pcall(function() TrajSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    -- Neon line (solid) from ball position to predicted impact
    local Line = Instance.new("Frame", TrajSG)
    Line.BackgroundColor3 = Color3.fromRGB(255,60,60)
    Line.BorderSizePixel = 0; Line.ZIndex = 5; Line.Visible = false
    Instance.new("UICorner",Line).CornerRadius = UDim.new(1,0)
    -- Glow behind line
    local LineGlow = Instance.new("Frame", TrajSG)
    LineGlow.BackgroundColor3 = Color3.fromRGB(255,80,80)
    LineGlow.BackgroundTransparency = 0.7; LineGlow.BorderSizePixel = 0; LineGlow.ZIndex = 4; LineGlow.Visible = false
    Instance.new("UICorner",LineGlow).CornerRadius = UDim.new(1,0)
    -- Arrow tip triangle (just a rotated frame)
    local Arrow = Instance.new("Frame", TrajSG)
    Arrow.AnchorPoint = Vector2.new(0.5,0.5)
    Arrow.Size = UDim2.new(0,10,0,10); Arrow.BorderSizePixel = 0
    Arrow.BackgroundColor3 = Color3.fromRGB(255,60,60); Arrow.ZIndex = 6; Arrow.Visible = false
    Instance.new("UICorner",Arrow).CornerRadius = UDim.new(0,2)

    TrajConnActive = true
    RunService.Heartbeat:Connect(function()
        if not PF.TrajectoryESP or not PremiumUnlocked then
            Line.Visible = false; LineGlow.Visible = false; Arrow.Visible = false; return
        end
        local ball = GetBall(); if not ball then Line.Visible=false; LineGlow.Visible=false; Arrow.Visible=false; return end
        local hrp = GetHRP(); if not hrp then Line.Visible=false; LineGlow.Visible=false; Arrow.Visible=false; return end
        local vel = GetVel(ball); local spd = vel.Magnitude
        if spd < 2 then Line.Visible=false; LineGlow.Visible=false; Arrow.Visible=false; return end

        local isTgt = (ball:GetAttribute("target") or "") == Plr.Name
        local ahead = ball.Position + vel.Unit * math.min(spd * 0.5, 50)
        local sp1, on1 = Cam:WorldToViewportPoint(ball.Position)
        local sp2, on2 = Cam:WorldToViewportPoint(ahead)
        if not on1 then Line.Visible=false; LineGlow.Visible=false; Arrow.Visible=false; return end

        local dx = sp2.X - sp1.X; local dy = sp2.Y - sp1.Y
        local len = math.sqrt(dx*dx + dy*dy)
        if len < 3 then return end
        local rot = math.deg(math.atan2(dy, dx))
        local col = isTgt and Color3.fromRGB(255,50,50) or Color3.fromRGB(0,200,255)
        local glowCol = isTgt and Color3.fromRGB(255,100,100) or Color3.fromRGB(60,210,255)

        pcall(function()
            Line.Visible=true; Line.BackgroundColor3=col
            Line.Size=UDim2.new(0,len,0,2)
            Line.Position=UDim2.new(0,sp1.X,0,sp1.Y-1)
            Line.Rotation=rot
            LineGlow.Visible=true; LineGlow.BackgroundColor3=glowCol
            LineGlow.Size=UDim2.new(0,len,0,6)
            LineGlow.Position=UDim2.new(0,sp1.X,0,sp1.Y-3)
            LineGlow.Rotation=rot
            -- Arrow tip at end point
            Arrow.Visible=true; Arrow.BackgroundColor3=col
            Arrow.Position=UDim2.new(0,sp2.X,0,sp2.Y)
            Arrow.Rotation=rot+45
        end)
    end)
end

-- ── DIST ESP HEARTBEAT LOOP ────────────────────────────────────────
local DistESPBBs = {}

local function UpdateDistESP()
    -- Clean dead entries
    for plr,bb in pairs(DistESPBBs) do
        if not plr.Parent then
            pcall(function() bb:Destroy() end); DistESPBBs[plr]=nil
        end
    end
    if not PF.DistESP or not PremiumUnlocked then
        for _,bb in pairs(DistESPBBs) do pcall(function() bb:Destroy() end) end
        DistESPBBs = {}; return
    end
    local hrp = GetHRP(); if not hrp then return end
    for _,p in ipairs(Players:GetPlayers()) do
        if p == Plr then continue end
        local char = p.Character; if not char then continue end
        local head = char:FindFirstChild("Head"); if not head then continue end
        local pHRP = char:FindFirstChild("HumanoidRootPart"); if not pHRP then continue end
        if not DistESPBBs[p] or not DistESPBBs[p].Parent then
            local bb = Instance.new("BillboardGui")
            bb.Name="RyuuDistESP"; bb.AlwaysOnTop=true
            bb.Size=UDim2.new(0,90,0,18); bb.StudsOffset=Vector3.new(0,3,0)
            bb.MaxDistance=130; bb.Parent=head
            local lbl = Instance.new("TextLabel",bb)
            lbl.Name="D"; lbl.Size=UDim2.new(1,0,1,0); lbl.BackgroundTransparency=1
            lbl.Font=Enum.Font.GothamBold; lbl.TextSize=11; lbl.TextStrokeTransparency=0.5
            lbl.TextColor3=Color3.fromRGB(0,220,255)
            DistESPBBs[p] = bb
        end
        local dist = (hrp.Position - pHRP.Position).Magnitude
        local lbl = DistESPBBs[p]:FindFirstChild("D")
        if lbl then
            local col = dist<12 and Color3.fromRGB(255,70,70) or dist<35 and Color3.fromRGB(255,200,40) or Color3.fromRGB(0,220,255)
            lbl.Text = ("%.0f st  %s"):format(dist, p.Name)
            lbl.TextColor3 = col
        end
    end
end

RunService.Heartbeat:Connect(function()
    if PF.DistESP and PremiumUnlocked then UpdateDistESP() end
end)

-- ── CHAMS ──────────────────────────────────────────────────────────
local ChamParts = {}

local function ApplyChams(on)
    if not on then
        for part, data in pairs(ChamParts) do
            pcall(function()
                if part and part.Parent then
                    part.Material = data.mat; part.Color = data.col
                end
            end)
        end
        ChamParts = {}; return
    end
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= Plr and p.Character then
            for _,part in ipairs(p.Character:GetDescendants()) do
                if part:IsA("BasePart") and not ChamParts[part] then
                    ChamParts[part] = {mat=part.Material, col=part.Color}
                    pcall(function()
                        part.Material = Enum.Material.Neon
                        part.Color = Color3.fromRGB(0,200,255)
                    end)
                end
            end
        end
    end
end

-- New players auto-cham
Players.PlayerAdded:Connect(function(p)
    p.CharacterAdded:Connect(function()
        task.wait(1)
        if PF.Chams and PremiumUnlocked then ApplyChams(true) end
    end)
end)

-- ── TIME TO IMPACT (TTI) ───────────────────────────────────────────
local TTISg = nil

local function BuildTTI()
    if TTISg then pcall(function() TTISg:Destroy() end) end
    TTISg = Instance.new("ScreenGui")
    TTISg.Name="RyuuTTI"; TTISg.ResetOnSpawn=false
    TTISg.IgnoreGuiInset=true; TTISg.DisplayOrder=9991
    pcall(function() TTISg.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not TTISg.Parent then pcall(function() TTISg.Parent=Plr:WaitForChild("PlayerGui") end) end

    local TTIFrame = Instance.new("Frame",TTISg)
    TTIFrame.AnchorPoint=Vector2.new(0.5,0); TTIFrame.Position=UDim2.new(0.5,0,0,20)
    TTIFrame.Size=UDim2.new(0,150,0,30); TTIFrame.BackgroundColor3=Color3.fromRGB(4,12,20)
    TTIFrame.BackgroundTransparency=0.18; TTIFrame.BorderSizePixel=0; TTIFrame.Visible=false
    Instance.new("UICorner",TTIFrame).CornerRadius=UDim.new(0,8)
    local TTIStr=Instance.new("UIStroke",TTIFrame)
    TTIStr.Thickness=1.2; TTIStr.Color=Color3.fromRGB(0,180,255); TTIStr.Transparency=0.3

    local TTILabel=Instance.new("TextLabel",TTIFrame)
    TTILabel.Size=UDim2.new(1,0,1,0); TTILabel.BackgroundTransparency=1
    TTILabel.Text="⏱  --.-s"; TTILabel.Font=Enum.Font.GothamBold; TTILabel.TextSize=13
    TTILabel.TextColor3=Color3.fromRGB(0,220,255)

    RunService.Heartbeat:Connect(function()
        if not PF.TimeToImpact or not PremiumUnlocked then
            pcall(function() TTIFrame.Visible=false end); return
        end
        local ball=GetBall(); local hrp=GetHRP()
        if not ball or not hrp then TTIFrame.Visible=false; return end
        local tgt=ball:GetAttribute("target") or ""
        if tgt~=Plr.Name then TTIFrame.Visible=false; return end
        local vel=GetVel(ball); local spd=vel.Magnitude
        if spd<1 then TTIFrame.Visible=false; return end
        local dist=(hrp.Position-ball.Position).Magnitude
        local tti=dist/spd
        local col=tti<0.3 and Color3.fromRGB(255,60,60) or tti<0.6 and Color3.fromRGB(255,200,40) or Color3.fromRGB(0,220,255)
        pcall(function()
            TTIFrame.Visible=true
            TTILabel.Text=("⏱  %.2fs"):format(tti)
            TTILabel.TextColor3=col; TTIStr.Color=col
        end)
    end)
end

-- ── BALL SPEED METER ───────────────────────────────────────────────
local SpeedMeterSG = nil

local function BuildSpeedMeter()
    if SpeedMeterSG then pcall(function() SpeedMeterSG:Destroy() end) end
    SpeedMeterSG = Instance.new("ScreenGui")
    SpeedMeterSG.Name="RyuuSpeedMeter"; SpeedMeterSG.ResetOnSpawn=false
    SpeedMeterSG.IgnoreGuiInset=true; SpeedMeterSG.DisplayOrder=9993
    pcall(function() SpeedMeterSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not SpeedMeterSG.Parent then pcall(function() SpeedMeterSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    local SMFrame=Instance.new("Frame",SpeedMeterSG)
    SMFrame.AnchorPoint=Vector2.new(0.5,1); SMFrame.Position=UDim2.new(0.5,0,1,-60)
    SMFrame.Size=UDim2.new(0,200,0,62); SMFrame.BackgroundColor3=Color3.fromRGB(4,12,20)
    SMFrame.BackgroundTransparency=0.18; SMFrame.BorderSizePixel=0
    Instance.new("UICorner",SMFrame).CornerRadius=UDim.new(0,10)
    local SMStr=Instance.new("UIStroke",SMFrame)
    SMStr.Thickness=1.2; SMStr.Color=Color3.fromRGB(0,180,255); SMStr.Transparency=0.3

    local SMAccent=Instance.new("Frame",SMFrame)
    SMAccent.Size=UDim2.new(1,0,0,2); SMAccent.BackgroundColor3=Color3.fromRGB(0,200,255)
    SMAccent.BorderSizePixel=0
    Instance.new("UICorner",SMAccent).CornerRadius=UDim.new(0,10)

    local SMTitle=Instance.new("TextLabel",SMFrame)
    SMTitle.Size=UDim2.new(1,-8,0,13); SMTitle.Position=UDim2.new(0,8,0,5)
    SMTitle.BackgroundTransparency=1; SMTitle.Text="⚡  BALL SPEED"
    SMTitle.Font=Enum.Font.GothamBlack; SMTitle.TextSize=9
    SMTitle.TextColor3=Color3.fromRGB(0,170,220); SMTitle.TextXAlignment=Enum.TextXAlignment.Left

    local SMVal=Instance.new("TextLabel",SMFrame)
    SMVal.Size=UDim2.new(0,100,0,22); SMVal.Position=UDim2.new(0,8,0,20)
    SMVal.BackgroundTransparency=1; SMVal.Text="-- st/s"
    SMVal.Font=Enum.Font.GothamBlack; SMVal.TextSize=18
    SMVal.TextColor3=Color3.fromRGB(0,220,255); SMVal.TextXAlignment=Enum.TextXAlignment.Left

    -- Mini bar graph (12 bars)
    local GraphBG=Instance.new("Frame",SMFrame)
    GraphBG.Size=UDim2.new(1,-16,0,16); GraphBG.Position=UDim2.new(0,8,0,44)
    GraphBG.BackgroundColor3=Color3.fromRGB(0,20,35); GraphBG.BorderSizePixel=0
    Instance.new("UICorner",GraphBG).CornerRadius=UDim.new(0,4)

    local graphBars={}
    for i=1,12 do
        local bar=Instance.new("Frame",GraphBG)
        bar.AnchorPoint=Vector2.new(0,1); bar.Position=UDim2.new(0,(i-1)*15,1,-1)
        bar.Size=UDim2.new(0,10,0,2); bar.BackgroundColor3=Color3.fromRGB(0,180,255)
        bar.BorderSizePixel=0
        Instance.new("UICorner",bar).CornerRadius=UDim.new(0,2)
        table.insert(graphBars,bar)
    end

    local speedHist={}
    RunService.Heartbeat:Connect(function()
        if not PF.BallSpeedMeter or not PremiumUnlocked then return end
        local ball=GetBall(); if not ball then return end
        local spd=GetVel(ball).Magnitude
        table.insert(speedHist,spd); if #speedHist>12 then table.remove(speedHist,1) end
        local maxS=1
        for _,v in ipairs(speedHist) do if v>maxS then maxS=v end end
        pcall(function()
            local col=spd>220 and Color3.fromRGB(255,70,70) or spd>120 and Color3.fromRGB(255,200,40) or Color3.fromRGB(0,220,255)
            SMVal.Text=("%.0f st/s"):format(spd); SMVal.TextColor3=col; SMStr.Color=col
            for i,bar in ipairs(graphBars) do
                local h=speedHist[i] or 0
                local ratio=math.clamp(h/maxS,0,1)
                bar.Size=UDim2.new(0,10,0,math.max(2,math.floor(ratio*14)))
                bar.BackgroundColor3=h>220 and Color3.fromRGB(255,70,70) or h>120 and Color3.fromRGB(255,180,40) or Color3.fromRGB(0,180,255)
            end
        end)
    end)
end

-- ── TARGET ALERT (red edge flash) ─────────────────────────────────
local AlertSG = nil
local alertActive = false

local function BuildTargetAlert()
    if AlertSG then pcall(function() AlertSG:Destroy() end) end
    AlertSG=Instance.new("ScreenGui")
    AlertSG.Name="RyuuAlert"; AlertSG.ResetOnSpawn=false
    AlertSG.IgnoreGuiInset=true; AlertSG.DisplayOrder=9990
    pcall(function() AlertSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not AlertSG.Parent then pcall(function() AlertSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    local function makeEdge(size, pos)
        local f=Instance.new("Frame",AlertSG)
        f.Size=size; f.Position=pos; f.BackgroundColor3=Color3.fromRGB(255,50,50)
        f.BackgroundTransparency=1; f.BorderSizePixel=0; f.ZIndex=5; return f
    end
    local edges={
        makeEdge(UDim2.new(1,0,0,5), UDim2.new(0,0,0,0)),   -- top
        makeEdge(UDim2.new(1,0,0,5), UDim2.new(0,0,1,-5)),   -- bottom
        makeEdge(UDim2.new(0,5,1,0), UDim2.new(0,0,0,0)),    -- left
        makeEdge(UDim2.new(0,5,1,0), UDim2.new(1,-5,0,0)),   -- right
    }

    RunService.Heartbeat:Connect(function()
        if not PF.TargetAlert or not PremiumUnlocked then
            for _,e in ipairs(edges) do pcall(function() e.BackgroundTransparency=1 end) end
            alertActive=false; return
        end
        local ball=GetBall(); if not ball then return end
        local nowTgt=(ball:GetAttribute("target") or "")==Plr.Name
        if nowTgt then
            local t=os.clock()*8
            local alpha=math.abs(math.sin(t))*0.6+0.05
            for _,e in ipairs(edges) do pcall(function() e.BackgroundTransparency=1-alpha end) end
        else
            for _,e in ipairs(edges) do pcall(function() e.BackgroundTransparency=1 end) end
        end
    end)
end

-- ── SAFE ZONE WARNING (vignette) ───────────────────────────────────
local SafeZoneSG=nil

local function BuildSafeZoneWarning()
    if SafeZoneSG then pcall(function() SafeZoneSG:Destroy() end) end
    SafeZoneSG=Instance.new("ScreenGui")
    SafeZoneSG.Name="RyuuSafeZone"; SafeZoneSG.ResetOnSpawn=false
    SafeZoneSG.IgnoreGuiInset=true; SafeZoneSG.DisplayOrder=9988
    pcall(function() SafeZoneSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not SafeZoneSG.Parent then pcall(function() SafeZoneSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    -- Orange vignette edges
    local function makeVig(size,pos)
        local f=Instance.new("Frame",SafeZoneSG)
        f.Size=size; f.Position=pos; f.BackgroundColor3=Color3.fromRGB(255,130,0)
        f.BackgroundTransparency=1; f.BorderSizePixel=0; f.ZIndex=3; return f
    end
    local vigEdges={
        makeVig(UDim2.new(1,0,0,8),UDim2.new(0,0,0,0)),
        makeVig(UDim2.new(1,0,0,8),UDim2.new(0,0,1,-8)),
        makeVig(UDim2.new(0,8,1,0),UDim2.new(0,0,0,0)),
        makeVig(UDim2.new(0,8,1,0),UDim2.new(1,-8,0,0)),
    }
    local WarnLbl=Instance.new("TextLabel",SafeZoneSG)
    WarnLbl.AnchorPoint=Vector2.new(0.5,0); WarnLbl.Position=UDim2.new(0.5,0,0,8)
    WarnLbl.Size=UDim2.new(0,160,0,20); WarnLbl.BackgroundTransparency=1
    WarnLbl.Text="⚠  EXPOSED — STAY ALERT"; WarnLbl.Font=Enum.Font.GothamBold; WarnLbl.TextSize=11
    WarnLbl.TextColor3=Color3.fromRGB(255,160,30); WarnLbl.Visible=false; WarnLbl.ZIndex=4

    RunService.Heartbeat:Connect(function()
        if not PF.SafeZoneWarn or not PremiumUnlocked then
            for _,e in ipairs(vigEdges) do pcall(function() e.BackgroundTransparency=1 end) end
            pcall(function() WarnLbl.Visible=false end); return
        end
        local ball=GetBall()
        local exposed=ball and (ball:GetAttribute("target") or "")==Plr.Name
        if exposed then
            local t=os.clock()*3
            local alpha=math.abs(math.sin(t))*0.45+0.05
            for _,e in ipairs(vigEdges) do pcall(function() e.BackgroundTransparency=1-alpha end) end
            pcall(function() WarnLbl.Visible=true end)
        else
            for _,e in ipairs(vigEdges) do pcall(function() e.BackgroundTransparency=1 end) end
            pcall(function() WarnLbl.Visible=false end)
        end
    end)
end

-- ── PREMIUM WATERMARK ──────────────────────────────────────────────
local WatermarkSG=nil

local function BuildWatermark()
    if WatermarkSG then pcall(function() WatermarkSG:Destroy() end) end
    WatermarkSG=Instance.new("ScreenGui")
    WatermarkSG.Name="RyuuWatermark"; WatermarkSG.ResetOnSpawn=false
    WatermarkSG.IgnoreGuiInset=true; WatermarkSG.DisplayOrder=9987
    pcall(function() WatermarkSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not WatermarkSG.Parent then pcall(function() WatermarkSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    local WMFrame=Instance.new("Frame",WatermarkSG)
    WMFrame.AnchorPoint=Vector2.new(0,1); WMFrame.Position=UDim2.new(0,10,1,-10)
    WMFrame.Size=UDim2.new(0,190,0,28); WMFrame.BackgroundColor3=Color3.fromRGB(3,10,20)
    WMFrame.BackgroundTransparency=0.2; WMFrame.BorderSizePixel=0
    Instance.new("UICorner",WMFrame).CornerRadius=UDim.new(0,8)
    local WMStr=Instance.new("UIStroke",WMFrame)
    WMStr.Thickness=1; WMStr.Color=Color3.fromRGB(0,180,255); WMStr.Transparency=0.3
    local WMAccent=Instance.new("Frame",WMFrame)
    WMAccent.Size=UDim2.new(1,0,0,2); WMAccent.BackgroundColor3=Color3.fromRGB(0,200,255)
    WMAccent.BorderSizePixel=0
    Instance.new("UICorner",WMAccent).CornerRadius=UDim.new(0,8)

    local WMLbl=Instance.new("TextLabel",WMFrame)
    WMLbl.Size=UDim2.new(1,-8,1,0); WMLbl.Position=UDim2.new(0,8,0,0)
    WMLbl.BackgroundTransparency=1; WMLbl.Text="◈  Ryuu HUB V2  ·  PREMIUM"
    WMLbl.Font=Enum.Font.GothamBold; WMLbl.TextSize=10
    WMLbl.TextColor3=Color3.fromRGB(0,200,255); WMLbl.TextXAlignment=Enum.TextXAlignment.Left
    local WMGrad=Instance.new("UIGradient",WMLbl)
    WMGrad.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,Color3.fromRGB(0,160,255)),
        ColorSequenceKeypoint.new(0.5,Color3.fromRGB(140,255,255)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(0,160,255))}

    task.spawn(function()
        local rot=0
        while WatermarkSG and WatermarkSG.Parent do
            rot=(rot+0.6)%360; pcall(function() WMGrad.Rotation=rot end); task.wait(0.016)
        end
    end)
end

-- ── COMBO TRACKER WIDGET ───────────────────────────────────────────
local ComboSG=nil
local ComboFrame=nil
local ComboLbl2=nil

local function BuildComboWidget()
    if ComboSG then pcall(function() ComboSG:Destroy() end) end
    ComboSG=Instance.new("ScreenGui")
    ComboSG.Name="RyuuCombo"; ComboSG.ResetOnSpawn=false
    ComboSG.IgnoreGuiInset=true; ComboSG.DisplayOrder=9994
    pcall(function() ComboSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not ComboSG.Parent then pcall(function() ComboSG.Parent=Plr:WaitForChild("PlayerGui") end) end

    ComboFrame=Instance.new("Frame",ComboSG)
    ComboFrame.AnchorPoint=Vector2.new(0.5,0)
    ComboFrame.Position=UDim2.new(0.5,0,0,80)
    ComboFrame.Size=UDim2.new(0,160,0,58)
    ComboFrame.BackgroundColor3=Color3.fromRGB(6,3,14)
    ComboFrame.BackgroundTransparency=0.15; ComboFrame.BorderSizePixel=0
    Instance.new("UICorner",ComboFrame).CornerRadius=UDim.new(0,12)
    local CStr=Instance.new("UIStroke",ComboFrame)
    CStr.Thickness=1.4; CStr.Color=Color3.fromRGB(0,200,255); CStr.Transparency=0.2

    local CAccent=Instance.new("Frame",ComboFrame)
    CAccent.Size=UDim2.new(1,0,0,2); CAccent.BackgroundColor3=Color3.fromRGB(0,200,255)
    CAccent.BorderSizePixel=0; Instance.new("UICorner",CAccent).CornerRadius=UDim.new(0,12)

    local CTitle=Instance.new("TextLabel",ComboFrame)
    CTitle.Size=UDim2.new(1,0,0,14); CTitle.Position=UDim2.new(0,0,0,6)
    CTitle.BackgroundTransparency=1; CTitle.Text="COMBO STREAK"
    CTitle.Font=Enum.Font.GothamBold; CTitle.TextSize=8
    CTitle.TextColor3=Color3.fromRGB(0,130,180)

    ComboLbl2=Instance.new("TextLabel",ComboFrame)
    ComboLbl2.Size=UDim2.new(1,0,0,30); ComboLbl2.Position=UDim2.new(0,0,0,18)
    ComboLbl2.BackgroundTransparency=1; ComboLbl2.Text="0×"
    ComboLbl2.Font=Enum.Font.GothamBlack; ComboLbl2.TextSize=26
    ComboLbl2.TextColor3=Color3.fromRGB(0,220,255)

    local CSub=Instance.new("TextLabel",ComboFrame)
    CSub.Size=UDim2.new(1,0,0,12); CSub.Position=UDim2.new(0,0,0,46)
    CSub.BackgroundTransparency=1; CSub.Text="— drag to reposition —"
    CSub.Font=Enum.Font.Gotham; CSub.TextSize=7
    CSub.TextColor3=Color3.fromRGB(40,80,100)

    -- Drag logic
    local dragging,dragStart,startPos=false,nil,nil
    ComboFrame.InputBegan:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseButton1 then
            dragging=true; dragStart=inp.Position; startPos=ComboFrame.Position
        end
    end)
    UIS.InputChanged:Connect(function(inp)
        if not dragging then return end
        if inp.UserInputType==Enum.UserInputType.MouseMovement then
            local d=inp.Position-dragStart
            ComboFrame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+d.X,
                startPos.Y.Scale,startPos.Y.Offset+d.Y)
        end
    end)
    UIS.InputEnded:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseButton1 then dragging=false end
    end)
end

local function UpdateCombo(scored)
    if not PF.ComboTracker or not PremiumUnlocked then return end
    if not ComboFrame then return end
    local now=os.clock()
    if scored then
        if now-PF.ComboLastParry<5 then PF.ComboCount=PF.ComboCount+1 else PF.ComboCount=1 end
        PF.ComboLastParry=now
        pcall(function()
            ComboLbl2.Text=PF.ComboCount.."×"
            local hue=math.clamp(PF.ComboCount/25,0,1)
            ComboLbl2.TextColor3=Color3.fromHSV(0.56-hue*0.22,1,1)
            TweenService:Create(ComboFrame,TweenInfo.new(0.08,Enum.EasingStyle.Back,Enum.EasingDirection.Out),
                {Size=UDim2.new(0,180,0,68)}):Play()
            task.delay(0.14,function()
                pcall(function()
                    TweenService:Create(ComboFrame,TweenInfo.new(0.2),
                        {Size=UDim2.new(0,160,0,58)}):Play()
                end)
            end)
        end)
    end
    task.delay(5.1,function()
        if os.clock()-PF.ComboLastParry>=5 then
            PF.ComboCount=0
            pcall(function() ComboLbl2.Text="0×"; ComboLbl2.TextColor3=Color3.fromRGB(0,220,255) end)
        end
    end)
end

-- ── SPEED BOOST ON PARRY ───────────────────────────────────────────
local function ApplySpeedBoost()
    if not PF.SpeedOnParry or not PremiumUnlocked then return end
    local char=Plr.Character; if not char then return end
    local hum=char:FindFirstChildOfClass("Humanoid"); if not hum then return end
    hum.WalkSpeed=PF.SpeedOnParryAmt
    task.delay(0.8,function()
        pcall(function() if hum and hum.Parent then hum.WalkSpeed=S.WalkSpeed end end)
    end)
end

-- ── GHOST MODE ─────────────────────────────────────────────────────
local function ApplyGhost(on)
    local char=Plr.Character; if not char then return end
    for _,p in ipairs(char:GetDescendants()) do
        if p:IsA("BasePart") then
            pcall(function()
                p.LocalTransparencyModifier=on and 0.85 or 0
                p.CanCollide=not on
            end)
        end
    end
end

-- ── BALL HIGHLIGHT ─────────────────────────────────────────────────
local BallHighlightConn=nil

local function StartBallHighlight()
    if BallHighlightConn then BallHighlightConn:Disconnect() end
    BallHighlightConn=RunService.Heartbeat:Connect(function()
        if not PF.BallHighlight or not PremiumUnlocked then
            BallHighlightConn:Disconnect(); BallHighlightConn=nil; return
        end
        local ball=GetBall(); if not ball then return end
        local isTgt=(ball:GetAttribute("target") or "")==Plr.Name
        pcall(function()
            ball.Material=Enum.Material.Neon
            ball.Color=isTgt and Color3.fromRGB(255,50,50) or Color3.fromRGB(80,220,255)
        end)
    end)
end

-- ── RAINBOW TRAIL ──────────────────────────────────────────────────
local RainbowTrailConn=nil
local RainbowHue=0

local function StartRainbowTrail()
    if RainbowTrailConn then RainbowTrailConn:Disconnect() end
    RainbowTrailConn=RunService.Heartbeat:Connect(function(dt)
        if not PF.RainbowTrail or not PremiumUnlocked then
            RainbowTrailConn:Disconnect(); RainbowTrailConn=nil; return
        end
        RainbowHue=(RainbowHue+dt*0.45)%1
        local ball=GetBall(); if not ball then return end
        pcall(function() ball.Color=Color3.fromHSV(RainbowHue,1,1); ball.Material=Enum.Material.Neon end)
    end)
end

-- ── AUTO DODGE ─────────────────────────────────────────────────────
local DodgeConn=nil

local function StartAutoDodge()
    if DodgeConn then DodgeConn:Disconnect() end
    DodgeConn=RunService.Heartbeat:Connect(function()
        if not PF.AutoDodge or not PremiumUnlocked then
            DodgeConn:Disconnect(); DodgeConn=nil; return
        end
        local ball=GetBall(); if not ball then return end
        local hrp=GetHRP(); if not hrp then return end
        if (ball:GetAttribute("target") or "")~=Plr.Name then return end
        local dist=(hrp.Position-ball.Position).Magnitude
        if dist>10 then return end
        local vel=GetVel(ball); if vel.Magnitude<8 then return end
        local away=(hrp.Position-ball.Position).Unit
        away=Vector3.new(away.X,0,away.Z)
        pcall(function() hrp.CFrame=hrp.CFrame+away*0.55 end)
    end)
end

-- ── PREMIUM FPS UNLOCKER ───────────────────────────────────────────
local function SetPremFPS(on, cap)
    if on then
        pcall(function() setfpscap(cap or 0) end)
        pcall(function() game:GetService("RunService"):Set60FpsThrottleEnabled(false) end)
    else
        pcall(function() setfpscap(60) end)
    end
end

-- ── ANTI-BLOCK ─────────────────────────────────────────────────────
local AntiBlockConn=nil
local BLOCK_SIGS={"Forcefield","Guardian","Reaper","Titan","Flash"}

local function StartAntiBlock()
    if AntiBlockConn then AntiBlockConn:Disconnect() end
    local lastFire=0
    AntiBlockConn=RunService.Heartbeat:Connect(function()
        if not PF.AntiBlock or not PremiumUnlocked then
            AntiBlockConn:Disconnect(); AntiBlockConn=nil; return
        end
        local now=os.clock(); if now-lastFire<0.6 then return end
        local hrp=GetHRP(); if not hrp then return end
        for _,p in ipairs(Players:GetPlayers()) do
            if p==Plr then continue end
            local char=p.Character; if not char then continue end
            local pHRP=char:FindFirstChild("HumanoidRootPart"); if not pHRP then continue end
            if (hrp.Position-pHRP.Position).Magnitude>22 then continue end
            for _,sig in ipairs(BLOCK_SIGS) do
                local found=false
                pcall(function()
                    for _,v in ipairs(char:GetDescendants()) do
                        if tostring(v.Name):lower():find(sig:lower()) then found=true; break end
                    end
                end)
                if found then lastFire=now; pcall(PremiumFire); break end
            end
        end
    end)
end

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM GUARD HELPER
-- ══════════════════════════════════════════════════════════════════
local function PremGuard(val, label, fn)
    if not PremiumUnlocked then
        Rayfield:Notify({Title="🔒 "..label.." Locked",
            Content="Enter your premium key in the Premium tab.",Duration=3,Image=4483362458})
        return
    end
    fn(val)
end

local function RemovePremiumLock() end  -- placeholder for future lock UI

-- ══════════════════════════════════════════════════════════════════
-- RAYFIELD WINDOW
-- ══════════════════════════════════════════════════════════════════
local W = Rayfield:CreateWindow({
    Name="Ryuu's HUB V2",
    LoadingTitle="Ryuu's HUB V2",
    LoadingSubtitle="",
    Theme="Default",
    DisableRayfieldPrompts=false,
    DisableBuildWarnings=false,
    ConfigurationSaving={Enabled=true,FolderName="RyuuV2",FileName="Config"},
    KeySystem=false,
})

-- ── TAB 1: AUTO PARRY ────────────────────────────────────────────
local T1 = W:CreateTab("⚔ Auto Parry", nil)
T1:CreateSection("Auto Parry")
T1:CreateToggle({Name="Auto Parry",CurrentValue=false,Flag="AP",Callback=function(v)
    S.AutoParry=v
    if v then StartEngine() else StopEngine() end
end})
T1:CreateToggle({Name="Auto Face Ball",CurrentValue=false,Flag="AF",Callback=function(v) S.AutoFace=v end})
T1:CreateToggle({Name="Triggerbot (≤2 studs instant)",CurrentValue=false,Flag="TB",Callback=function(v) S.Triggerbot=v end})
T1:CreateLabel("LSQ regression closing rate · Quadratic time-to-impact · Jitter-aware ping")
T1:CreateDivider()

T1:CreateSection("🎯 Parry Accuracy")
T1:CreateLabel("100 = machine precision · Lower = human-like timing variance")
T1:CreateSlider({Name="Parry Accuracy",Range={1,100},Increment=1,CurrentValue=100,Flag="PA",
    Callback=function(v)
        S.ParryAccuracy=v
        local tier=v>=95 and "PERFECT" or v>=80 and "Elite" or v>=60 and "Balanced" or v>=40 and "Normal" or "Low"
        Rayfield:Notify({Title="Accuracy: "..v.."  —  "..tier,Content="",Duration=2,Image=4483362458})
    end})
T1:CreateDivider()

T1:CreateSection("📡 Ping Compensation")
PingLabel = T1:CreateLabel("Ping: measuring...")
T1:CreateLabel("Reads the lowest ping sample seen — gives you the fastest possible compensation.")
T1:CreateSlider({Name="Ping Scale",Range={0,1},Increment=0.05,Suffix="x",CurrentValue=0.5,Flag="PCS",
    Callback=function(v) PingScale=v end})
T1:CreateDivider()

T1:CreateSection("⏱ Parry Cooldown")
T1:CreateSlider({Name="Parry Cooldown",Range={0.05,0.60},Increment=0.01,Suffix="s",
    CurrentValue=0.30,Flag="CD",Callback=function(v) S.ParryCooldown=v end})
T1:CreateDivider()

T1:CreateSection("🚨 Emergency Distance Parry")
T1:CreateToggle({Name="Emergency Parry",CurrentValue=true,Flag="EP",Callback=function(v) S.EmergencyParry=v end})
T1:CreateSlider({Name="Emergency Distance",Range={2,12},Increment=1,Suffix=" studs",
    CurrentValue=6,Flag="EPD",Callback=function(v) S.EmergencyDist=v end})
T1:CreateDivider()

T1:CreateButton({Name="⚡ Open Manual Spam Widget",Callback=function()
    if _G.RyuuOpenManualSpam then _G.RyuuOpenManualSpam() end
end})

-- ══════════════════════════════════════════════════════════════════
-- MANUAL SPAM WIDGET  ◈  Upgraded: glass card, pulse dot, smooth drag
-- ══════════════════════════════════════════════════════════════════
do
    local MSOn=false; local MSThr=nil

    local MSSG=Instance.new("ScreenGui"); MSSG.Name="RyuuManualSpam"
    MSSG.ResetOnSpawn=false; MSSG.IgnoreGuiInset=true; MSSG.DisplayOrder=9995; MSSG.Enabled=false
    pcall(function() MSSG.Parent=(typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not MSSG.Parent then MSSG.Parent=Plr:WaitForChild("PlayerGui") end

    local C_OFF={BG=Color3.fromRGB(10,7,22),BRD=Color3.fromRGB(75,45,155),
                 TXT=Color3.fromRGB(190,170,245),SUB=Color3.fromRGB(110,90,190),DOT=Color3.fromRGB(90,55,185)}
    local C_ON ={BG=Color3.fromRGB(8,30,12),BRD=Color3.fromRGB(35,215,85),
                 TXT=Color3.fromRGB(70,250,130),SUB=Color3.fromRGB(45,170,90), DOT=Color3.fromRGB(55,235,110)}

    -- Shadow layer
    local Shadow=Instance.new("Frame",MSSG); Shadow.Name="Shadow"
    Shadow.AnchorPoint=Vector2.new(0.5,0.5); Shadow.Size=UDim2.new(0,192,0,68)
    Shadow.Position=UDim2.new(0.5,3,0.5,3)
    Shadow.BackgroundColor3=Color3.fromRGB(0,0,0); Shadow.BackgroundTransparency=0.60
    Shadow.BorderSizePixel=0
    Instance.new("UICorner",Shadow).CornerRadius=UDim.new(0,18)

    -- Main card
    local Card=Instance.new("TextButton",MSSG); Card.Name="Card"
    Card.AnchorPoint=Vector2.new(0.5,0.5); Card.Size=UDim2.new(0,188,0,64)
    Card.Position=UDim2.new(0.5,0,0.5,0)
    Card.BackgroundColor3=C_OFF.BG; Card.BorderSizePixel=0; Card.Text=""; Card.AutoButtonColor=false; Card.Active=true
    Instance.new("UICorner",Card).CornerRadius=UDim.new(0,18)

    local CardGrad=Instance.new("UIGradient",Card)
    CardGrad.Color=ColorSequence.new{
        ColorSequenceKeypoint.new(0,Color3.fromRGB(22,14,50)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(8,5,18))
    }
    CardGrad.Rotation=130

    local MSStroke=Instance.new("UIStroke",Card); MSStroke.Thickness=1.8
    MSStroke.Color=C_OFF.BRD; MSStroke.Transparency=0.05

    -- Top accent line
    local Accent=Instance.new("Frame",Card); Accent.Size=UDim2.new(0.6,0,0,2)
    Accent.Position=UDim2.new(0.2,0,0,0); Accent.BackgroundColor3=C_OFF.BRD; Accent.BorderSizePixel=0
    Instance.new("UICorner",Accent).CornerRadius=UDim.new(1,0)

    -- Pulse dot
    local Dot=Instance.new("Frame",Card); Dot.Size=UDim2.new(0,11,0,11)
    Dot.Position=UDim2.new(0,15,0.5,-5.5); Dot.BackgroundColor3=C_OFF.DOT; Dot.BorderSizePixel=0
    Instance.new("UICorner",Dot).CornerRadius=UDim.new(1,0)

    -- Labels
    local Tit=Instance.new("TextLabel",Card); Tit.Size=UDim2.new(1,-80,0,20)
    Tit.Position=UDim2.new(0,34,0,8); Tit.BackgroundTransparency=1; Tit.Text="⚡  MANUAL SPAM"
    Tit.Font=Enum.Font.GothamBold; Tit.TextSize=13; Tit.TextColor3=C_OFF.TXT; Tit.TextXAlignment=Enum.TextXAlignment.Left

    local Sub=Instance.new("TextLabel",Card); Sub.Size=UDim2.new(1,-80,0,14)
    Sub.Position=UDim2.new(0,34,0,30); Sub.BackgroundTransparency=1; Sub.Text="OFF  ·  Press E to toggle"
    Sub.Font=Enum.Font.Gotham; Sub.TextSize=10; Sub.TextColor3=C_OFF.SUB; Sub.TextXAlignment=Enum.TextXAlignment.Left

    -- E badge
    local Badge=Instance.new("TextLabel",Card); Badge.Size=UDim2.new(0,26,0,20)
    Badge.Position=UDim2.new(1,-38,0.5,-10); Badge.BackgroundColor3=Color3.fromRGB(22,16,48)
    Badge.BorderSizePixel=0; Badge.Text="E"; Badge.Font=Enum.Font.GothamBlack; Badge.TextSize=12
    Badge.TextColor3=C_OFF.BRD
    Instance.new("UICorner",Badge).CornerRadius=UDim.new(0,8)

    -- Close
    local Close=Instance.new("TextButton",Card); Close.Size=UDim2.new(0,20,0,20)
    Close.Position=UDim2.new(1,-24,0,6); Close.BackgroundTransparency=1; Close.Text="✕"
    Close.Font=Enum.Font.GothamBold; Close.TextSize=13; Close.TextColor3=Color3.fromRGB(130,110,190)

    -- Drag
    local dragging,dragStart,startPos=false,nil,nil
    Card.InputBegan:Connect(function(input)
        if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
            dragging=true; dragStart=input.Position; startPos=Card.Position
            TweenService:Create(Card,TweenInfo.new(0.1),{Size=UDim2.new(0,184,0,61)}):Play()
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if not dragging then return end
        if input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch then
            local delta=input.Position-dragStart
            local vs=Cam.ViewportSize
            local nx=math.clamp(startPos.X.Offset+delta.X,-200,vs.X-20)
            local ny=math.clamp(startPos.Y.Offset+delta.Y,-100,vs.Y-80)
            Card.Position=UDim2.new(startPos.X.Scale,nx,startPos.Y.Scale,ny)
            Shadow.Position=UDim2.new(startPos.X.Scale,nx+3,startPos.Y.Scale,ny+3)
        end
    end)
    UIS.InputEnded:Connect(function(input)
        if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
            dragging=false
            TweenService:Create(Card,TweenInfo.new(0.15),{Size=UDim2.new(0,188,0,64)}):Play()
        end
    end)

    local TIF=TweenInfo.new(0.18,Enum.EasingStyle.Quart,Enum.EasingDirection.Out)
    local dotPulseConn=nil

    local function updateVisuals(on)
        local c=on and C_ON or C_OFF
        TweenService:Create(Card,TIF,{BackgroundColor3=c.BG}):Play()
        TweenService:Create(MSStroke,TIF,{Color=c.BRD}):Play()
        TweenService:Create(Tit,TIF,{TextColor3=c.TXT}):Play()
        TweenService:Create(Sub,TIF,{TextColor3=c.SUB}):Play()
        TweenService:Create(Dot,TIF,{BackgroundColor3=c.DOT}):Play()
        TweenService:Create(Accent,TIF,{BackgroundColor3=c.BRD}):Play()
        TweenService:Create(Badge,TIF,{TextColor3=c.BRD}):Play()
        Sub.Text=on and "ON  ·  SPAMMING" or "OFF  ·  Press E to toggle"

        if dotPulseConn then dotPulseConn:Disconnect(); dotPulseConn=nil end
        if on then
            local big=false
            dotPulseConn=RunService.Heartbeat:Connect(function()
                pcall(function()
                    TweenService:Create(Dot,TweenInfo.new(0.35,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),
                        {Size=big and UDim2.new(0,11,0,11) or UDim2.new(0,15,0,15),
                         Position=big and UDim2.new(0,15,0.5,-5.5) or UDim2.new(0,13,0.5,-7.5)}):Play()
                    big=not big
                end)
                task.wait(0.35)
            end)
        end
    end

    local function doStart()
        if MSThr then return end
        MSThr=RunService.Heartbeat:Connect(function()
            if not MSOn then MSThr:Disconnect(); MSThr=nil; return end
            VIM:SendMouseButtonEvent(0,0,0,true,game,0)
            VIM:SendMouseButtonEvent(0,0,0,false,game,0)
        end)
    end
    local function doStop()
        MSOn=false; if MSThr then MSThr:Disconnect(); MSThr=nil end
    end
    local function toggle()
        MSOn=not MSOn; updateVisuals(MSOn)
        if MSOn then doStart() else doStop() end
    end

    UIS.InputBegan:Connect(function(input)
        if input.KeyCode==Enum.KeyCode.E and MSSG.Enabled then toggle() end
    end)
    Card.MouseButton1Click:Connect(toggle)
    Close.MouseButton1Click:Connect(function()
        doStop(); updateVisuals(false); MSSG.Enabled=false
    end)
    Close.MouseEnter:Connect(function()
        TweenService:Create(Close,TweenInfo.new(0.12),{TextColor3=Color3.fromRGB(255,90,90)}):Play()
    end)
    Close.MouseLeave:Connect(function()
        TweenService:Create(Close,TweenInfo.new(0.12),{TextColor3=Color3.fromRGB(130,110,190)}):Play()
    end)

    _G.RyuuOpenManualSpam=function()
        MSSG.Enabled=true
        Card.Position=UDim2.new(0.5,0,1.3,0)
        Shadow.Position=UDim2.new(0.5,3,1.3,3)
        TweenService:Create(Card,TweenInfo.new(0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out),
            {Position=UDim2.new(0.5,0,0.5,0)}):Play()
        TweenService:Create(Shadow,TweenInfo.new(0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out),
            {Position=UDim2.new(0.5,3,0.5,3)}):Play()
        updateVisuals(MSOn)
    end
end

-- ── TAB 3: VISUALS ────────────────────────────────────────────────
local T3=W:CreateTab("🎨 Visuals",nil)
T3:CreateSection("Ball ESP")
T3:CreateToggle({Name="Ball ESP",CurrentValue=false,Flag="ESP",
    Callback=function(v) S.BallESP=v; if v then StartESP() else CleanESP() end end})
T3:CreateLabel("Red = targeting you  |  Green = targeting other")
T3:CreateDivider()
T3:CreateSection("Lighting")
T3:CreateToggle({Name="Fullbright",CurrentValue=false,Flag="FB",
    Callback=function(v) S.Fullbright=v; SetFB(v) end})
T3:CreateDivider()
T3:CreateSection("Camera")
T3:CreateSlider({Name="Field of View",Range={30,120},Increment=1,Suffix="°",CurrentValue=70,Flag="FOV",
    Callback=function(v) Cam.FieldOfView=v end})
T3:CreateDivider()
T3:CreateSection("🔮 Ability ESP")
T3:CreateLabel("Upgraded: top accent bar, warn flash, dirty-flag refresh.")
T3:CreateToggle({Name="Ability ESP",CurrentValue=false,Flag="AESP",Callback=function(v)
    S.AbilityESP=v
    if v then
        StartAbilityESP()
        Rayfield:Notify({Title="Ability ESP ◈ ON",Content="Cards active.",Duration=3,Image=4483362458})
    else
        CleanAbilityESP()
        Rayfield:Notify({Title="Ability ESP ◈ OFF",Content="Cards removed.",Duration=2,Image=4483362458})
    end
end})
T3:CreateDivider()
T3:CreateSection("⚡ Ability Detector (Free)")
T3:CreateLabel("Covers: Slashes of Fury · Pull · Infinity")
T3:CreateToggle({Name="Ability Detector",CurrentValue=false,Flag="ADET",
    Callback=function(v)
        if v then StartAbilDetect()
            Rayfield:Notify({Title="⚡ Detector ON",Content="3 abilities monitored.",Duration=3,Image=4483362458})
        else StopAbilDetect() end
    end})

-- ── TAB 4: PLAYER ─────────────────────────────────────────────────
local T4=W:CreateTab("🧍 Player",nil)
T4:CreateSection("Movement")
T4:CreateSlider({Name="Walk Speed",Range={16,150},Increment=1,CurrentValue=16,Flag="WS",
    Callback=function(v)
        S.WalkSpeed=v
        local h=Plr.Character and Plr.Character:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed=v end
    end})
T4:CreateSlider({Name="Jump Power",Range={50,500},Increment=5,CurrentValue=50,Flag="JP",
    Callback=function(v)
        S.JumpPower=v
        local h=Plr.Character and Plr.Character:FindFirstChildOfClass("Humanoid")
        if h then h.JumpPower=v end
    end})
T4:CreateDivider()
T4:CreateSection("Abilities")
T4:CreateToggle({Name="Infinity Jump",CurrentValue=false,Flag="IJ",
    Callback=function(v) S.InfJump=v; if v then StartIJ() end end})
T4:CreateToggle({Name="Noclip",CurrentValue=false,Flag="NC",Callback=function(v)
    S.Noclip=v
    if not v then
        local c=Plr.Character
        if c then for _,p in ipairs(c:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=true end
        end end
    else StartNoclip() end
end})

-- ── TAB: SWORD ────────────────────────────────────────────────────
local T6=W:CreateTab("⚔ Sword",nil)
T6:CreateSection("Sword Color  ◈  Client-Side")
T6:CreateDropdown({
    Name="Sword Color",
    Options={"Default","Gold","Shadow Black","Crystal Blue","Crimson","Ghost White",
             "Neon Purple","Emerald","Rose Gold","Obsidian","Cyan","Orange","Rainbow"},
    CurrentOption={"Default"},MultipleOptions=false,Flag="SWSK",
    Callback=function(v)
        local name=type(v)=="table" and v[1] or tostring(v)
        ApplySwordPreset(name)
        Rayfield:Notify({Title="Sword ◈ "..name,Content="Applied!",Duration=2,Image=4483362458})
    end})
T6:CreateButton({Name="⚔ Apply Now",Callback=function()
    local _,parts=GetSwordParts()
    if #parts>0 then ApplySwordPreset(CurrentSwordPreset)
        Rayfield:Notify({Title="Applied: "..CurrentSwordPreset,Content="Sword updated!",Duration=2,Image=4483362458})
    else
        Rayfield:Notify({Title="No sword equipped",Content="Equip your sword first.",Duration=2,Image=4483362458})
    end
end})

-- ── TAB: PREMIUM ──────────────────────────────────────────────────
local PT = W:CreateTab("✨ Premium", nil)
PT:CreateSection("🔑 Premium Key Scanner")
PT:CreateLabel("Click below to open the holographic key scanner.")
PT:CreateButton({
    Name = "◈ Open Key Scanner",
    Callback = function()
        -- ── HOLOGRAPHIC PREMIUM KEY SCANNER ──────────────────────────
        task.spawn(function()
            local KSG = Instance.new("ScreenGui")
            KSG.Name = "RyuuKeyScanner"
            KSG.ResetOnSpawn = false
            KSG.IgnoreGuiInset = true
            KSG.DisplayOrder = 99999
            pcall(function() KSG.Parent = (typeof(gethui) == "function" and gethui()) or game:GetService("CoreGui") end)
            if not KSG.Parent then pcall(function() KSG.Parent = Plr:WaitForChild("PlayerGui") end) end

            local TS = game:GetService("TweenService")
            local RS = game:GetService("RunService")
            local Debris = game:GetService("Debris")

            local function playSound(id, vol, pitch, parent)
                pcall(function()
                    local s = Instance.new("Sound")
                    s.SoundId = "rbxassetid://" .. tostring(id)
                    s.Volume = vol or 0.6
                    s.PlaybackSpeed = pitch or 1
                    s.RollOffMaxDistance = 500
                    s.Parent = parent or workspace
                    s:Play()
                    Debris:AddItem(s, 6)
                end)
            end

            -- Background overlay with subtle vignette
            local Overlay = Instance.new("Frame", KSG)
            Overlay.Size = UDim2.new(1,0,1,0)
            Overlay.BackgroundColor3 = Color3.fromRGB(3,0,8)
            Overlay.BackgroundTransparency = 1
            Overlay.ZIndex = 1
            TS:Create(Overlay, TweenInfo.new(0.4, Enum.EasingStyle.Sine), {BackgroundTransparency = 0.4}):Play()

            local Vignette = Instance.new("ImageLabel", Overlay)
            Vignette.Size = UDim2.new(1,80,1,80)
            Vignette.Position = UDim2.new(0,-40,0,-40)
            Vignette.BackgroundTransparency = 1
            Vignette.Image = "rbxassetid://6014261993" -- subtle vignette / radial gradient
            Vignette.ImageTransparency = 0.65
            Vignette.ImageColor3 = Color3.fromRGB(90,40,180)
            Vignette.ZIndex = 1

            -- Central holographic orb / scanner core
            local CoreOrb = Instance.new("Frame", KSG)
            CoreOrb.AnchorPoint = Vector2.new(0.5,0.5)
            CoreOrb.Position = UDim2.new(0.5,0,0.5,0)
            CoreOrb.Size = UDim2.new(0,180,0,180)
            CoreOrb.BackgroundColor3 = Color3.fromRGB(40,180,255)
            CoreOrb.BackgroundTransparency = 0.75
            CoreOrb.ZIndex = 5
            local orbCorner = Instance.new("UICorner", CoreOrb)
            orbCorner.CornerRadius = UDim.new(1,0)
            local orbStroke = Instance.new("UIStroke", CoreOrb)
            orbStroke.Thickness = 2.5
            orbStroke.Color = Color3.fromRGB(120,240,255)
            orbStroke.Transparency = 0.3
            local orbGradient = Instance.new("UIGradient", CoreOrb)
            orbGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Color3.fromRGB(180,255,255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(40,100,255))
            }
            orbGradient.Rotation = 45

            -- Rotating rings around orb
            local rings = {}
            for i = 1, 3 do
                local ring = Instance.new("Frame", CoreOrb)
                ring.AnchorPoint = Vector2.new(0.5,0.5)
                ring.Position = UDim2.new(0.5,0,0.5,0)
                ring.Size = UDim2.new(1, i*45, 1, i*45)
                ring.BackgroundTransparency = 1
                ring.ZIndex = 6 + i
                local stroke = Instance.new("UIStroke", ring)
                stroke.Thickness = 1.5 - i*0.3
                stroke.Color = Color3.fromRGB(100 + i*50, 220 - i*30, 255)
                stroke.Transparency = 0.4 + i*0.1
                local corner = Instance.new("UICorner", ring)
                corner.CornerRadius = UDim.new(1,0)
                table.insert(rings, ring)
            end

            -- Gentle pulse on core
            task.spawn(function()
                while CoreOrb.Parent do
                    TS:Create(CoreOrb, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                        Size = UDim2.new(0,192,0,192), BackgroundTransparency = 0.68
                    }):Play()
                    task.wait(1.8)
                    TS:Create(CoreOrb, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                        Size = UDim2.new(0,168,0,168), BackgroundTransparency = 0.82
                    }):Play()
                    task.wait(1.8)
                end
            end)

            -- Rotating rings animation
            task.spawn(function()
                local rot = 0
                while CoreOrb.Parent do
                    rot = rot + 0.8
                    for i, ring in ipairs(rings) do
                        local dir = (i % 2 == 0) and 1 or -1
                        ring.Rotation = rot * dir * (0.7 + i*0.4)
                    end
                    task.wait(0.016)
                end
            end)

            -- Scanner card (appears after short delay)
            task.wait(0.6)
            playSound(9119713951, 0.35, 0.95) -- scan startup sound

            local Card = Instance.new("Frame", KSG)
            Card.AnchorPoint = Vector2.new(0.5,0.5)
            Card.Position = UDim2.new(0.5,0,0.5,60)
            Card.Size = UDim2.new(0,420,0,210)
            Card.BackgroundColor3 = Color3.fromRGB(5,8,18)
            Card.BackgroundTransparency = 0.15
            Card.ZIndex = 10
            local cardCorner = Instance.new("UICorner", Card)
            cardCorner.CornerRadius = UDim.new(0,18)
            local cardStroke = Instance.new("UIStroke", Card)
            cardStroke.Thickness = 2.2
            cardStroke.Color = Color3.fromRGB(80,220,255)
            cardStroke.Transparency = 0.25
            local cardGradient = Instance.new("UIGradient", Card)
            cardGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Color3.fromRGB(10,25,45)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(4,12,25)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(10,25,45))
            }
            cardGradient.Rotation = -45

            -- Header
            local Header = Instance.new("TextLabel", Card)
            Header.Size = UDim2.new(1,-80,0,40)
            Header.Position = UDim2.new(0,40,0,12)
            Header.BackgroundTransparency = 1
            Header.Text = "RYUU HUB V2 • PREMIUM ACCESS"
            Header.Font = Enum.Font.GothamBlack
            Header.TextSize = 16
            Header.TextColor3 = Color3.fromRGB(120,240,255)
            Header.TextXAlignment = Enum.TextXAlignment.Left
            Header.ZIndex = 12

            local SubHeader = Instance.new("TextLabel", Card)
            SubHeader.Size = UDim2.new(1,-80,0,18)
            SubHeader.Position = UDim2.new(0,40,0,38)
            SubHeader.BackgroundTransparency = 1
            SubHeader.Text = "Enter your premium authorization key"
            SubHeader.Font = Enum.Font.Gotham
            SubHeader.TextSize = 11
            SubHeader.TextColor3 = Color3.fromRGB(90,180,220)
            SubHeader.TextXAlignment = Enum.TextXAlignment.Left
            SubHeader.ZIndex = 12

            -- Input field
            local InputFrame = Instance.new("Frame", Card)
            InputFrame.Size = UDim2.new(1,-80,0,48)
            InputFrame.Position = UDim2.new(0,40,0,70)
            InputFrame.BackgroundColor3 = Color3.fromRGB(8,18,32)
            InputFrame.ZIndex = 11
            local inputCorner = Instance.new("UICorner", InputFrame)
            inputCorner.CornerRadius = UDim.new(0,10)
            local inputStroke = Instance.new("UIStroke", InputFrame)
            inputStroke.Thickness = 1.4
            inputStroke.Color = Color3.fromRGB(60,200,255)
            inputStroke.Transparency = 0.3

            local InputBox = Instance.new("TextBox", InputFrame)
            InputBox.Size = UDim2.new(1,-16,1,0)
            InputBox.Position = UDim2.new(0,8,0,0)
            InputBox.BackgroundTransparency = 1
            InputBox.PlaceholderText = "••••••••-••••-••••-••••-••••••••••••"
            InputBox.PlaceholderColor3 = Color3.fromRGB(60,120,180)
            InputBox.Text = ""
            InputBox.TextColor3 = Color3.fromRGB(180,240,255)
            InputBox.Font = Enum.Font.GothamBold
            InputBox.TextSize = 15
            InputBox.ClearTextOnFocus = false
            InputBox.TextXAlignment = Enum.TextXAlignment.Left
            InputBox.ZIndex = 12

            -- Status label
            local Status = Instance.new("TextLabel", Card)
            Status.Size = UDim2.new(1,-80,0,20)
            Status.Position = UDim2.new(0,40,0,130)
            Status.BackgroundTransparency = 1
            Status.Text = "Awaiting key sequence..."
            Status.Font = Enum.Font.Gotham
            Status.TextSize = 11
            Status.TextColor3 = Color3.fromRGB(100,180,220)
            Status.TextXAlignment = Enum.TextXAlignment.Left
            Status.ZIndex = 12

            -- Verify Button
            local VerifyBtn = Instance.new("TextButton", Card)
            VerifyBtn.Size = UDim2.new(1,-80,0,42)
            VerifyBtn.Position = UDim2.new(0,40,0,158)
            VerifyBtn.BackgroundColor3 = Color3.fromRGB(0,90,160)
            VerifyBtn.Text = "VERIFY & AUTHENTICATE"
            VerifyBtn.Font = Enum.Font.GothamBlack
            VerifyBtn.TextSize = 14
            VerifyBtn.TextColor3 = Color3.fromRGB(220,255,255)
            VerifyBtn.ZIndex = 12
            local btnCorner = Instance.new("UICorner", VerifyBtn)
            btnCorner.CornerRadius = UDim.new(0,10)
            local btnStroke = Instance.new("UIStroke", VerifyBtn)
            btnStroke.Thickness = 1.6
            btnStroke.Color = Color3.fromRGB(120,240,255)

            VerifyBtn.MouseEnter:Connect(function()
                TS:Create(VerifyBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0,120,200)}):Play()
            end)
            VerifyBtn.MouseLeave:Connect(function()
                TS:Create(VerifyBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0,90,160)}):Play()
            end)

            -- Close button
            local CloseBtn = Instance.new("TextButton", Card)
            CloseBtn.Size = UDim2.new(0,32,0,32)
            CloseBtn.Position = UDim2.new(1,-42,0,8)
            CloseBtn.BackgroundTransparency = 1
            CloseBtn.Text = "×"
            CloseBtn.Font = Enum.Font.GothamBold
            CloseBtn.TextSize = 22
            CloseBtn.TextColor3 = Color3.fromRGB(180,180,200)
            CloseBtn.ZIndex = 13

            local function closeScanner()
                playSound(4590662766, 0.4, 0.9)
                TS:Create(Overlay, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
                TS:Create(Card, TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
                    Size = UDim2.new(0,0,0,0), Position = UDim2.new(0.5,0,1.2,0)
                }):Play()
                TS:Create(CoreOrb, TweenInfo.new(0.4), {Size = UDim2.new(0,0,0,0)}):Play()
                task.wait(0.45)
                KSG:Destroy()
            end

            CloseBtn.MouseButton1Click:Connect(closeScanner)

            -- Entrance animation
            Card.Size = UDim2.new(0,0,0,0)
            TS:Create(Card, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(0,420,0,210)
            }):Play()

            -- ── VERIFICATION LOGIC ───────────────────────────────────────
            local function doVerify()
                local key = InputBox.Text:match("^%s*(.-)%s*$") or ""
                if #key < 6 then
                    Status.Text = "Key sequence too short"
                    Status.TextColor3 = Color3.fromRGB(255,160,60)
                    playSound(6042053626, 0.5, 0.8)
                    return
                end

                InputBox.TextEditable = false
                VerifyBtn.Active = false
                Status.Text = "AUTHENTICATING..."
                Status.TextColor3 = Color3.fromRGB(120,240,255)

                -- Scanning effect
                playSound(9114261993, 0.5, 1.05) -- scanning hum

                local scanActive = true
                task.spawn(function()
                    local t = 0
                    while scanActive and Card.Parent do
                        t = t + 0.04
                        local hue = math.sin(t * 3) * 0.5 + 0.5
                        local col = Color3.fromHSV(hue * 0.15 + 0.55, 0.9, 0.95)
                        cardStroke.Color = col
                        orbStroke.Color = col
                        task.wait(0.04)
                    end
                end)

                task.wait(1.4 + math.random()*0.4)
                scanActive = false

                local result = ValidateKey(key)

                if result == "ok" then
                    -- ── SUCCESS SEQUENCE ────────────────────────────────
                    playSound(1847668471, 0.7, 1.0)     -- success base
                    task.delay(0.1, function() playSound(1847668590, 0.6, 1.3) end) -- high chime
                    task.delay(0.25, function() playSound(1847668590, 0.5, 1.6) end) -- sparkle

                    Status.Text = "AUTHENTICATION SUCCESSFUL"
                    Status.TextColor3 = Color3.fromRGB(80,255,140)

                    TS:Create(cardStroke, TweenInfo.new(0.4), {Color = Color3.fromRGB(60,255,140)}):Play()
                    TS:Create(orbStroke, TweenInfo.new(0.4), {Color = Color3.fromRGB(100,255,160)}):Play()
                    TS:Create(InputFrame, TweenInfo.new(0.4), {BackgroundColor3 = Color3.fromRGB(10,40,20)}):Play()

                    -- Particle burst on success
                    for i = 1, 18 do
                        task.delay(i*0.04, function()
                            local p = Instance.new("Frame", KSG)
                            p.AnchorPoint = Vector2.new(0.5,0.5)
                            p.Position = CoreOrb.Position
                            p.Size = UDim2.new(0,6,0,6)
                            p.BackgroundColor3 = Color3.fromRGB(120,255,180)
                            p.BackgroundTransparency = 0.2
                            p.ZIndex = 20
                            Instance.new("UICorner", p).CornerRadius = UDim.new(1,0)
                            TS:Create(p, TweenInfo.new(0.9, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                                Position = UDim2.new(0.5, math.random(-180,180), 0.5, math.random(-180,180)),
                                Size = UDim2.new(0,0,0,0),
                                BackgroundTransparency = 1
                            }):Play()
                            Debris:AddItem(p, 1.2)
                        end)
                    end

                    -- Flash + confirmation
                    local successFlash = Instance.new("Frame", KSG)
                    successFlash.Size = UDim2.new(1,0,1,0)
                    successFlash.BackgroundColor3 = Color3.fromRGB(80,255,140)
                    successFlash.BackgroundTransparency = 0.7
                    successFlash.ZIndex = 999
                    TS:Create(successFlash, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
                    Debris:AddItem(successFlash, 0.8)

                    PremiumUnlocked = true
                    PremiumActive = true
                    task.spawn(RemovePremiumLock)
                    Rayfield:Notify({
                        Title = "✨ Premium Activated",
                        Content = "Full access granted. Enjoy Ryuu V2 Premium.",
                        Duration = 6,
                        Image = 4483362458
                    })

                    if S.AutoParry then
                        StopEngine()
                        task.wait(0.08)
                        P.AutoParry = true
                        StartPremiumEngine()
                    end

                    task.wait(1.8)
                    closeScanner()
                else
                    -- ── FAILURE SEQUENCE ────────────────────────────────
                    playSound(6042053626, 0.65, 0.75)
                    Status.Text = result == "locked_other" and "DEVICE MISMATCH — ACCESS DENIED" or "INVALID KEY SEQUENCE"
                    Status.TextColor3 = Color3.fromRGB(255,80,80)

                    TS:Create(cardStroke, TweenInfo.new(0.25), {Color = Color3.fromRGB(220,60,60)}):Play()
                    TS:Create(orbStroke, TweenInfo.new(0.25), {Color = Color3.fromRGB(255,100,100)}):Play()

                    -- Red shake + flash
                    local failFlash = Instance.new("Frame", KSG)
                    failFlash.Size = UDim2.new(1,0,1,0)
                    failFlash.BackgroundColor3 = Color3.fromRGB(220,40,40)
                    failFlash.BackgroundTransparency = 0.65
                    failFlash.ZIndex = 999
                    TS:Create(failFlash, TweenInfo.new(0.25), {BackgroundTransparency = 1}):Play()
                    Debris:AddItem(failFlash, 0.7)

                    -- Shake animation
                    for i = 1, 8 do
                        task.delay((i-1)*0.04, function()
                            local offset = (i % 2 == 0) and 8 or -8
                            Card.Position = UDim2.new(0.5, offset, 0.5, 60 + offset*0.5)
                        end)
                    end
                    task.delay(0.36, function()
                        Card.Position = UDim2.new(0.5,0,0.5,60)
                    end)

                    task.wait(1.6)
                    InputBox.Text = ""
                    InputBox.TextEditable = true
                    VerifyBtn.Active = true
                    Status.Text = "Enter premium key..."
                    Status.TextColor3 = Color3.fromRGB(100,180,220)
                    cardStroke.Color = Color3.fromRGB(80,220,255)
                end
            end

            VerifyBtn.MouseButton1Click:Connect(doVerify)
            InputBox.FocusLost:Connect(function(enterPressed)
                if enterPressed then doVerify() end
            end)

        end)
    end
})
PT:CreateDivider()
PT:CreateSection("⚡ Premium Parry Engine")
PT:CreateLabel("Quintic prediction · 5-point acceleration · 0.15s cooldown · God Emergency")
PT:CreateToggle({Name="Premium Auto Parry",CurrentValue=false,Flag="PAP",
    Callback=function(v)
        PremGuard(v,"Premium Parry",function(val)
            if val then
                StopEngine(); P.AutoParry=true; StartPremiumEngine()
                Rayfield:Notify({Title="⚡ Premium Parry ON",Content="Quintic engine active.",Duration=3,Image=4483362458})
            else StopPremiumEngine(); P.AutoParry=false end
        end)
    end})
PT:CreateToggle({Name="God Emergency (2.5 studs)",CurrentValue=false,Flag="PGE",
    Callback=function(v) PremGuard(v,"God Emergency",function(val) P.GodEmergency=val end) end})
PT:CreateToggle({Name="Curve Crusher",CurrentValue=true,Flag="PCC",
    Callback=function(v) PremGuard(v,"Curve Crusher",function(val) P.CurveCrusher=val end) end})
PT:CreateToggle({Name="Auto Face Ball (Premium)",CurrentValue=false,Flag="PAF",
    Callback=function(v) PremGuard(v,"Auto Face",function(val) P.AutoFace=val end) end})
PT:CreateToggle({Name="Boost Detection",CurrentValue=true,Flag="PABD",
    Callback=function(v) PremGuard(v,"Boost Detect",function(val) P.SpeedBoostDetect=val end) end})
PT:CreateDivider()

PT:CreateSection("🎯 Premium Hitbox")
PT:CreateSlider({Name="Hitbox Target",Range={2.5,6.0},Increment=0.1,CurrentValue=4.0,Flag="PHB",
    Callback=function(v)
        PremGuard(v,"Hitbox",function(val) P.HitboxTarget=val end)
    end})
PT:CreateDivider()

PT:CreateSection("⚡ Premium Ability Detector")
PT:CreateLabel("10 signatures: Fury · Time Hole · Duality · Dragon · Pull · Infinity · Phantom · Hook · Thunder · Doppelganger")
PT:CreateToggle({Name="Premium Ability Detector",CurrentValue=false,Flag="PADET",
    Callback=function(v)
        PremGuard(v,"Prem Detector",function(val)
            if val then StartPremAbilDetect()
                Rayfield:Notify({Title="⚡ Prem Detector ON",Content="10 abilities monitored.",Duration=3,Image=4483362458})
            else StopPremAbilDetect() end
        end)
    end})
PT:CreateSlider({Name="Detect Cooldown (per enemy)",Range={10,100},Increment=5,CurrentValue=40,Suffix="0ms",Flag="PDETCD",
    Callback=function(v) if PremiumUnlocked then PDet.cd=v/100 end end})
PT:CreateDivider()

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM TAB ◈ EXTENDED FEATURE SECTIONS
-- ══════════════════════════════════════════════════════════════════

-- ───────────────── ⚔ PARRY EXTRAS ────────────────────────────────
PT:CreateDivider()
PT:CreateSection("⚔ Parry Extras")

PT:CreateToggle({Name="Parry Sound (Sword Clash)",CurrentValue=false,Flag="PFS",
    Callback=function(v) PremGuard(v,"Parry Sound",function(val) PF.ParrySound=val end) end})

PT:CreateToggle({Name="Anti-Block (auto-fire on blockers)",CurrentValue=false,Flag="PAB",
    Callback=function(v)
        PremGuard(v,"Anti-Block",function(val)
            PF.AntiBlock=val
            if val then StartAntiBlock()
                Rayfield:Notify({Title="🛡 Anti-Block ON",Content="Auto-fires on blocker abilities.",Duration=3,Image=4483362458})
            else if AntiBlockConn then AntiBlockConn:Disconnect() end end
        end)
    end})

PT:CreateToggle({Name="Timing Randomizer (human delay)",CurrentValue=false,Flag="PTR",
    Callback=function(v)
        PremGuard(v,"Timing Random",function(val)
            PF.TimingRandom=val
            Rayfield:Notify({Title="🎲 Timing Randomizer "..(val and "ON" or "OFF"),
                Content=val and "Adds ±30ms human jitter." or "Removed.",Duration=3,Image=4483362458})
        end)
    end})

PT:CreateToggle({Name="Combo Tracker (floating widget)",CurrentValue=false,Flag="PCT",
    Callback=function(v)
        PremGuard(v,"Combo Tracker",function(val)
            PF.ComboTracker=val
            if val then
                BuildComboWidget()
                Rayfield:Notify({Title="🔥 Combo Tracker ON",Content="Drag the widget to reposition.",Duration=3,Image=4483362458})
            else
                PF.ComboCount=0
                pcall(function() if ComboSG then ComboSG:Destroy() end end)
            end
        end)
    end})

PT:CreateToggle({Name="Auto Chat after Parry",CurrentValue=false,Flag="PAC",
    Callback=function(v)
        PremGuard(v,"Auto Chat",function(val)
            PF.AutoChat=val
            Rayfield:Notify({Title="💬 Auto Chat "..(val and "ON" or "OFF"),
                Content=val and "Will chat after each parry." or "Silenced.",Duration=3,Image=4483362458})
        end)
    end})

PT:CreateDropdown({
    Name="Auto Chat Message Style",
    Options={"Rotate all messages","nice try 😏","too slow ⚡","skill issue 💯","not even close 🎯","L + ratio 😴"},
    CurrentOption={"Rotate all messages"},MultipleOptions=false,Flag="PACM",
    Callback=function(v)
        local pick=type(v)=="table" and v[1] or tostring(v)
        if pick=="Rotate all messages" then
            AutoChatIdx=1
        else
            for i,m in ipairs(AUTO_CHAT_MSGS) do
                if m==pick then AutoChatIdx=i; break end
            end
        end
    end})

-- ───────────────── 🔮 PREDICTION & BALL TRACKING ─────────────────
PT:CreateDivider()
PT:CreateSection("🔮 Prediction & Ball Tracking")

PT:CreateToggle({Name="Trajectory Line ESP",CurrentValue=false,Flag="PTESP",
    Callback=function(v)
        PremGuard(v,"Trajectory ESP",function(val)
            PF.TrajectoryESP=val
            if val then
                BuildTrajectoryESP()
                Rayfield:Notify({Title="🔮 Trajectory ESP ON",Content="Neon line shows ball path.",Duration=3,Image=4483362458})
            else pcall(function() if TrajSG then TrajSG:Destroy() end end) end
        end)
    end})

PT:CreateToggle({Name="Ball Speed Meter",CurrentValue=false,Flag="PBSM",
    Callback=function(v)
        PremGuard(v,"Speed Meter",function(val)
            PF.BallSpeedMeter=val
            if val then
                BuildSpeedMeter()
                Rayfield:Notify({Title="⚡ Speed Meter ON",Content="Bottom-center widget.",Duration=3,Image=4483362458})
            else pcall(function() if SpeedMeterSG then SpeedMeterSG:Destroy() end end) end
        end)
    end})

PT:CreateToggle({Name="Time-to-Impact (top of screen)",CurrentValue=false,Flag="PTTI",
    Callback=function(v)
        PremGuard(v,"Time-to-Impact",function(val)
            PF.TimeToImpact=val
            if val then
                BuildTTI()
                Rayfield:Notify({Title="⏱ TTI ON",Content="Shows seconds until ball hits you.",Duration=3,Image=4483362458})
            else pcall(function() if TTISg then TTISg:Destroy() end end) end
        end)
    end})

PT:CreateToggle({Name="Ball Target Alert (red edge flash)",CurrentValue=false,Flag="PBTA",
    Callback=function(v)
        PremGuard(v,"Target Alert",function(val)
            PF.TargetAlert=val
            if val then
                BuildTargetAlert()
                Rayfield:Notify({Title="🚨 Target Alert ON",Content="Red edges flash when targeted.",Duration=3,Image=4483362458})
            else pcall(function() if AlertSG then AlertSG:Destroy() end end) end
        end)
    end})

-- ───────────────── 👁 VISUAL & ESP ───────────────────────────────
PT:CreateDivider()
PT:CreateSection("👁 Visual & ESP")

PT:CreateToggle({Name="Distance ESP (all players)",CurrentValue=false,Flag="PDESP",
    Callback=function(v)
        PremGuard(v,"Dist ESP",function(val)
            PF.DistESP=val
            if not val then UpdateDistESP() end
            Rayfield:Notify({Title="📏 Distance ESP "..(val and "ON" or "OFF"),
                Content=val and "Shows st distance above heads." or "Removed.",Duration=3,Image=4483362458})
        end)
    end})

PT:CreateToggle({Name="Chams (see through walls)",CurrentValue=false,Flag="PCH",
    Callback=function(v)
        PremGuard(v,"Chams",function(val)
            PF.Chams=val; ApplyChams(val)
            Rayfield:Notify({Title="🌐 Chams "..(val and "ON" or "OFF"),
                Content=val and "Neon players visible through walls." or "Restored.",Duration=3,Image=4483362458})
        end)
    end})

PT:CreateToggle({Name="Ball Highlight (red = targeting you)",CurrentValue=false,Flag="PBH",
    Callback=function(v)
        PremGuard(v,"Ball Highlight",function(val)
            PF.BallHighlight=val
            if val then StartBallHighlight()
                Rayfield:Notify({Title="🎯 Ball Highlight ON",Content="Red = targeting you, cyan = other.",Duration=3,Image=4483362458})
            else if BallHighlightConn then BallHighlightConn:Disconnect() end end
        end)
    end})

PT:CreateToggle({Name="Rainbow Ball Trail",CurrentValue=false,Flag="PRBT",
    Callback=function(v)
        PremGuard(v,"Rainbow Trail",function(val)
            PF.RainbowTrail=val
            if val then StartRainbowTrail()
                Rayfield:Notify({Title="🌈 Rainbow Trail ON",Content="Ball cycles full spectrum.",Duration=3,Image=4483362458})
            else if RainbowTrailConn then RainbowTrailConn:Disconnect() end end
        end)
    end})

-- ───────────────── 🏃 MOVEMENT & COMBAT ──────────────────────────
PT:CreateDivider()
PT:CreateSection("🏃 Movement & Combat")

PT:CreateToggle({Name="Auto Dodge (step back on target)",CurrentValue=false,Flag="PAD",
    Callback=function(v)
        PremGuard(v,"Auto Dodge",function(val)
            PF.AutoDodge=val
            if val then StartAutoDodge()
                Rayfield:Notify({Title="💨 Auto Dodge ON",Content="Steps away when ball targets you.",Duration=3,Image=4483362458})
            else if DodgeConn then DodgeConn:Disconnect() end end
        end)
    end})

PT:CreateToggle({Name="Speed Boost on Parry",CurrentValue=false,Flag="PSBP",
    Callback=function(v) PremGuard(v,"Speed Boost",function(val) PF.SpeedOnParry=val end) end})

PT:CreateSlider({Name="Boost Speed Amount",Range={20,100},Increment=5,CurrentValue=40,Flag="PSBA",
    Callback=function(v) PremGuard(v,"Speed Boost Amt",function(val) PF.SpeedOnParryAmt=val end) end})

PT:CreateToggle({Name="Ghost Mode (85% transparent + noclip)",CurrentValue=false,Flag="PGM",
    Callback=function(v)
        PremGuard(v,"Ghost Mode",function(val)
            PF.GhostMode=val; ApplyGhost(val)
            Rayfield:Notify({Title="👻 Ghost Mode "..(val and "ON" or "OFF"),
                Content=val and "85% transparent + noclip." or "Restored.",Duration=3,Image=4483362458})
        end)
    end})

PT:CreateToggle({Name="Safe Zone Warning (vignette)",CurrentValue=false,Flag="PSZW",
    Callback=function(v)
        PremGuard(v,"Safe Zone Warn",function(val)
            PF.SafeZoneWarn=val
            if val then BuildSafeZoneWarning()
                Rayfield:Notify({Title="⚠ Safe Zone Warn ON",Content="Orange vignette when ball targets you.",Duration=3,Image=4483362458})
            else pcall(function() if SafeZoneSG then SafeZoneSG:Destroy() end end) end
        end)
    end})

-- ───────────────── 📊 HUD & UI ───────────────────────────────────
PT:CreateDivider()
PT:CreateSection("📊 HUD & UI")

PT:CreateToggle({Name="Premium FPS Unlocker",CurrentValue=false,Flag="PFPS",
    Callback=function(v)
        PremGuard(v,"FPS Unlock",function(val)
            PF.PremFPS=val; SetPremFPS(val, PF.PremFPSCap)
            Rayfield:Notify({Title="🔓 FPS Unlocker "..(val and "ON" or "OFF"),
                Content=val and "FPS cap removed — runs as fast as possible." or "FPS restored to 60.",Duration=4,Image=4483362458})
        end)
    end})

PT:CreateDropdown({
    Name="FPS Cap (when unlocked)",
    Options={"Uncapped","144","240","360"},
    CurrentOption={"Uncapped"},MultipleOptions=false,Flag="PFPSC",
    Callback=function(v)
        local pick=type(v)=="table" and v[1] or tostring(v)
        PF.PremFPSCap = pick=="Uncapped" and 0 or tonumber(pick) or 0
        if PF.PremFPS then SetPremFPS(true,PF.PremFPSCap) end
    end})

PT:CreateToggle({Name="Kill Tracker (widget)",CurrentValue=false,Flag="PKT",
    Callback=function(v)
        PremGuard(v,"Kill Tracker",function(val)
            PF.KillTracker=val
            if val then
                BuildKillWidget()
                Rayfield:Notify({Title="☠ Kill Tracker ON",Content="Counts offensive parry kills.",Duration=3,Image=4483362458})
            else pcall(function() if KillSG then KillSG:Destroy() end end) end
        end)
    end})

PT:CreateButton({Name="☠ Reset Kill Count",Callback=function()
    PremGuard(true,"Kill Tracker",function()
        PF.KillCount=0
        pcall(function() if KillLbl then KillLbl.Text="0" end end)
        Rayfield:Notify({Title="Kill Count Reset",Content="Back to 0.",Duration=2,Image=4483362458})
    end)
end})

PT:CreateToggle({Name="Premium Watermark Badge",CurrentValue=false,Flag="PWM",
    Callback=function(v)
        PremGuard(v,"Watermark",function(val)
            PF.PremWatermark=val
            if val then
                BuildWatermark()
                Rayfield:Notify({Title="◈ Watermark ON",Content="Ryuu HUB V2 badge bottom-left.",Duration=3,Image=4483362458})
            else pcall(function() if WatermarkSG then WatermarkSG:Destroy() end end) end
        end)
    end})

-- ───────────────── 📊 STATUS ──────────────────────────────────────
PT:CreateDivider()
PT:CreateSection("📊 Status")
PT:CreateButton({Name="Check Premium Status",Callback=function()
    if PremiumUnlocked then
        local active={}
        if PF.ParrySound    then table.insert(active,"🔊 Sound") end
        if PF.AntiBlock     then table.insert(active,"🛡 Anti-Block") end
        if PF.ComboTracker  then table.insert(active,"🔥 Combo") end
        if PF.TrajectoryESP then table.insert(active,"🔮 Traj ESP") end
        if PF.BallSpeedMeter then table.insert(active,"⚡ Speed") end
        if PF.DistESP       then table.insert(active,"📏 DistESP") end
        if PF.Chams         then table.insert(active,"🌐 Chams") end
        if PF.RainbowTrail  then table.insert(active,"🌈 Trail") end
        if PF.AutoDodge     then table.insert(active,"💨 Dodge") end
        if PF.GhostMode     then table.insert(active,"👻 Ghost") end
        if PF.PremFPS       then table.insert(active,"🔓 FPS") end
        if PF.KillTracker   then table.insert(active,"☠ Kills:"..PF.KillCount) end
        local summary=#active>0 and table.concat(active," · ") or "No extras active"
        Rayfield:Notify({Title="✨ Premium ACTIVE",Content=summary,Duration=6,Image=4483362458})
    else
        Rayfield:Notify({Title="🔒 Not Unlocked",Content="Open Key Scanner to unlock.",Duration=4,Image=4483362458})
    end
end})

-- ── TAB: SETTINGS ─────────────────────────────────────────────────
local T5=W:CreateTab("⚙ Settings",nil)
T5:CreateSection("Keybinds")
T5:CreateKeybind({Name="Toggle Auto Parry",CurrentKeybind="F5",HoldToInteract=false,Flag="KB2",
    Callback=function()
        S.AutoParry=not S.AutoParry
        if S.AutoParry then StartEngine() else StopEngine() end
        Rayfield:Notify({Title="Auto Parry",Content=S.AutoParry and "ON" or "OFF",Duration=2,Image=4483362458})
    end})
T5:CreateKeybind({Name="Toggle Ball ESP",CurrentKeybind="F8",HoldToInteract=false,Flag="KB3",
    Callback=function()
        S.BallESP=not S.BallESP
        if S.BallESP then StartESP() else CleanESP() end
    end})
T5:CreateDivider()
T5:CreateSection("About")
T5:CreateLabel("Ryuu's HUB V2  ·  by ZivX")
T5:CreateDivider()
T5:CreateSection("🔗 Discord")
T5:CreateButton({Name="📋 Copy Discord Link",Callback=function()
    local link="https://discord.gg/S2fKywJutE"
    local ok=pcall(function() setclipboard(link) end)
    if ok then
        Rayfield:Notify({Title="Discord Copied!",Content=link,Duration=4,Image=4483362458})
    else
        Rayfield:Notify({Title="discord.gg/S2fKywJutE",Content="Copy manually.",Duration=6,Image=4483362458})
    end
end})
