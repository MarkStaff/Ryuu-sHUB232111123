-- ══════════════════════════════════════════════════════════════════
-- Ryuu's HUB V1 BETA
-- by ZivX ◈ Auto Parry + Ability ESP + Sword + FPS + Spam
-- ══════════════════════════════════════════════════════════════════
-- ── Services ─────────────────────────────────────────────────────
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VIM = game:GetService("VirtualInputManager")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")
local Plr = Players.LocalPlayer
local Cam = workspace.CurrentCamera
-- ── Destroy old instances ────────────────────────────────────────
pcall(function()
    for _, g in ipairs(CoreGui:GetChildren()) do
        if g.Name:find("Ryuu") then pcall(function() g:Destroy() end) end
    end
end)
-- ══════════════════════════════════════════════════════════════════
-- LOAD RAYFIELD ◈ Synchronous + clean intro
-- ══════════════════════════════════════════════════════════════════
local Rayfield
local function makeStub()
    local function makeTab()
        return {
            CreateToggle = function(_,t) if t and t.Callback then t.Callback(t.CurrentValue or false) end end,
            CreateSlider = function(_,t) if t and t.Callback then t.Callback(t.CurrentValue or 0) end end,
            CreateLabel = function() return {Text=""} end,
            CreateSection = function() end,
            CreateDivider = function() end,
            CreateButton = function() end,
            CreateKeybind = function() end,
            CreateInput = function() end,
            CreateDropdown = function(_,t) if t and t.Callback then t.Callback({t.CurrentOption and t.CurrentOption[1] or ""}) end end,
        }
    end
    return {
        CreateWindow = function() return {CreateTab=function() return makeTab() end} end,
        Notify = function() end,
    }
end
-- Step 1: Ryuu Script animated intro
do  -- scope: intro screen gui locals
local IntroSG = Instance.new("ScreenGui")
IntroSG.Name = "RyuuIntro"
IntroSG.ResetOnSpawn = false
IntroSG.IgnoreGuiInset = true
IntroSG.DisplayOrder = 9999
pcall(function()
    IntroSG.Parent = (typeof(gethui)=="function" and gethui()) or game:GetService("CoreGui")
end)
if not IntroSG.Parent then
    pcall(function() IntroSG.Parent = Plr:WaitForChild("PlayerGui") end)
end
-- ── Background ─────────────────────────────────────────────────
local IBG = Instance.new("Frame", IntroSG)
IBG.Size = UDim2.new(1,0,1,0)
IBG.BackgroundColor3 = Color3.fromRGB(3,1,8)
IBG.BorderSizePixel = 0

-- ── Ring ─────────────────────────────────────────────────────────
local Ring = Instance.new("Frame", IBG)
Ring.AnchorPoint = Vector2.new(0.5,0.5)
Ring.Position = UDim2.new(0.5,0,0.46,0)
Ring.Size = UDim2.new(0,0,0,0)
Ring.BackgroundTransparency = 1
Ring.BorderSizePixel = 0
Ring.ZIndex = 2
Instance.new("UICorner", Ring).CornerRadius = UDim.new(1,0)
local RingStroke = Instance.new("UIStroke", Ring)
RingStroke.Thickness = 1.5
RingStroke.Color = Color3.fromRGB(130,50,255)

-- ── Inner ring ───────────────────────────────────────────────────
local Ring2 = Instance.new("Frame", IBG)
Ring2.AnchorPoint = Vector2.new(0.5,0.5)
Ring2.Position = UDim2.new(0.5,0,0.46,0)
Ring2.Size = UDim2.new(0,0,0,0)
Ring2.BackgroundTransparency = 1
Ring2.BorderSizePixel = 0
Ring2.ZIndex = 2
Instance.new("UICorner", Ring2).CornerRadius = UDim.new(1,0)
local Ring2Stroke = Instance.new("UIStroke", Ring2)
Ring2Stroke.Thickness = 1
Ring2Stroke.Color = Color3.fromRGB(180,90,255)
Ring2Stroke.Transparency = 0.4

-- ── Centre orb ───────────────────────────────────────────────────
local Orb = Instance.new("Frame", IBG)
Orb.AnchorPoint = Vector2.new(0.5,0.5)
Orb.Position = UDim2.new(0.5,0,0.46,0)
Orb.Size = UDim2.new(0,0,0,0)
Orb.BackgroundColor3 = Color3.fromRGB(150,60,255)
Orb.BackgroundTransparency = 0.3
Orb.BorderSizePixel = 0
Orb.ZIndex = 3
Instance.new("UICorner", Orb).CornerRadius = UDim.new(1,0)

-- ── RYUU title ───────────────────────────────────────────────────
local ITitle = Instance.new("TextLabel", IBG)
ITitle.AnchorPoint = Vector2.new(0.5,0.5)
ITitle.Position = UDim2.new(0.5,0,0.52,0)
ITitle.Size = UDim2.new(0,300,0,70)
ITitle.BackgroundTransparency = 1
ITitle.Text = "RYUU"
ITitle.Font = Enum.Font.GothamBlack
ITitle.TextSize = 62
ITitle.TextColor3 = Color3.fromRGB(255,255,255)
ITitle.TextTransparency = 1
ITitle.ZIndex = 4
local ITG = Instance.new("UIGradient", ITitle)
ITG.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0,  Color3.fromRGB(180,90,255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255,255,255)),
    ColorSequenceKeypoint.new(1,  Color3.fromRGB(180,90,255)),
}
ITG.Rotation = 90

-- ── Subtitle ─────────────────────────────────────────────────────
local ISub = Instance.new("TextLabel", IBG)
ISub.AnchorPoint = Vector2.new(0.5,0.5)
ISub.Position = UDim2.new(0.5,0,0.62,0)
ISub.Size = UDim2.new(0,260,0,20)
ISub.BackgroundTransparency = 1
ISub.Text = "— S C R I P T —"
ISub.Font = Enum.Font.GothamBold
ISub.TextSize = 11
ISub.TextColor3 = Color3.fromRGB(150,90,255)
ISub.TextTransparency = 1
ISub.ZIndex = 4

-- ── Loading label ─────────────────────────────────────────────────
local ILbl = Instance.new("TextLabel", IBG)
ILbl.AnchorPoint = Vector2.new(0.5,0.5)
ILbl.Position = UDim2.new(0.5,0,0.72,0)
ILbl.Size = UDim2.new(0,200,0,18)
ILbl.BackgroundTransparency = 1
ILbl.Text = "Loading..."
ILbl.Font = Enum.Font.Gotham
ILbl.TextSize = 11
ILbl.TextColor3 = Color3.fromRGB(90,55,160)
ILbl.ZIndex = 4

-- ── IStroke alias (used by post-load READY pulse) ─────────────────
local IStroke = RingStroke

-- ── Run animation ─────────────────────────────────────────────────
-- Phase 1: orb pops, ring expands (sequential, reliable)
TweenService:Create(Orb, TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size=UDim2.new(0,50,0,50)}):Play()
TweenService:Create(Ring, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size=UDim2.new(0,260,0,260)}):Play()
task.wait(0.1)
TweenService:Create(Ring2, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size=UDim2.new(0,200,0,200)}):Play()
TweenService:Create(Orb, TweenInfo.new(0.3, Enum.EasingStyle.Quad),
    {Size=UDim2.new(0,20,0,20), BackgroundTransparency=0.7}):Play()
task.wait(0.25)

-- Phase 2: RYUU slams in
ITitle.Position = UDim2.new(0.5,0,0.56,0)
TweenService:Create(ITitle, TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Position=UDim2.new(0.5,0,0.52,0), TextTransparency=0}):Play()
task.wait(0.18)

-- Phase 3: subtitle fades in
TweenService:Create(ISub, TweenInfo.new(0.25), {TextTransparency=0}):Play()

-- Gentle ring pulse while loading
task.spawn(function()
    while IntroSG and IntroSG.Parent do
        TweenService:Create(RingStroke, TweenInfo.new(1, Enum.EasingStyle.Sine),
            {Transparency=0.5}):Play()
        task.wait(1)
        TweenService:Create(RingStroke, TweenInfo.new(1, Enum.EasingStyle.Sine),
            {Transparency=0}):Play()
        task.wait(1)
    end
end)

task.spawn(function()
    local dots = {"Loading.","Loading..","Loading...","Loading...."}
    local i = 1
    while true do
        local ok = pcall(function()
            if not IntroSG or not IntroSG.Parent then error("done") end
            ILbl.Text = dots[i]
        end)
        if not ok then break end
        i = i % #dots + 1
        task.wait(0.3)
    end
end)

local RF_URLS = {
    "https://sirius.menu/rayfield",
    "https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua",
    "https://raw.githubusercontent.com/UI-Interface/Rayfield/main/source.lua",
}
for _, url in ipairs(RF_URLS) do
    local ok, res = pcall(function()
        return loadstring(game:HttpGet(url, true))()
    end)
    if ok and res then
        Rayfield = res
        break
    end
end
if not Rayfield then
    warn("[ZivX Ryuu] Rayfield load failed — using stub")
    Rayfield = makeStub()
end
pcall(function()
    ILbl.Text = "READY ✓"
    ILbl.TextColor3 = Color3.fromRGB(80,220,140)
    IStroke.Color = Color3.fromRGB(80,220,140)
end)
task.wait(0.5)
pcall(function()
    local fadeCover = Instance.new("Frame", IntroSG)
    fadeCover.Size = UDim2.new(1,0,1,0)
    fadeCover.BackgroundColor3 = Color3.fromRGB(4,2,10)
    fadeCover.BorderSizePixel = 0
    fadeCover.BackgroundTransparency = 1
    fadeCover.ZIndex = 500
    TweenService:Create(fadeCover,
        TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {BackgroundTransparency = 0}
    ):Play()
    task.wait(0.55)
end)
pcall(function() IntroSG:Destroy() end)
end  -- end intro screen scope
-- ══════════════════════════════════════════════════════════════════
-- ALWAYS-ON HUD
-- ══════════════════════════════════════════════════════════════════
do  -- scope: HUD building locals
local HudSG = Instance.new("ScreenGui")
HudSG.Name = "RyuuHUD"
HudSG.ResetOnSpawn = false
HudSG.IgnoreGuiInset = true
HudSG.DisplayOrder = 9998
pcall(function()
    HudSG.Parent = (typeof(gethui)=="function" and gethui()) or game:GetService("CoreGui")
end)
if not HudSG.Parent then
    pcall(function() HudSG.Parent = Plr:WaitForChild("PlayerGui") end)
end
local HudFrame = Instance.new("Frame", HudSG)
HudFrame.AnchorPoint = Vector2.new(1, 0)
HudFrame.Position = UDim2.new(1, -12, 0, 12)
HudFrame.Size = UDim2.new(0, 160, 0, 54)
HudFrame.BackgroundColor3 = Color3.fromRGB(8, 4, 18)
HudFrame.BackgroundTransparency = 0.25
HudFrame.BorderSizePixel = 0
Instance.new("UICorner", HudFrame).CornerRadius = UDim.new(0, 8)
local HudStroke = Instance.new("UIStroke", HudFrame)
HudStroke.Thickness = 1
HudStroke.Color = Color3.fromRGB(90, 40, 180)
HudStroke.Transparency = 0.4
local HudPing = Instance.new("TextLabel", HudFrame)
HudPing.Size = UDim2.new(1, -12, 0, 22)
HudPing.Position = UDim2.new(0, 8, 0, 6)
HudPing.BackgroundTransparency = 1
HudPing.Text = "📶 Ping: --ms"
HudPing.Font = Enum.Font.GothamBold
HudPing.TextSize = 13
HudPing.TextColor3 = Color3.fromRGB(200, 200, 200)
HudPing.TextXAlignment = Enum.TextXAlignment.Left
local HudFPS = Instance.new("TextLabel", HudFrame)
HudFPS.Size = UDim2.new(1, -12, 0, 22)
HudFPS.Position = UDim2.new(0, 8, 0, 28)
HudFPS.BackgroundTransparency = 1
HudFPS.Text = "🖥 FPS: --"
HudFPS.Font = Enum.Font.GothamBold
HudFPS.TextSize = 13
HudFPS.TextColor3 = Color3.fromRGB(200, 200, 200)
HudFPS.TextXAlignment = Enum.TextXAlignment.Left
local fpsCount, fpsTimer, fpsNow = 0, 0, 0
RunService.Heartbeat:Connect(function(dt)
    fpsCount = fpsCount + 1
    fpsTimer = fpsTimer + dt
    if fpsTimer >= 0.5 then
        fpsNow = math.floor(fpsCount / fpsTimer)
        fpsCount = 0
        fpsTimer = 0
        local fpsCol = fpsNow >= 55 and Color3.fromRGB(34,197,110)
                    or fpsNow >= 30 and Color3.fromRGB(255,200,50)
                    or Color3.fromRGB(255,80,80)
        pcall(function()
            HudFPS.Text = ("🖥 FPS: %d"):format(fpsNow)
            HudFPS.TextColor3 = fpsCol
        end)
    end
    pcall(function()
        local col = PingMs < 60 and Color3.fromRGB(34,197,110)
                 or PingMs < 120 and Color3.fromRGB(255,200,50)
                 or Color3.fromRGB(255,80,80)
        HudPing.Text = ("📶 Ping: %dms"):format(math.floor(PingMs))
        HudPing.TextColor3 = col
    end)
end)
end  -- end HUD scope
-- ══════════════════════════════════════════════════════════════════
-- STATE
-- ══════════════════════════════════════════════════════════════════
local S = {
    AutoParry = false,
    AutoFace = false,
    BallESP = false,
    AbilityESP = false,
    Triggerbot = false,
    Fullbright = false,
    Noclip = false,
    InfJump = false,
    ParryCount = 0,
    WalkSpeed = 16,
    JumpPower = 50,
    LastParry = 0,
    ParryCooldown = 0.32,
    DidParry = false,
    EmergencyParry = true,
    EmergencyDist = 6,
    ParryAccuracy = 100,
    AntiCurve = true,
    HitboxTarget = 5.0,
    SpeedBoostDetect = false,
}
-- ══════════════════════════════════════════════════════════════════
-- KEY SYSTEM
-- ══════════════════════════════════════════════════════════════════
local VALID_KEYS = {
    "RYUU-KEY1434s9df9sdfsdf",
    "RYUU-KEY1a0da90sdacsdaasdx",
    "RYUU-KEY2o3i4j2o3232sxasdv",
    "09153552478",
    "x31THEGOAT",
    "RYUU-KEYS0CF94TEDFGVasd4swdf",
}
local IsWhitelisted = false
local PremiumActive = false
local DEVICE_FILE = "ryuu_device.txt"
local LOCK_FILE = "ryuu_keys.txt"
local function GetDeviceID()
    local ok, stored = pcall(function() return readfile(DEVICE_FILE) end)
    if ok and stored and #stored >= 16 then
        return stored:match("^%s*(.-)%s*$")
    end
    local parts = {}
    pcall(function()
        local id = tostring(game:GetService("RbxAnalyticsService"):GetClientId())
        if id and #id > 4 then table.insert(parts, id) end
    end)
    pcall(function() table.insert(parts, tostring(Plr.UserId)) end)
    table.insert(parts, tostring(math.random(100000, 999999)))
    table.insert(parts, tostring(os.time()))
    local raw = table.concat(parts, "-")
    local hash = 0
    for i = 1, #raw do
        hash = (hash * 31 + string.byte(raw, i)) % 0xFFFFFFFF
    end
    local deviceID = string.format("DEV-%08X-%08X-%06d",
        hash,
        (hash * 1000003) % 0xFFFFFFFF,
        math.random(100000, 999999)
    )
    pcall(function() writefile(DEVICE_FILE, deviceID) end)
    return deviceID
end
local function LoadLocks()
    local ok, data = pcall(function() return readfile(LOCK_FILE) end)
    if not ok or not data or data == "" then return {} end
    local locks = {}
    for line in data:gmatch("[^\n]+") do
        local k, d = line:match("^(.+)=(.+)$")
        if k and d then locks[k] = d end
    end
    return locks
end
local function SaveLocks(locks)
    local lines = {}
    for k, d in pairs(locks) do
        table.insert(lines, k .. "=" .. d)
    end
    pcall(function() writefile(LOCK_FILE, table.concat(lines, "\n")) end)
end
local function ValidateKey(key)
    if not key or #key < 4 then return "invalid" end
    local k = key:match("^%s*(.-)%s*$")
    local keyExists = false
    for _, v in ipairs(VALID_KEYS) do
        if v == k then keyExists = true; break end
    end
    if not keyExists then return "invalid" end
    local myDevice = GetDeviceID()
    local locks = LoadLocks()
    if locks[k] == nil then
        locks[k] = myDevice
        SaveLocks(locks)
        return "ok"
    elseif locks[k] == myDevice then
        return "ok"
    else
        return "locked_other"
    end
end
local Conn = {}
-- ══════════════════════════════════════════════════════════════════
-- BALL HELPERS
-- ══════════════════════════════════════════════════════════════════
local function GetBall()
    local folder = workspace:FindFirstChild("Balls")
    if folder then
        for _, v in ipairs(folder:GetChildren()) do
            if v:IsA("BasePart") and v:GetAttribute("realBall") then return v end
        end
        for _, v in ipairs(folder:GetChildren()) do
            if v:IsA("BasePart") then return v end
        end
    end
    for _, v in ipairs(workspace:GetChildren()) do
        if v:IsA("BasePart") and v.Name:lower():find("ball") then return v end
    end
    return nil
end
local function GetHRP()
    local c = Plr.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end
local function GetVel(ball)
    local best = Vector3.zero
    local ok1, v1 = pcall(function() return ball.AssemblyLinearVelocity end)
    if ok1 and v1.Magnitude > best.Magnitude then best = v1 end
    local ok2, v2 = pcall(function()
        local bv = ball:FindFirstChild("zoomies")
        return (bv and bv:IsA("BodyVelocity")) and bv.VectorVelocity or Vector3.zero
    end)
    if ok2 and v2.Magnitude > best.Magnitude then best = v2 end
    pcall(function()
        for _, d in ipairs(ball:GetChildren()) do
            if d:IsA("BodyVelocity") and d.VectorVelocity.Magnitude > best.Magnitude then
                best = d.VectorVelocity
            end
            if d:IsA("LinearVelocity") and d.VectorVelocity.Magnitude > best.Magnitude then
                best = d.VectorVelocity
            end
        end
    end)
    return best
end
-- ══════════════════════════════════════════════════════════════════
-- PING SAMPLER
-- ══════════════════════════════════════════════════════════════════
local PingMs = 60
local PingBonus = 0
local PingScale = 1.0
local PingSamples = {}
task.spawn(function()
    while true do
        local ok, raw = pcall(function()
            return Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
        end)
        if ok and type(raw)=="number" and raw > 0 then
            table.insert(PingSamples, raw)
            if #PingSamples > 6 then table.remove(PingSamples, 1) end
            local sum = 0
            for _, v in ipairs(PingSamples) do sum = sum + v end
            PingMs = sum / #PingSamples
            PingBonus = (PingMs/1000) * PingScale
        end
        task.wait(2)
    end
end)
local PingLabel = nil
task.spawn(function()
    while true do
        task.wait(1)
        if PingLabel then
            pcall(function()
                local col = PingMs<60 and Color3.fromRGB(34,197,110)
                         or PingMs<120 and Color3.fromRGB(255,200,50)
                         or Color3.fromRGB(255,80,80)
                PingLabel.Text = ("Ping: %dms | +%.3fs"):format(math.floor(PingMs), PingBonus)
                PingLabel.TextColor3 = col
            end)
        end
    end
end)
-- ══════════════════════════════════════════════════════════════════
-- FREE PARRY ENGINE ◈ FIXED
-- ══════════════════════════════════════════════════════════════════
-- FIXES:
-- 1. Emergency parry now ONLY fires when ball is targeting YOU
--    (was firing for all nearby enemies → caused constant pre-clicks)
-- 2. Ping formula fixed: PingMs/1000 (full ping, was /2000 = half)
-- 3. Hitbox raised to 6.5 studs (was 5.0 → fired too late)
-- 4. AntiCurve threshold lowered: sc > 2 (was sc > 5 → missed curves)
-- 5. History expanded to 5 frames (was 3 → smoother curve reading)
-- 6. Cooldown reset fixed: always 0.18s (was using ParryCooldown
--    which user can set high, causing it to get stuck)
-- 7. Direction reversal threshold tightened to -0.3 (was -0.4)
-- ══════════════════════════════════════════════════════════════════
local freePosHist = {}
local freeSpeedHist = {}
local MAX_FREE_HIST = 5  -- FIX 5: expanded from 3
local function pushFreeHist(dist, t)
    table.insert(freePosHist, {d=dist, t=t})
    if #freePosHist > MAX_FREE_HIST then table.remove(freePosHist, 1) end
end
local function smoothFreeClosing(curDist, curTime)
    if #freePosHist < 2 then return 0 end
    local oldest = freePosHist[1]
    local dt = curTime - oldest.t
    if dt <= 0 then return 0 end
    return (oldest.d - curDist) / dt
end
local PrevBallPos = nil
local PrevBallTime = 0
S.ParryReset = 0.18
local function FireParry()
    if S.DidParry then return end
    if os.clock() - S.LastParry < S.ParryCooldown then return end
    VIM:SendMouseButtonEvent(0,0,0,true, game, 0)
    S.LastParry = os.clock()
    S.DidParry = true
    S.ParryCount = S.ParryCount + 1
    local rc
    rc = RunService.Heartbeat:Connect(function()
        VIM:SendMouseButtonEvent(0,0,0,false, game, 0)
        rc:Disconnect()
    end)
    task.delay(S.ParryReset, function() S.DidParry = false end)  -- FIX 6
end
local function StartEngine()
    if Conn.Parry then Conn.Parry:Disconnect(); Conn.Parry=nil end
    if Conn.Target then Conn.Target:Disconnect(); Conn.Target=nil end
    if Conn.BallAdded then Conn.BallAdded:Disconnect(); Conn.BallAdded=nil end
    if Conn.DirWatch then Conn.DirWatch:Disconnect(); Conn.DirWatch=nil end
    S.DidParry = false
    PrevBallPos = nil
    freePosHist = {}
    freeSpeedHist = {}
    local function HookBall(ball)
        if not ball then return end
        if Conn.Target then Conn.Target:Disconnect(); Conn.Target=nil end
        Conn.Target = ball:GetAttributeChangedSignal("target"):Connect(function()
            S.DidParry = false  -- instantly re-arm when target changes
        end)
    end
    HookBall(GetBall())
    local function hookFolder(folder)
        if not folder then return end
        Conn.BallAdded = folder.ChildAdded:Connect(function()
            task.wait(0.05)
            local b = GetBall()
            if b then S.DidParry=false; PrevBallPos=nil; HookBall(b) end
        end)
    end
    local bf = workspace:FindFirstChild("Balls")
    if bf then hookFolder(bf)
    else task.spawn(function()
        local f = workspace:WaitForChild("Balls", 30)
        if f and not Conn.BallAdded then hookFolder(f) end
    end) end
    local lastDir = Vector3.zero
    Conn.DirWatch = RunService.Heartbeat:Connect(function()
        if not S.AutoParry then return end
        local b = GetBall(); if not b then return end
        local v = GetVel(b)
        if v.Magnitude > 2 and lastDir.Magnitude > 0 then
            if v.Unit:Dot(lastDir.Unit) < -0.3 then  -- FIX 7: tighter reversal detection
                S.DidParry = false
            end
        end
        if v.Magnitude > 0.5 then lastDir = v end
    end)
    Conn.Parry = RunService.PreSimulation:Connect(function()
        if not S.AutoParry then return end
        local Ball = GetBall()
        if not Ball then PrevBallPos=nil; freePosHist={}; freeSpeedHist={}; return end
        local HRP = GetHRP()
        if not HRP then return end
        local BPos = Ball.Position
        local HPos = HRP.Position
        local Dist = (HPos - BPos).Magnitude
        local now = os.clock()

        local isTgt = Ball:GetAttribute("target") == Plr.Name

        if S.AutoFace and isTgt then
            HRP.CFrame = CFrame.lookAt(HPos, Vector3.new(BPos.X, HPos.Y, BPos.Z))
        end

        -- FIX 1: Emergency only fires when YOU are the target
        -- (old code fired for any nearby enemy even if ball wasn't targeting you)
        if S.EmergencyParry and isTgt and not S.DidParry then
            if Dist <= S.EmergencyDist then
                FireParry()
                return
            end
        end

        if not isTgt then PrevBallPos=nil; freePosHist={}; freeSpeedHist={}; return end
        if S.DidParry then return end

        local Vel = GetVel(Ball)
        local Speed = Vel.Magnitude

        -- ANTI PRE-CLICK: only fire if ball is getting closer (dist shrinking)
        -- Uses last stored position to confirm ball is closing, not retreating.
        if PrevBallPos ~= nil and Speed > 1 then
            local prevDist = (HPos - PrevBallPos).Magnitude
            if Dist >= prevDist - 0.01 then
                -- ball not closing — update pos but don't fire yet
                PrevBallPos = BPos; PrevBallTime = now
                pushFreeHist(Dist, now)
                return
            end
        end

        -- Slow/stationary ball
        if Speed < 1 then
            if Dist <= 13.0 then FireParry() end
            return
        end

        pushFreeHist(Dist, now)
        table.insert(freeSpeedHist, Speed)
        if #freeSpeedHist > 5 then table.remove(freeSpeedHist, 1) end

        -- Speed boost detection
        local effectiveSpeed = Speed
        if S.SpeedBoostDetect and #freeSpeedHist >= 2 then
            local oldest = freeSpeedHist[1]
            if oldest > 0 and Speed / oldest > 1.25 then
                effectiveSpeed = Speed * 1.15
            end
        end

        -- FIX 2+3: Full ping compensation, bigger hitbox
        local hbox = 13.0
        -- Adaptive ping multiplier: aggressive early fire
        local pingFactor = PingMs <= 100 and 1.4 or (1.4 + ((PingMs - 100) / 100) * 0.7)
        pingFactor = math.min(pingFactor, 5.0)
        local pingComp = (PingMs / 1000) * effectiveSpeed * pingFactor
        local fireAt = hbox + pingComp

        if Dist <= fireAt then
            FireParry()
            PrevBallPos = BPos
            PrevBallTime = now
            return
        end

        -- Next-frame extrapolation (2 frames ahead)
        if Speed > 1 then
            local nextPos = BPos + Vel * (4/60)
            local nextDist = (HPos - nextPos).Magnitude
            if nextDist <= fireAt then
                FireParry()
                return
            end
        end

        -- FIX 4: AntiCurve threshold lowered to 2 (was 5 → missed most curves)
        if S.AntiCurve then
            local sc = smoothFreeClosing(Dist, now)
            if sc > 2 then  -- FIX 4
                local curveFireAt = hbox + (PingMs / 1000) * sc
                if Dist <= curveFireAt then
                    FireParry()
                end
            end
        end

        PrevBallPos = BPos
        PrevBallTime = now
        if S.Triggerbot and Dist <= 2 then FireParry() end
    end)
end
local function StopEngine()
    if Conn.Parry then Conn.Parry:Disconnect(); Conn.Parry=nil end
    if Conn.Target then Conn.Target:Disconnect(); Conn.Target=nil end
    if Conn.BallAdded then Conn.BallAdded:Disconnect(); Conn.BallAdded=nil end
    if Conn.DirWatch then Conn.DirWatch:Disconnect(); Conn.DirWatch=nil end
    S.DidParry=false; PrevBallPos=nil; freePosHist={}; freeSpeedHist={}
end
-- ══════════════════════════════════════════════════════════════════
-- PREMIUM ENGINE ◈ FULLY UPGRADED
-- ══════════════════════════════════════════════════════════════════
-- Includes all free fixes PLUS premium-exclusive upgrades:
-- FREE FIXES APPLIED:
--   • Emergency only fires when ball targets YOU (no random pre-clicks)
--   • Full ping compensation: PingMs/1000 (was /2000)
--   • Hitbox 7.5 studs (free=6.5, old premium=4.0 which was too tight)
--   • AntiCurve threshold sc>1.5 (free=2, old=5 — premium catches gentler curves)
--   • DidParry reset fixed to 0.12s (not tied to slider)
--   • History expanded to 8 frames (free=5)
--   • Direction reversal threshold -0.25 (free=-0.3, tighter for premium)
-- PREMIUM EXCLUSIVE UPGRADES:
--   • 3-frame lookahead extrapolation (free=2 frames)
--   • Weighted closing speed: recent frames count more than old ones
--   • Dual prediction: both linear + quadratic, fires on whichever triggers first
--   • Adaptive ping: uses min of last 3 ping samples for tightest compensation
--   • God Emergency: only fires when YOU are target (same fix as free + tighter dist)
--   • Speed burst detection: 3-frame window, 1.18x adjust (free=1.15x)
-- ══════════════════════════════════════════════════════════════════
local PremiumConn = {}
local P = {
    AutoParry = false,
    GodEmergency = false,
    CurveCrusher = false,
    AutoFace = false,
    HitboxTarget = 12.0,    -- wider than free's 6.5 — catches everything
    SpeedBoostDetect = true,
    QuadCurvePredict = true,
}

-- 8-frame position history for quadratic prediction
local speedHist = {}
local posHistQuad = {}
local function pushQuadHist(pos, t)
    table.insert(posHistQuad, {p=pos, t=t})
    if #posHistQuad > 8 then table.remove(posHistQuad, 1) end
end

-- Quadratic position prediction using acceleration estimate
local function predictQuadPos(curPos, curVel, dt)
    if #posHistQuad < 3 then return curPos + curVel * dt end
    local p1, p2, p3 = posHistQuad[1].p, posHistQuad[2].p, posHistQuad[3].p
    local v1 = p2 - p1
    local v2 = p3 - p2
    local accel = v2 - v1
    return curPos + curVel * dt + 0.5 * accel * dt^2
end

-- 8-frame closing speed history with weighted average (recent = more weight)
local posHist = {}
local MAX_HIST = 8
local function pushHist(dist, t)
    table.insert(posHist, {d=dist, t=t})
    if #posHist > MAX_HIST then table.remove(posHist, 1) end
end
local function smoothClosing(curDist, curTime)
    if #posHist < 2 then return 0 end
    -- Weighted: compare current vs oldest for base, then weight by recency
    local oldest = posHist[1]
    local dt = curTime - oldest.t
    if dt <= 0 then return 0 end
    local base = (oldest.d - curDist) / dt
    -- Also check last 2 frames for recent acceleration
    if #posHist >= 3 then
        local recent = posHist[#posHist - 1]
        local rdt = curTime - recent.t
        if rdt > 0 then
            local recentClosing = (recent.d - curDist) / rdt
            -- Weight recent closing 60%, overall 40%
            return recentClosing * 0.6 + base * 0.4
        end
    end
    return base
end

-- Adaptive ping: use min of last 3 samples for best-case compensation
local PingAdapt = {min=60, samples={}}
task.spawn(function()
    while true do
        task.wait(1.5)
        table.insert(PingAdapt.samples, PingMs)
        if #PingAdapt.samples > 3 then table.remove(PingAdapt.samples, 1) end
        local min = PingMs
        for _, v in ipairs(PingAdapt.samples) do if v < min then min = v end end
        PingAdapt.min = min
    end
end)

S.PremReset = 0.12

local function PremiumFire()
    if S.DidParry then return end
    if os.clock() - S.LastParry < 0.12 then return end
    VIM:SendMouseButtonEvent(0,0,0,true, game, 0)
    S.LastParry = os.clock()
    S.DidParry = true
    S.ParryCount = S.ParryCount + 1
    local rc
    rc = RunService.Heartbeat:Connect(function()
        VIM:SendMouseButtonEvent(0,0,0,false, game, 0)
        rc:Disconnect()
    end)
    task.delay(S.PremReset, function() S.DidParry = false end)
end

local function StartPremiumEngine()
    for _, c in pairs(PremiumConn) do if c then c:Disconnect() end end
    PremiumConn = {}
    posHist={}; speedHist={}; posHistQuad={}
    local lastVelDir = Vector3.zero

    -- Direction reversal: re-arm instantly when ball redirects
    PremiumConn.DirWatch = RunService.Heartbeat:Connect(function()
        local b = GetBall(); if not b then return end
        local v = GetVel(b)
        if v.Magnitude > 2 and lastVelDir.Magnitude > 0 then
            if v.Unit:Dot(lastVelDir.Unit) < -0.20 then
                S.DidParry = false
            end
        end
        if v.Magnitude > 0.5 then lastVelDir = v end
    end)

    PremiumConn.Parry = RunService.PreSimulation:Connect(function()
        local Ball = GetBall()
        if not Ball then posHist={}; speedHist={}; posHistQuad={}; return end
        local HRP = GetHRP(); if not HRP then return end
        local BPos = Ball.Position
        local HPos = HRP.Position
        local Dist = (HPos - BPos).Magnitude
        local Vel  = GetVel(Ball)
        local Speed = Vel.Magnitude
        local now  = os.clock()
        local isTgt = Ball:GetAttribute("target") == Plr.Name

        -- Auto-face
        if P.AutoFace and isTgt then
            pcall(function()
                HRP.CFrame = CFrame.lookAt(HPos, Vector3.new(BPos.X,HPos.Y,BPos.Z))
            end)
        end

        -- Emergency (only when targeted)
        if not S.DidParry and isTgt then
            local eDist = P.GodEmergency and 3.5 or S.EmergencyDist
            if Dist <= eDist then PremiumFire(); posHist={}; return end
        end

        if not isTgt then posHist={}; speedHist={}; posHistQuad={}; return end
        if S.DidParry then return end

        -- History tracking
        pushHist(Dist, now)
        pushQuadHist(BPos, now)
        table.insert(speedHist, Speed)
        if #speedHist > 8 then table.remove(speedHist, 1) end

        -- Stationary/slow ball: tight window
        if Speed < 1 then
            if Dist <= 11.5 then PremiumFire() end
            return
        end

        -- ANTI PRE-CLICK: must be closing (compare last 2 dist entries)
        if #posHist >= 2 then
            local prevDist = posHist[#posHist - 1].d
            if Dist >= prevDist - 0.005 then return end
        end

        -- ── Effective speed with ultra-high velocity detection ──
        local effectiveSpeed = Speed
        if #speedHist >= 3 then
            local oldest = speedHist[1]
            -- Standard boost (ability-triggered)
            if P.SpeedBoostDetect and oldest > 0 and Speed / oldest > 1.15 then
                effectiveSpeed = Speed * 1.20
            end
            -- ULTRA HIGH VELOCITY: ball moving insanely fast (>180 studs/s)
            -- Pre-compensate heavily so we don't miss it entirely
            if Speed > 180 then
                effectiveSpeed = Speed * 1.35
            elseif Speed > 120 then
                effectiveSpeed = Speed * 1.25
            elseif Speed > 80 then
                effectiveSpeed = Speed * 1.15
            end
        end

        -- ── Ping compensation ──
        local ping = PingAdapt.min
        local pingFactor = ping <= 60  and 0.5
            or ping <= 100 and (0.5 + (ping-60)/40 * 0.2)   -- 0.5→0.7 for 60-100ms
            or (0.7 + ((ping-100)/100) * 0.3)                -- 0.7→1.0 for 100-200ms
        pingFactor = math.min(pingFactor, 1.8)
        local pingComp = (ping / 1000) * effectiveSpeed * pingFactor

        -- ── Hitbox: slight speed scale, capped so fireAt never exceeds ~13.5 ──
        local baseHbox = P.HitboxTarget or 10.5
        local speedScalar = math.min(Speed / 60, 1.8)
        local hbox = baseHbox * (1 + speedScalar * 0.06)  -- max ~11.6 at extreme speed
        local fireAt = math.min(hbox + pingComp, 13.4)    -- hard cap at 13.4

        -- Core distance check
        if Dist <= fireAt then PremiumFire(); return end

        -- ── Lookahead: 4-frame quadratic prediction ──
        local nextPos  = predictQuadPos(BPos, Vel, 4/60)
        local nextDist = (HPos - nextPos).Magnitude
        if nextDist <= fireAt then PremiumFire(); return end

        -- ── Curve Crusher: weighted closing speed ──
        if P.CurveCrusher or S.AntiCurve then
            local sc = smoothClosing(Dist, now)
            if sc > 1.2 then
                local curveAt = hbox + (ping / 1000) * sc * 1.1
                if Dist <= curveAt then PremiumFire(); return end
            end
        end

        -- ── Quad curve prediction (premium flag) ──
        if P.QuadCurvePredict then
            local predPos  = predictQuadPos(BPos, Vel, 1/15)
            local predDist = (HPos - predPos).Magnitude
            if predDist <= fireAt then PremiumFire(); return end
        end
    end)
end

local function StopPremiumEngine()
    for _, c in pairs(PremiumConn) do if c then c:Disconnect() end end
    PremiumConn = {}
    S.DidParry = false
    speedHist = {}
    posHist = {}
    posHistQuad = {}
end
-- ══════════════════════════════════════════════════════════════════

-- ══════════════════════════════════════════════════════════════════
-- ABILITY ESP
-- ══════════════════════════════════════════════════════════════════
local AbilityBBs = {}
local AbilityPoll = nil
local BB_ABILITIES = {
    ["Infinity"] = {tier="S", col=Color3.fromRGB(255,60,220), desc="Auto-parries ball at any speed", warn=true},
    ["Time Hole"] = {tier="S", col=Color3.fromRGB(255,60,220), desc="Creates time distortion zone", warn=true},
    ["Slash of Duality"] = {tier="S", col=Color3.fromRGB(255,60,220), desc="Dual slash — two trajectories at once", warn=true},
    ["Dribble"] = {tier="S", col=Color3.fromRGB(255,60,220), desc="Redirect ball unpredictable angle", warn=true},
    ["Dragon Spirit"] = {tier="S", col=Color3.fromRGB(255,60,220), desc="Dragon charge — fast spiral redirect", warn=true},
    ["Pull"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Forces ball to target them instantly", warn=true},
    ["Guardian Angel"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Revives once per round on death", warn=false},
    ["Reaper"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Execute low-HP players near ball", warn=false},
    ["Slashes of Fury"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Multi-slash barrage — fast ball return", warn=true},
    ["Forcefield"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Shield bubble — auto-deflects ball", warn=false},
    ["Bunny Leap"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Huge leap escape — hard to hit", warn=false},
    ["Calming Deflection"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Deflect — slows ball on parry", warn=false},
    ["Singularity"] = {tier="A", col=Color3.fromRGB(255,130,30), desc="Black hole pulls ball toward it", warn=true},
    ["Invisibility"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Goes invisible — ball untargetable", warn=false},
    ["Titan Blade"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Titan armor — tanks ball hit", warn=false},
    ["Scopophobia"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Fear aura — disrupts nearby players", warn=true},
    ["Blade Trap"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Trap on ground — hits ball near it", warn=true},
    ["Golden Ball"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Ball turns gold — bonus coins on hit", warn=false},
    ["Continuity Zero"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Resets all ability cooldowns", warn=false},
    ["Death Slash"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Hard spike — ball returns very fast", warn=true},
    ["Phantom"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Teleports behind nearest player", warn=true},
    ["Swap"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Swaps position with targeted player", warn=true},
    ["Doppelganger"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Spawns decoy clone of self", warn=false},
    ["Necromancer"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Raises clone to confuse ball target", warn=false},
    ["Serpent Shadow Clone"]= {tier="B", col=Color3.fromRGB(80,220,120), desc="Shadow serpent clone diversion", warn=false},
    ["Phase Bypass"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Phase out — godspeed mobility escape", warn=false},
    ["Hell Hook"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Hooks ball into sharp curve", warn=true},
    ["Blink"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Short-range instant blink dodge", warn=false},
    ["Qi-Charge"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Parry with no cooldown briefly", warn=false},
    ["Telekinesis"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Stops ball mid-air + retargets it", warn=true},
    ["Aerodynamic Slash"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Wind slash — speed + curve deflect", warn=false},
    ["Rapture"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Rare wheel ability — massive curve slash",warn=true},
    ["Thunder Dash"] = {tier="B", col=Color3.fromRGB(80,220,120), desc="Lightning-speed repositioning dash", warn=false},
    ["Flash Counter"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Instant counter — deflects on reaction", warn=false},
    ["Absolute Confidence"]= {tier="C", col=Color3.fromRGB(255,220,40), desc="Confident parry — stronger deflect", warn=false},
    ["Pulse"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Pulse wave pushes ball away", warn=false},
    ["Bounty"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Marks player — bonus reward on kill", warn=false},
    ["Luck"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Passive: increases coin drop chance", warn=false},
    ["Decrypted Clone"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Encrypted decoy — confuses targeting", warn=false},
    ["Raging Deflection"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Stronger deflect — big speed boost", warn=true},
    ["Tact"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Passive: tactical awareness buff", warn=false},
    ["Waypoint"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Place marker + teleport back to it", warn=false},
    ["Misfortune"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Passive: curses nearby players", warn=true},
    ["Freeze"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Freezes ball briefly mid-flight", warn=true},
    ["Shadow Step"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Shadow blink dodge behind attacker", warn=false},
    ["Quantum Arena"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Zone trap — locks players inside area", warn=true},
    ["Force"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Push force — knocks ball & players", warn=false},
    ["Dash"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Starter dash — quick reposition", warn=false},
    ["Wind Cloak"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Wind aura — speed + jump boost", warn=false},
    ["Quasar"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Energy burst redirects ball", warn=false},
    ["Chieftain's Totem"] = {tier="C", col=Color3.fromRGB(255,220,40), desc="Totem — buffs nearby teammates", warn=false},
    ["Freeze Trap"] = {tier="D", col=Color3.fromRGB(160,160,160), desc="Ground trap — freezes ball on contact", warn=false},
    ["Martyrdom"] = {tier="D", col=Color3.fromRGB(160,160,160), desc="Explodes on death — AOE damage", warn=true},
    ["Platform"] = {tier="D", col=Color3.fromRGB(160,160,160), desc="Pillar under feet — lifts you up", warn=false},
    ["Super Jump"] = {tier="D", col=Color3.fromRGB(160,160,160), desc="Big jump — escapes ball from above", warn=false},
    ["Quad Jump"] = {tier="D", col=Color3.fromRGB(160,160,160), desc="Passive: jump 4 times in the air", warn=false},
}
local TIER_COL = {
    S = Color3.fromRGB(255,60,220),
    A = Color3.fromRGB(255,130,30),
    B = Color3.fromRGB(80,220,120),
    C = Color3.fromRGB(255,220,40),
    D = Color3.fromRGB(160,160,160),
}
local RS = game:GetService("ReplicatedStorage")
local function ScanAbility(plr)
    if not plr then return nil end
    local char = plr.Character
    local function matchAbility(s)
        if not s or #s < 2 then return nil end
        if BB_ABILITIES[s] then return s end
        local sl = s:lower()
        for k in pairs(BB_ABILITIES) do
            if k:lower() == sl then return k end
        end
        return nil
    end
    for _, n in ipairs({"Ability","ability","AbilityName","EquippedAbility","Skill","CurrentAbility","equipped"}) do
        local ok,v = pcall(function() return plr:GetAttribute(n) end)
        if ok and type(v)=="string" then local r=matchAbility(v); if r then return r end end
    end
    if char then
        for _, n in ipairs({"Ability","ability","AbilityName","EquippedAbility","Skill","CurrentAbility"}) do
            local ok,v = pcall(function() return char:GetAttribute(n) end)
            if ok and type(v)=="string" then local r=matchAbility(v); if r then return r end end
        end
    end
    for _, v in ipairs(plr:GetDescendants()) do
        if v:IsA("StringValue") then
            local r = matchAbility(v.Value)
            if r then return r end
            local nl = v.Name:lower()
            if nl:find("ability") or nl:find("skill") or nl:find("equip") then
                if #v.Value > 0 then return v.Value end
            end
        end
    end
    if char then
        for _, v in ipairs(char:GetDescendants()) do
            if v:IsA("StringValue") then
                local r = matchAbility(v.Value)
                if r then return r end
                local nl = v.Name:lower()
                if nl:find("ability") or nl:find("skill") or nl:find("equip") then
                    if #v.Value > 0 then return v.Value end
                end
            end
        end
    end
    local ok5,result5 = pcall(function()
        for _, folder in ipairs(RS:GetChildren()) do
            local pdata = folder:FindFirstChild(plr.Name)
            if pdata then
                for _, v in ipairs(pdata:GetDescendants()) do
                    if v:IsA("StringValue") then
                        local r = matchAbility(v.Value)
                        if r then return r end
                    end
                end
            end
        end
        return nil
    end)
    if ok5 and result5 then return result5 end
    if char then
        for _, v in ipairs(char:GetChildren()) do
            if v:IsA("Tool") then
                local r = matchAbility(v.Name)
                return r or v.Name
            end
        end
    end
    local bp = plr:FindFirstChild("Backpack")
    if bp then
        for _, v in ipairs(bp:GetChildren()) do
            if v:IsA("Tool") then
                local r = matchAbility(v.Name)
                return r or v.Name
            end
        end
    end
    return nil
end
local AbilityLastSeen = {}  -- dirty-flag cache: [plr] = last ability string
local function BuildAbilityBB(plr)
    if not (plr and plr.Character) then return end
    local head = plr.Character:FindFirstChild("Head")
    if not head then return end
    if AbilityBBs[plr] and AbilityBBs[plr].Parent then
        pcall(function() AbilityBBs[plr]:Destroy() end)
    end
    AbilityLastSeen[plr] = nil  -- reset cache on rebuild
    local bb = Instance.new("BillboardGui")
    bb.Name = "RyuuAbilESP"
    bb.AlwaysOnTop = true
    bb.Size = UDim2.new(0,150,0,36)
    bb.StudsOffset = Vector3.new(0,3.0,0)
    bb.MaxDistance = 100
    bb.Parent = head
    local bg = Instance.new("Frame",bb)
    bg.Size = UDim2.new(1,0,1,0)
    bg.BackgroundColor3 = Color3.fromRGB(8,5,18)
    bg.BackgroundTransparency = 0.15
    bg.BorderSizePixel = 0
    Instance.new("UICorner",bg).CornerRadius = UDim.new(0,8)
    local stroke = Instance.new("UIStroke",bg)
    stroke.Thickness = 1.4
    stroke.Color = Color3.fromRGB(50,25,90)
    local nLbl = Instance.new("TextLabel",bg)
    nLbl.Name = "PN"
    nLbl.Size = UDim2.new(1,-30,0,13)
    nLbl.Position = UDim2.new(0,4,0,3)
    nLbl.BackgroundTransparency = 1
    nLbl.Text = plr.Name
    nLbl.TextColor3 = Color3.fromRGB(150,140,190)
    nLbl.Font = Enum.Font.GothamBold
    nLbl.TextSize = 10
    nLbl.TextXAlignment = Enum.TextXAlignment.Left
    nLbl.TextTruncate = Enum.TextTruncate.AtEnd
    local badge = Instance.new("TextLabel",bg)
    badge.Name = "TB"
    badge.Size = UDim2.new(0,24,0,13)
    badge.Position = UDim2.new(1,-27,0,3)
    badge.BackgroundColor3 = Color3.fromRGB(30,15,55)
    badge.BorderSizePixel = 0
    badge.Text = "?"
    badge.TextColor3 = Color3.fromRGB(180,100,255)
    badge.Font = Enum.Font.GothamBlack
    badge.TextSize = 10
    Instance.new("UICorner",badge).CornerRadius = UDim.new(0,4)
    local aLbl = Instance.new("TextLabel",bg)
    aLbl.Name = "AN"
    aLbl.Size = UDim2.new(1,-8,0,18)
    aLbl.Position = UDim2.new(0,4,0,14)
    aLbl.BackgroundTransparency = 1
    aLbl.Text = "Scanning..."
    aLbl.TextColor3 = Color3.fromRGB(200,200,200)
    aLbl.Font = Enum.Font.GothamBold
    aLbl.TextSize = 13
    aLbl.TextXAlignment = Enum.TextXAlignment.Left
    aLbl.TextTruncate = Enum.TextTruncate.AtEnd
    AbilityBBs[plr] = bb
end
local function RefreshBB(plr)
    if not (plr and plr.Character) then return end
    local bb = AbilityBBs[plr]
    if not (bb and bb.Parent) then BuildAbilityBB(plr); bb=AbilityBBs[plr] end
    if not bb then return end
    local bg = bb:FindFirstChildOfClass("Frame"); if not bg then return end
    local aLbl = bg:FindFirstChild("AN")
    local badge = bg:FindFirstChild("TB")
    local stk = bg:FindFirstChildOfClass("UIStroke")
    if not aLbl then return end
    local name = ScanAbility(plr)
    -- DIRTY FLAG: skip redraw if ability hasn't changed (kills FPS drop)
    local cacheKey = name or "__none__"
    if AbilityLastSeen[plr] == cacheKey then return end
    AbilityLastSeen[plr] = cacheKey
    if name then
        local data = BB_ABILITIES[name]
        if data then
            local col = data.warn and Color3.fromRGB(255,70,70) or data.col
            aLbl.Text = (data.warn and "⚠ " or "")..name
            aLbl.TextColor3 = col
            if badge then badge.Text = data.tier
                          badge.TextColor3 = TIER_COL[data.tier] or Color3.fromRGB(180,100,255) end
            if stk then stk.Color = data.warn and Color3.fromRGB(200,50,50) or data.col end
        else
            aLbl.Text = name
            aLbl.TextColor3 = Color3.fromRGB(200,200,200)
            if badge then badge.Text = "?" end
            if stk then stk.Color = Color3.fromRGB(50,25,90) end
        end
    else
        aLbl.Text = "No Ability"
        aLbl.TextColor3 = Color3.fromRGB(90,85,110)
        if badge then badge.Text = "-" end
        if stk then stk.Color = Color3.fromRGB(30,15,50) end
    end
end
local function CleanAbilityESP()
    if AbilityPoll then AbilityPoll:Disconnect(); AbilityPoll=nil end
    for _, bb in pairs(AbilityBBs) do
        pcall(function() if bb and bb.Parent then bb:Destroy() end end)
    end
    AbilityBBs = {}
end
local function StartAbilityESP()
    CleanAbilityESP()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr then
            BuildAbilityBB(p)
            RefreshBB(p)
        end
    end
    task.spawn(function()
        while S.AbilityESP do
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= Plr then
                    if p.Character then
                        local head = p.Character:FindFirstChild("Head")
                        if head then
                            local bb = AbilityBBs[p]
                            if not bb or not bb.Parent or bb.Parent ~= head then
                                BuildAbilityBB(p)
                            end
                            RefreshBB(p)
                        end
                    end
                end
            end
            for p, bb in pairs(AbilityBBs) do
                if not p.Parent then
                    pcall(function() if bb and bb.Parent then bb:Destroy() end end)
                    AbilityBBs[p] = nil
                end
            end
            task.wait(0.8)  -- slowed from 0.4 -- dirty cache means no visual difference
        end
    end)
    Players.PlayerAdded:Connect(function(p)
        if not S.AbilityESP then return end
        p.CharacterAdded:Connect(function()
            task.wait(0.5)
            if S.AbilityESP then BuildAbilityBB(p); RefreshBB(p) end
        end)
    end)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr then
            p.CharacterAdded:Connect(function()
                task.wait(0.5)
                if S.AbilityESP then BuildAbilityBB(p); RefreshBB(p) end
            end)
        end
    end
end
-- ══════════════════════════════════════════════════════════════════
-- SWORD CHANGER
-- ══════════════════════════════════════════════════════════════════
local SWORD_COLORS = {
    ["Default"] = nil,
    ["Gold"] = Color3.fromRGB(255, 200, 40),
    ["Shadow Black"] = Color3.fromRGB( 15, 10, 20),
    ["Crystal Blue"] = Color3.fromRGB( 40, 140, 255),
    ["Crimson"] = Color3.fromRGB(200, 30, 30),
    ["Ghost White"] = Color3.fromRGB(230, 230, 255),
    ["Neon Purple"] = Color3.fromRGB(180, 40, 255),
    ["Emerald"] = Color3.fromRGB( 20, 200, 80),
    ["Rose Gold"] = Color3.fromRGB(220, 140, 120),
    ["Obsidian"] = Color3.fromRGB( 30, 28, 35),
    ["Cyan"] = Color3.fromRGB( 0, 220, 220),
    ["Orange"] = Color3.fromRGB(255, 120, 20),
    ["Rainbow"] = nil,
}
local SWORD_MATERIALS = {
    ["Default"] = Enum.Material.SmoothPlastic,
    ["Neon Purple"] = Enum.Material.Neon,
    ["Emerald"] = Enum.Material.Neon,
    ["Cyan"] = Enum.Material.Neon,
    ["Rainbow"] = Enum.Material.Neon,
}
local CurrentSwordPreset = "Default"
local SwordOrigColors = {}
local RainbowHue = 0
local function GetSwordParts()
    local char = Plr.Character; if not char then return nil, {} end
    local tool = nil
    for _, v in ipairs(char:GetChildren()) do
        if v:IsA("Tool") then tool = v; break end
    end
    if not tool then return nil, {} end
    local parts = {}
    for _, v in ipairs(tool:GetDescendants()) do
        if v:IsA("BasePart") or v:IsA("UnionOperation") then
            parts[#parts+1] = v
        end
    end
    local h = tool:FindFirstChild("Handle")
    if h then parts[#parts+1] = h end
    return tool, parts
end
local function SaveSwordOriginals(parts)
    for _, p in ipairs(parts) do
        if not SwordOrigColors[p] then
            SwordOrigColors[p] = {color=p.Color, mat=p.Material}
        end
    end
end
local function ApplySwordPreset(presetName)
    CurrentSwordPreset = presetName
    local _, parts = GetSwordParts()
    if #parts == 0 then return end
    SaveSwordOriginals(parts)
    local mat = SWORD_MATERIALS[presetName] or Enum.Material.SmoothPlastic
    if presetName == "Default" then
        for _, p in ipairs(parts) do
            local orig = SwordOrigColors[p]
            if orig then
                pcall(function() p.Color = orig.color; p.Material = orig.mat end)
            end
        end
        return
    end
    if presetName == "Rainbow" then
        RainbowHue = (RainbowHue + 0.08) % 1
        local col = Color3.fromHSV(RainbowHue, 1, 1)
        for _, p in ipairs(parts) do
            pcall(function() p.Color = col; p.Material = Enum.Material.Neon end)
        end
        return
    end
    local col = SWORD_COLORS[presetName]
    if not col then return end
    for _, p in ipairs(parts) do
        pcall(function() p.Color = col; p.Material = mat end)
    end
end
Plr.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    char.ChildAdded:Connect(function(v)
        if v:IsA("Tool") then
            task.wait(0.1)
            ApplySwordPreset(CurrentSwordPreset)
        end
    end)
end)
if Plr.Character then
    Plr.Character.ChildAdded:Connect(function(v)
        if v:IsA("Tool") then
            task.wait(0.1)
            ApplySwordPreset(CurrentSwordPreset)
        end
    end)
end
-- ══════════════════════════════════════════════════════════════════
-- RAYFIELD WINDOW
-- ══════════════════════════════════════════════════════════════════
local W = Rayfield:CreateWindow({
    Name = "Ryuu's HUB V1 BETA",
    LoadingTitle = "Ryuu's HUB V1 BETA",
    LoadingSubtitle = "",
    Theme = "Default",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = {Enabled=true, FolderName="RyuuV5", FileName="Config"},
    KeySystem = false,
})
-- ── TAB 1: AUTO PARRY ────────────────────────────────────────────
local T1 = W:CreateTab("⚔ Auto Parry", nil)
T1:CreateSection("Auto Parry")
T1:CreateToggle({Name="Auto Parry",CurrentValue=false,Flag="AP",Callback=function(v)
    S.AutoParry=v; if v then StartEngine() else StopEngine() end
end})
T1:CreateToggle({Name="Auto Face Ball",CurrentValue=false,Flag="AF",Callback=function(v) S.AutoFace=v end})
T1:CreateToggle({Name="Triggerbot (≤2 studs instant)",CurrentValue=false,Flag="TB",Callback=function(v) S.Triggerbot=v end})
T1:CreateLabel("Stud-distance gate · AntiCurve predictor · Emergency distance")
T1:CreateLabel("Speed-independent timing — same perfect parry at any ball speed")
T1:CreateDivider()
T1:CreateSection("🎯 Parry Accuracy")
T1:CreateLabel("Higher = more precise timing. 100 = perfect every single time.")
T1:CreateLabel("Lower = human-like variance — fires slightly off-rhythm.")
T1:CreateLabel("acc=100: 0ms jitter — fires at mathematically perfect moment every time")
T1:CreateLabel("acc=50: ~90ms jitter — slightly off-rhythm, human-like")
T1:CreateLabel("acc=1: ~180ms jitter — very imprecise, misses fast balls")
T1:CreateSlider({
    Name="Parry Accuracy", Range={1,100}, Increment=1, CurrentValue=100, Flag="PA",
    Callback=function(v)
        S.ParryAccuracy=v
        local tier = v>=95 and "PERFECT — Machine precision"
                  or v>=80 and "Elite — Near-perfect"
                  or v>=60 and "Balanced"
                  or v>=40 and "Normal — Human-ish"
                  or "Low — Often misses"
        Rayfield:Notify({Title="Accuracy: "..v,Content=tier,Duration=2,Image=4483362458})
    end,
})
T1:CreateDivider()
T1:CreateSection("🌀 Anti-Curve Engine")
T1:CreateLabel("Tracks ball turn rate + speed changes every frame.")
T1:CreateLabel("Curved balls, speed boosts, slows — all predicted correctly.")
T1:CreateLabel("D3: Rodrigues rotation sim | D5: Closing-velocity check")
T1:CreateToggle({Name="Anti-Curve",CurrentValue=true,Flag="AC",Callback=function(v)
    S.AntiCurve=v
    Rayfield:Notify({
        Title = "Anti-Curve " .. (v and "ON" or "OFF"),
        Content = v and "Curve tracking active — nothing gets past" or "Disabled",
        Duration = 2, Image = 4483362458,
    })
end})
T1:CreateDivider()
T1:CreateSection("📡 Ping Compensation")
PingLabel = T1:CreateLabel("Ping: measuring...")
T1:CreateSlider({Name="Ping Scale",Range={0,1},Increment=0.05,Suffix="x",
    CurrentValue=0.5,Flag="PCS",Callback=function(v) PingScale=v; PingBonus=(PingMs/1000)*PingScale end})
T1:CreateDivider()
T1:CreateSection("⏱ Parry Cooldown")
T1:CreateLabel("No pre-click guard. Parry fires the moment the ball is in range.")
T1:CreateSlider({Name="Parry Cooldown",Range={0.05,0.60},Increment=0.01,Suffix="s",
    CurrentValue=0.32,Flag="CD",Callback=function(v) S.ParryCooldown=v end})
T1:CreateDivider()
T1:CreateSection("🚨 Emergency Distance Parry")
T1:CreateLabel("Auto-parries instantly if the ball is dangerously close.")
T1:CreateLabel("Triggers when ball ≤ distance AND an enemy is nearby.")
T1:CreateLabel("Protects from: point-blank rush, Pull close, Hell Hook, Dribble.")
T1:CreateToggle({Name="Emergency Parry",CurrentValue=true,Flag="EP",Callback=function(v)
    S.EmergencyParry=v
    Rayfield:Notify({
        Title="Emergency Parry " .. (v and "ON" or "OFF"),
        Content=v and "Point-blank protection active" or "Disabled",
        Duration=2, Image=4483362458,
    })
end})
T1:CreateSlider({Name="Emergency Distance",Range={2,12},Increment=1,Suffix=" studs",
    CurrentValue=6,Flag="EPD",Callback=function(v) S.EmergencyDist=v end})

T1:CreateDivider()
T1:CreateSection("⚡ Manual Spam")
T1:CreateLabel("Opens a draggable widget — press [E] to toggle spam on/off.")
T1:CreateLabel("Widget turns 🟢 green when actively spamming.")
T1:CreateButton({Name="⚡ Open Manual Spam Widget", Callback=function()
    if _G.RyuuOpenManualSpam then _G.RyuuOpenManualSpam() end
end})
T1:CreateDivider()
T1:CreateKeybind({Name="Toggle Auto Parry [F5]",CurrentKeybind="F5",HoldToInteract=false,Flag="KB_AP",
    Callback=function()
        S.AutoParry=not S.AutoParry
        if S.AutoParry then StartEngine() else StopEngine() end
        Rayfield:Notify({Title="Auto Parry",Content=S.AutoParry and "ON ✓" or "OFF",Duration=2,Image=4483362458})
    end})

do -- scope: manual spam widget
local MSOn = false
local MSThr = nil
local MSSG = nil
local MSCard = nil
local MSShadow = nil

local function DoStartMS()
    if MSThr then return end
    -- Fire click down+up on EVERY single Heartbeat frame — absolute maximum speed
    MSThr = RunService.Heartbeat:Connect(function()
        if not MSOn then
            MSThr:Disconnect(); MSThr = nil; return
        end
        VIM:SendMouseButtonEvent(0,0,0,true,game,0)
        VIM:SendMouseButtonEvent(0,0,0,false,game,0)
    end)
end

local function DoStopMS()
    MSOn = false
    if MSThr then pcall(function() MSThr:Disconnect() end); MSThr = nil end
end

-- Colors
local C_OFF_BG   = Color3.fromRGB(10, 6, 22)
local C_OFF_BRD  = Color3.fromRGB(70, 35, 130)
local C_OFF_TXT  = Color3.fromRGB(160, 130, 220)
local C_OFF_SUB  = Color3.fromRGB(90, 70, 140)
local C_ON_BG    = Color3.fromRGB(8, 32, 14)
local C_ON_BRD   = Color3.fromRGB(30, 200, 80)
local C_ON_TXT   = Color3.fromRGB(60, 255, 120)
local C_ON_SUB   = Color3.fromRGB(30, 160, 70)

local TIF = TweenInfo.new(0.15, Enum.EasingStyle.Quad)

-- Build the floating GUI
MSSG = Instance.new("ScreenGui")
MSSG.Name = "RyuuManualSpam"
MSSG.ResetOnSpawn = false
MSSG.IgnoreGuiInset = true
MSSG.DisplayOrder = 9990
MSSG.Enabled = false
pcall(function() MSSG.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui end)
if not MSSG.Parent then pcall(function() MSSG.Parent = Plr:WaitForChild("PlayerGui") end) end

-- Shadow
MSShadow = Instance.new("Frame", MSSG)
MSShadow.Name = "Shadow"
MSShadow.Size = UDim2.new(0, 170, 0, 54)
MSShadow.AnchorPoint = Vector2.new(0.5, 0.5)
MSShadow.Position = UDim2.new(0.5, 4, 0.5, 4)
MSShadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MSShadow.BackgroundTransparency = 0.55
MSShadow.BorderSizePixel = 0
Instance.new("UICorner", MSShadow).CornerRadius = UDim.new(0, 14)

-- Main card (TextButton so tapping works on mobile too)
MSCard = Instance.new("TextButton", MSSG)
MSCard.Name = "Card"
MSCard.Size = UDim2.new(0, 170, 0, 54)
MSCard.AnchorPoint = Vector2.new(0.5, 0.5)
MSCard.Position = UDim2.new(0.5, 0, 0.5, 0)
MSCard.BackgroundColor3 = C_OFF_BG
MSCard.BorderSizePixel = 0
MSCard.Text = ""
MSCard.AutoButtonColor = false
MSCard.Active = true
-- manual drag implemented below (Draggable causes input delay)
Instance.new("UICorner", MSCard).CornerRadius = UDim.new(0, 14)
local MSStroke = Instance.new("UIStroke", MSCard)
MSStroke.Thickness = 1.5
MSStroke.Color = C_OFF_BRD

-- Status dot
local MSDot = Instance.new("Frame", MSCard)
MSDot.Size = UDim2.new(0, 8, 0, 8)
MSDot.Position = UDim2.new(0, 14, 0.5, -14)
MSDot.BackgroundColor3 = C_OFF_BRD
MSDot.BorderSizePixel = 0
Instance.new("UICorner", MSDot).CornerRadius = UDim.new(1, 0)

-- Title
local MSTit = Instance.new("TextLabel", MSCard)
MSTit.Size = UDim2.new(1, -60, 0, 16)
MSTit.Position = UDim2.new(0, 30, 0, 8)
MSTit.BackgroundTransparency = 1
MSTit.Text = "MANUAL SPAM"
MSTit.Font = Enum.Font.GothamBlack
MSTit.TextSize = 11
MSTit.TextColor3 = C_OFF_TXT
MSTit.TextXAlignment = Enum.TextXAlignment.Left

-- Status label
local MSSub = Instance.new("TextLabel", MSCard)
MSSub.Size = UDim2.new(1, -60, 0, 12)
MSSub.Position = UDim2.new(0, 30, 0, 26)
MSSub.BackgroundTransparency = 1
MSSub.Text = "OFF — press [E] to toggle"
MSSub.Font = Enum.Font.Gotham
MSSub.TextSize = 9
MSSub.TextColor3 = C_OFF_SUB
MSSub.TextXAlignment = Enum.TextXAlignment.Left

-- Keybind badge
local MSBadge = Instance.new("TextLabel", MSCard)
MSBadge.Size = UDim2.new(0, 22, 0, 16)
MSBadge.Position = UDim2.new(1, -30, 0.5, -8)
MSBadge.BackgroundColor3 = Color3.fromRGB(25, 14, 50)
MSBadge.BorderSizePixel = 0
MSBadge.Text = "E"
MSBadge.Font = Enum.Font.GothamBold
MSBadge.TextSize = 9
MSBadge.TextColor3 = C_OFF_BRD
Instance.new("UICorner", MSBadge).CornerRadius = UDim.new(0, 6)

-- Close button
local MSClose = Instance.new("TextButton", MSCard)
MSClose.Size = UDim2.new(0, 16, 0, 16)
MSClose.Position = UDim2.new(1, -20, 0, 4)
MSClose.BackgroundTransparency = 1
MSClose.Text = "✕"
MSClose.Font = Enum.Font.GothamBold
MSClose.TextSize = 10
MSClose.TextColor3 = Color3.fromRGB(90, 70, 130)

-- Manual drag (no Draggable = true — it causes 2-second input delay/acceleration)
do
    local dragging, dragStart, startPos = false, nil, nil
    MSCard.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1
        or inp.UserInputType == Enum.UserInputType.Touch then
            dragging  = true
            dragStart = inp.Position
            startPos  = MSCard.Position
        end
    end)
    MSCard.InputChanged:Connect(function(inp)
        if dragging and (inp.UserInputType == Enum.UserInputType.MouseMovement
            or inp.UserInputType == Enum.UserInputType.Touch) then
            local delta = inp.Position - dragStart
            local np = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
            MSCard.Position   = np
            MSShadow.Position = UDim2.new(np.X.Scale, np.X.Offset+4, np.Y.Scale, np.Y.Offset+4)
        end
    end)
    MSCard.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1
        or inp.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

local function SetMSVisual(on)
    TweenService:Create(MSCard,   TIF, {BackgroundColor3 = on and C_ON_BG  or C_OFF_BG}):Play()
    TweenService:Create(MSStroke, TIF, {Color            = on and C_ON_BRD or C_OFF_BRD}):Play()
    TweenService:Create(MSTit,    TIF, {TextColor3       = on and C_ON_TXT or C_OFF_TXT}):Play()
    TweenService:Create(MSSub,    TIF, {TextColor3       = on and C_ON_SUB or C_OFF_SUB}):Play()
    TweenService:Create(MSDot,    TIF, {BackgroundColor3 = on and C_ON_BRD or C_OFF_BRD}):Play()
    TweenService:Create(MSBadge,  TIF, {TextColor3       = on and C_ON_BRD or C_OFF_BRD}):Play()
    MSSub.Text = on and "ON ◈ SPAMMING" or "OFF — press [E] to toggle"
end

local function ToggleMS()
    MSOn = not MSOn
    SetMSVisual(MSOn)
    if MSOn then DoStartMS() else DoStopMS() end
end

-- [E] keybind
UIS.InputBegan:Connect(function(inp, gp)
    if gp then return end
    if inp.KeyCode == Enum.KeyCode.E and MSSG.Enabled then
        ToggleMS()
    end
end)

-- MouseButton1Down fires the instant finger/click touches — zero delay
MSCard.MouseButton1Down:Connect(function()
    ToggleMS()
end)

-- Close button
MSClose.MouseButton1Click:Connect(function()
    if MSOn then MSOn=false; DoStopMS(); SetMSVisual(false) end
    MSSG.Enabled = false
end)

-- Expose open function to T1 tab button
_G.RyuuOpenManualSpam = function()
    MSSG.Enabled = true
    -- Slide in animation
    MSCard.Position = UDim2.new(0.5, 0, 1.2, 0)
    MSShadow.Position = UDim2.new(0.5, 4, 1.2, 4)
    TweenService:Create(MSCard, TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
    TweenService:Create(MSShadow, TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(0.5, 4, 0.5, 4)}):Play()
end

end -- end manual spam widget scope

-- ── TAB 3: VISUALS ───────────────────────────────────────────────
local T3 = W:CreateTab("🎨 Visuals", nil)
T3:CreateSection("Ball ESP")
T3:CreateToggle({Name="Ball ESP",CurrentValue=false,Flag="ESP",
    Callback=function(v) S.BallESP=v; if v then StartESP() else CleanESP() end end})
T3:CreateLabel("Red = targeting you | Green = targeting other")
T3:CreateDivider()
T3:CreateSection("Lighting")
T3:CreateToggle({Name="Fullbright",CurrentValue=false,Flag="FB",
    Callback=function(v) S.Fullbright=v; SetFB(v) end})
T3:CreateDivider()
T3:CreateSection("Camera")
T3:CreateSlider({Name="Field of View",Range={30,120},Increment=1,Suffix="°",
    CurrentValue=70,Flag="FOV",Callback=function(v) Cam.FieldOfView=v end})
T3:CreateDivider()
T3:CreateSection("🔮 Ability ESP")
T3:CreateLabel("Floating card above each enemy showing their equipped ability.")
T3:CreateLabel("Updates 5x per second. Shows: name, tier, distance, and ACTIVE flash.")
T3:CreateLabel("⚡ ACTIVE = ability is firing RIGHT NOW. Plan your parry.")
T3:CreateLabel("Color: 🟣 Mythic 🟠 Legendary 🟡 Rare ⚪ Common/None")
T3:CreateToggle({Name="Ability ESP",CurrentValue=false,Flag="AESP",Callback=function(v)
    S.AbilityESP=v
    if v then
        StartAbilityESP()
        Rayfield:Notify({
            Title="Ability ESP ◈ ON",
            Content="Scanning all enemies. Cards appear above heads.",
            Duration=3,Image=4483362458,
        })
    else
        CleanAbilityESP()
        Rayfield:Notify({Title="Ability ESP ◈ OFF",Content="Cards removed.",Duration=2,Image=4483362458})
    end
end})
T3:CreateDivider()
T3:CreateSection("⚡ Ability Detector (Free)")
T3:CreateLabel("Detects enemy ability activations and parries INSTANTLY.")
T3:CreateLabel("Covers: Slashes of Fury, Pull, Infinity.")
T3:CreateLabel("Fires before the ball even moves — ability-triggered parry.")
T3:CreateToggle({Name="Ability Detector", CurrentValue=false, Flag="ADET",
    Callback=function(v)
        if v then
            StartAbilDetect()
            Rayfield:Notify({Title="⚡ Ability Detector ON",Content="3 abilities monitored.",Duration=3,Image=4483362458})
        else
            StopAbilDetect()
        end
    end})

-- ── TAB 4: PLAYER ────────────────────────────────────────────────
local T4 = W:CreateTab("🧍 Player", nil)
T4:CreateSection("Movement")
T4:CreateSlider({Name="Walk Speed",Range={16,150},Increment=1,CurrentValue=16,Flag="WS",
    Callback=function(v) S.WalkSpeed=v
        local h=Plr.Character and Plr.Character:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed=v end end})
T4:CreateSlider({Name="Jump Power",Range={50,500},Increment=5,CurrentValue=50,Flag="JP",
    Callback=function(v) S.JumpPower=v
        local h=Plr.Character and Plr.Character:FindFirstChildOfClass("Humanoid")
        if h then h.JumpPower=v end end})
T4:CreateDivider()
T4:CreateSection("Abilities")
T4:CreateToggle({Name="Infinity Jump",CurrentValue=false,Flag="IJ",
    Callback=function(v) S.InfJump=v; if v then StartIJ() end end})
T4:CreateToggle({Name="Noclip",CurrentValue=false,Flag="NC",Callback=function(v)
    S.Noclip=v
    if v then StartNoclip() else
        local c=Plr.Character
        if c then for _,p in ipairs(c:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=true end end end
    end
end})
-- ── TAB: SWORD CHANGER ───────────────────────────────────────────
local T6 = W:CreateTab("⚔ Sword", nil)
T6:CreateSection("Sword Color ◈ Client-Side")
T6:CreateLabel("Changes your sword color. Visual only (only you see it).")
T6:CreateLabel("Rainbow cycles hue each time Apply is pressed.")
T6:CreateDropdown({
    Name="Sword Color",
    Options={"Default","Gold","Shadow Black","Crystal Blue","Crimson","Ghost White",
             "Neon Purple","Emerald","Rose Gold","Obsidian","Cyan","Orange","Rainbow"},
    CurrentOption={"Default"},
    MultipleOptions=false,
    Flag="SWSK",
    Callback=function(v)
        local name = type(v)=="table" and v[1] or tostring(v)
        ApplySwordPreset(name)
        Rayfield:Notify({Title="Sword ◈ "..name, Content="Color applied!", Duration=2, Image=4483362458})
    end
})
T6:CreateButton({Name="⚔ Apply Now", Callback=function()
    local _, parts = GetSwordParts()
    if #parts > 0 then
        ApplySwordPreset(CurrentSwordPreset)
        Rayfield:Notify({Title="Applied: "..CurrentSwordPreset, Content="Sword updated!", Duration=2, Image=4483362458})
    else
        Rayfield:Notify({Title="No sword equipped", Content="Equip your sword first", Duration=2, Image=4483362458})
    end
end})
T6:CreateDivider()
T6:CreateSection("Custom Color (RGB)")
T6:CreateSlider({Name="Red", Range={0,255},Increment=1,CurrentValue=128,Flag="SCR",
    Callback=function(v)
        local _,parts=GetSwordParts(); if #parts==0 then return end
        local curCol = parts[1].Color
        pcall(function()
            local col=Color3.fromRGB(v, curCol.G*255, curCol.B*255)
            for _,p in ipairs(parts) do pcall(function() p.Color=col end) end
        end)
    end})
T6:CreateSlider({Name="Green", Range={0,255},Increment=1,CurrentValue=128,Flag="SCG",
    Callback=function(v)
        local _,parts=GetSwordParts(); if #parts==0 then return end
        local curCol = parts[1].Color
        pcall(function()
            local col=Color3.fromRGB(curCol.R*255, v, curCol.B*255)
            for _,p in ipairs(parts) do pcall(function() p.Color=col end) end
        end)
    end})
T6:CreateSlider({Name="Blue", Range={0,255},Increment=1,CurrentValue=128,Flag="SCB",
    Callback=function(v)
        local _,parts=GetSwordParts(); if #parts==0 then return end
        local curCol = parts[1].Color
        pcall(function()
            local col=Color3.fromRGB(curCol.R*255, curCol.G*255, v)
            for _,p in ipairs(parts) do pcall(function() p.Color=col end) end
        end)
    end})
-- ── TAB 5: SETTINGS ──────────────────────────────────────────────
local T5 = W:CreateTab("⚙ Settings", nil)
T5:CreateSection("Keybinds")
T5:CreateKeybind({Name="Toggle Auto Parry",CurrentKeybind="F5",HoldToInteract=false,Flag="KB2",
    Callback=function()
        S.AutoParry=not S.AutoParry
        if S.AutoParry then StartEngine() else StopEngine() end
        Rayfield:Notify({Title="Auto Parry",Content=S.AutoParry and "ON" or "OFF",Duration=2,Image=4483362458})
    end})
T5:CreateKeybind({Name="Toggle Ball ESP",CurrentKeybind="F8",HoldToInteract=false,Flag="KB3",
    Callback=function() S.BallESP=not S.BallESP; if S.BallESP then StartESP() else CleanESP() end end})
T5:CreateDivider()
T5:CreateSection("About")
T5:CreateLabel("Ryuu's HUB V1 BETA")
T5:CreateDivider()
T5:CreateSection("🔗 Discord")
T5:CreateLabel("Click the button to copy the Discord invite link.")
T5:CreateButton({Name="📋 Copy Discord Link", Callback=function()
    local link = "https://discord.gg/S2fKywJutE"
    local ok = pcall(function() setclipboard(link) end)
    if ok then
        Rayfield:Notify({Title="Discord Link Copied!",Content=link,Duration=4,Image=4483362458})
    else
        Rayfield:Notify({Title="Link: discord.gg/S2fKywJutE",Content="Manually copy: https://discord.gg/S2fKywJutE",Duration=6,Image=4483362458})
    end
end})

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM UNLOCK ANIMATION ◈ NEW ADDITION
-- ══════════════════════════════════════════════════════════════════
local function PlayUnlockAnimation()
    local AnimSG = Instance.new("ScreenGui")
    AnimSG.Name = "RyuuPremiumAnim"
    AnimSG.ResetOnSpawn = false
    AnimSG.IgnoreGuiInset = true
    AnimSG.DisplayOrder = 99999
    pcall(function() AnimSG.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not AnimSG.Parent then pcall(function() AnimSG.Parent = Plr:WaitForChild("PlayerGui") end) end

    -- Dark overlay
    local Overlay = Instance.new("Frame", AnimSG)
    Overlay.Size = UDim2.new(1,0,1,0)
    Overlay.BackgroundColor3 = Color3.fromRGB(3,1,8)
    Overlay.BackgroundTransparency = 1
    Overlay.BorderSizePixel = 0
    Overlay.ZIndex = 1
    TweenService:Create(Overlay, TweenInfo.new(0.3), {BackgroundTransparency=0}):Play()
    task.wait(0.35)

    -- Ring burst
    local Ring = Instance.new("Frame", AnimSG)
    Ring.AnchorPoint = Vector2.new(0.5,0.5)
    Ring.Position = UDim2.new(0.5,0,0.42,0)
    Ring.Size = UDim2.new(0,0,0,0)
    Ring.BackgroundTransparency = 1
    Ring.BorderSizePixel = 0
    Ring.ZIndex = 2
    Instance.new("UICorner", Ring).CornerRadius = UDim.new(1,0)
    local RS = Instance.new("UIStroke", Ring)
    RS.Thickness = 2; RS.Color = Color3.fromRGB(140,50,255)
    TweenService:Create(Ring, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Size=UDim2.new(0,240,0,240)}):Play()
    task.wait(0.1)

    -- Inner ring
    local Ring2 = Instance.new("Frame", AnimSG)
    Ring2.AnchorPoint = Vector2.new(0.5,0.5)
    Ring2.Position = UDim2.new(0.5,0,0.42,0)
    Ring2.Size = UDim2.new(0,0,0,0)
    Ring2.BackgroundTransparency = 1
    Ring2.BorderSizePixel = 0
    Ring2.ZIndex = 2
    Instance.new("UICorner", Ring2).CornerRadius = UDim.new(1,0)
    local RS2 = Instance.new("UIStroke", Ring2)
    RS2.Thickness = 1; RS2.Color = Color3.fromRGB(200,120,255); RS2.Transparency = 0.3
    TweenService:Create(Ring2, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Size=UDim2.new(0,185,0,185)}):Play()
    task.wait(0.1)

    -- RYUU label
    local RyuuLbl = Instance.new("TextLabel", AnimSG)
    RyuuLbl.AnchorPoint = Vector2.new(0.5,0.5)
    RyuuLbl.Position = UDim2.new(0.5,0,0.42,0)
    RyuuLbl.Size = UDim2.new(0,260,0,60)
    RyuuLbl.BackgroundTransparency = 1
    RyuuLbl.Text = "RYUU"
    RyuuLbl.Font = Enum.Font.GothamBlack
    RyuuLbl.TextSize = 54
    RyuuLbl.TextColor3 = Color3.fromRGB(255,255,255)
    RyuuLbl.TextTransparency = 1
    RyuuLbl.ZIndex = 3
    local RG = Instance.new("UIGradient", RyuuLbl)
    RG.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(180,90,255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(180,90,255)),
    }
    RG.Rotation = 90
    TweenService:Create(RyuuLbl, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {TextTransparency=0}):Play()
    task.wait(0.2)

    -- Card slides up
    local Card = Instance.new("Frame", AnimSG)
    Card.AnchorPoint = Vector2.new(0.5,0)
    Card.Position = UDim2.new(0.5,0,0.56,0)
    Card.Size = UDim2.new(0,0,0,0)
    Card.BackgroundColor3 = Color3.fromRGB(10,5,24)
    Card.BorderSizePixel = 0
    Card.ZIndex = 4
    Instance.new("UICorner", Card).CornerRadius = UDim.new(0,14)
    local CS = Instance.new("UIStroke", Card)
    CS.Thickness = 1.5; CS.Color = Color3.fromRGB(150,60,255)
    TweenService:Create(Card, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Size=UDim2.new(0,280,0,100)}):Play()
    task.wait(0.2)

    -- Heading
    local H = Instance.new("TextLabel", Card)
    H.Size = UDim2.new(1,-16,0,26)
    H.Position = UDim2.new(0,8,0,14)
    H.BackgroundTransparency = 1
    H.Text = "PREMIUM UNLOCKED"
    H.Font = Enum.Font.GothamBlack
    H.TextSize = 20
    H.TextTransparency = 1
    H.ZIndex = 5
    local HG = Instance.new("UIGradient", H)
    HG.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(200,120,255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(200,120,255)),
    }
    TweenService:Create(H, TweenInfo.new(0.22), {TextTransparency=0}):Play()

    local Div = Instance.new("Frame", Card)
    Div.Size = UDim2.new(0.7,0,0,1)
    Div.Position = UDim2.new(0.15,0,0,48)
    Div.BackgroundColor3 = Color3.fromRGB(100,40,200)
    Div.BorderSizePixel = 0; Div.ZIndex = 5

    local Sub = Instance.new("TextLabel", Card)
    Sub.Size = UDim2.new(1,-16,0,16)
    Sub.Position = UDim2.new(0,8,0,56)
    Sub.BackgroundTransparency = 1
    Sub.Text = "All premium features are now active"
    Sub.Font = Enum.Font.Gotham
    Sub.TextSize = 11
    Sub.TextColor3 = Color3.fromRGB(155,100,240)
    Sub.TextTransparency = 1; Sub.ZIndex = 5
    TweenService:Create(Sub, TweenInfo.new(0.22), {TextTransparency=0}):Play()

    local Tag = Instance.new("TextLabel", Card)
    Tag.Size = UDim2.new(1,0,0,14)
    Tag.Position = UDim2.new(0,0,1,-18)
    Tag.BackgroundTransparency = 1
    Tag.Text = "Ryuu's HUB · V1 BETA"
    Tag.Font = Enum.Font.Gotham
    Tag.TextSize = 9
    Tag.TextColor3 = Color3.fromRGB(70,45,120)
    Tag.TextTransparency = 1; Tag.ZIndex = 5
    TweenService:Create(Tag, TweenInfo.new(0.22), {TextTransparency=0}):Play()

    -- Stroke pulse
    TweenService:Create(CS, TweenInfo.new(0.2), {Color=Color3.fromRGB(255,200,255), Thickness=2.5}):Play()
    task.wait(0.25)
    TweenService:Create(CS, TweenInfo.new(0.35), {Color=Color3.fromRGB(150,60,255), Thickness=1.5}):Play()

    task.wait(1.8)

    -- Fade out
    local ft = TweenInfo.new(0.3)
    for _, obj in ipairs({H, Sub, Tag, RyuuLbl}) do
        TweenService:Create(obj, ft, {TextTransparency=1}):Play()
    end
    TweenService:Create(Card,    ft, {BackgroundTransparency=1}):Play()
    TweenService:Create(CS,      ft, {Transparency=1}):Play()
    TweenService:Create(RS,      ft, {Transparency=1}):Play()
    TweenService:Create(RS2,     ft, {Transparency=1}):Play()
    TweenService:Create(Overlay, TweenInfo.new(0.35), {BackgroundTransparency=1}):Play()
    task.wait(0.4)
    pcall(function() AnimSG:Destroy() end)
end
-- RemovePremiumLock is a no-op now (no overlay to remove)
local function RemovePremiumLock() end

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM TAB
-- ══════════════════════════════════════════════════════════════════
-- ── Forward declarations: all premium backend functions ──────────
-- Declared here so PT callbacks (below) can reference them as upvalues.
-- Actual implementations are assigned further down inside do..end blocks.
local StartTrajectory, StopTrajectory
local StartDistESP, StopDistESP
local StartBallSpeedMeter, StopBallSpeedMeter
local ApplyChams, RemoveChams
local StartSafeZone, StopSafeZone
local ApplyPremFPS, RemovePremFPS
local SetGhost
local StartAutoDodge, StopAutoDodge
local StartComboTracker, StopComboTracker
local StartSilentBallHighlight, StopSilentBallHighlight
local StartTargetAlert, StopTargetAlert
local StartAntiBlock, StopAntiBlock
local StartParrySound, StopParrySound
local StartAbilDetect, StopAbilDetect
local StartPremAbilDetect, StopPremAbilDetect
local StartPremAbilityESP, StopPremAbilityESP

local PT = W:CreateTab("✨ Premium", nil)
local PremiumUnlocked = false
local EnteredKey = ""

local function PremGuard(v, name, cb)
    if not PremiumUnlocked then
        Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
        return
    end
    cb(v)
end

PT:CreateSection("✨ Ryuu's HUB Premium")
PT:CreateLabel("Premium gives you the Aggressive Parry engine —")
PT:CreateLabel("lower cooldown, faster fire, god-tier close range.")
PT:CreateDivider()

PT:CreateSection("🔑 Enter Whitelist Key")
PT:CreateLabel("Paste the key you received from the Discord bot below.")
PT:CreateInput({
    Name = "Your Key",
    PlaceholderText = "RYUU-XXXXXX-XXXXXX-XXXXXX-XXXXXX",
    RemoveTextAfterFocusLost = false,
    Flag = "WLKey",
    Callback = function(val)
        EnteredKey = val or ""
    end,
})
PT:CreateButton({Name = "✅ Unlock Premium", Callback = function()
    if EnteredKey == "" then
        pcall(function()
            local fl = Rayfield.Flags and Rayfield.Flags.WLKey
            EnteredKey = (fl and fl.Value) or ""
        end)
    end
    local key = EnteredKey:match("^%s*(.-)%s*$")
    if #key < 10 then
        Rayfield:Notify({Title="⚠️ No Key",Content="Paste your key in the field first.",Duration=4,Image=4483362458})
        return
    end
    if PremiumUnlocked then
        Rayfield:Notify({Title="Already Unlocked",Content="Premium is active!",Duration=3,Image=4483362458})
        return
    end
    local result = ValidateKey(key)
    if result == "ok" then
        PremiumUnlocked = true
        PremiumActive   = true
        -- Remove lock overlay + play cinematic
        task.spawn(RemovePremiumLock)
        task.spawn(PlayUnlockAnimation)
        Rayfield:Notify({Title="✨ Premium Unlocked!",Content="Welcome to Ryuu's HUB Premium.",Duration=5,Image=4483362458})
        if S.AutoParry then
            StopEngine()
            task.wait(0.1)
            StartPremiumEngine()
        end
    elseif result == "locked_other" then
        Rayfield:Notify({Title="🔒 Key Already Used",Content="This key is bound to another device.",Duration=5,Image=4483362458})
    else
        Rayfield:Notify({Title="❌ Invalid Key",Content="That key is not valid. Check Discord.",Duration=4,Image=4483362458})
    end
end})

PT:CreateDivider()

-- Premium features — guarded by PremiumUnlocked check
PT:CreateSection("⚡ Premium Parry Engine")
PT:CreateLabel("Ultra-tight hitbox · 2-frame prediction · Quad curve · 0.15s cooldown")
PT:CreateLabel("God Emergency (2.5 studs) · Boost Detection (3-frame) · Auto-face")

PT:CreateToggle({Name="Premium Auto Parry", CurrentValue=false, Flag="PAP",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        if v then
            StopEngine(); P.AutoParry=true; StartPremiumEngine()
            Rayfield:Notify({Title="⚡ Premium Engine ON",Content="God-tier parry active.",Duration=3,Image=4483362458})
        else
            StopPremiumEngine(); P.AutoParry=false
        end
    end})

PT:CreateToggle({Name="God Emergency (2.5 studs)", CurrentValue=false, Flag="PGE",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.GodEmergency=v
    end})

PT:CreateToggle({Name="Curve Crusher (5-frame AntiCurve)", CurrentValue=true, Flag="PCC",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.CurveCrusher=v
    end})

PT:CreateToggle({Name="Auto Face Ball (Premium)", CurrentValue=false, Flag="PAF",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.AutoFace=v
    end})

PT:CreateToggle({Name="Quad Curve Prediction", CurrentValue=true, Flag="PQCP",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.QuadCurvePredict=v
    end})

PT:CreateToggle({Name="Advanced Boost Detection", CurrentValue=true, Flag="PABD",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.SpeedBoostDetect=v
    end})

PT:CreateDivider()
PT:CreateSection("🎯 Premium Hitbox")
PT:CreateLabel("4.0 studs default — fires even later for ultimate precision.")
PT:CreateSlider({Name="Hitbox Target",Range={2.5,6.0},Increment=0.1,CurrentValue=4.0,Flag="PHB",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        P.HitboxTarget=v
    end})

PT:CreateDivider()
PT:CreateSection("📊 Premium Status")
PT:CreateButton({Name="Check Premium Status", Callback=function()
    if PremiumUnlocked then
        Rayfield:Notify({Title="✨ Premium ACTIVE",Content="All premium engines are unlocked.",Duration=4,Image=4483362458})
    else
        Rayfield:Notify({Title="🔒 Not Unlocked",Content="Enter your key above.",Duration=4,Image=4483362458})
    end
end})

PT:CreateDivider()

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM ABILITY ESP ◈ Clean + Performance Optimised
-- ══════════════════════════════════════════════════════════════════
local PremAbilityBBs    = {}
local PremAbilityRunning = false

local TIER_COLORS = {
    S = Color3.fromRGB(255, 60, 220),
    A = Color3.fromRGB(255, 130, 30),
    B = Color3.fromRGB(80,  220, 120),
    C = Color3.fromRGB(255, 220, 40),
    D = Color3.fromRGB(160, 160, 160),
}

-- Ability icon image IDs. 0 = no image yet, shows tier letter fallback.
-- The auto-scanner below tries to find real IDs from the game at runtime.
local ABILITY_IMAGES = {
    ["Infinity"]=0,["Time Hole"]=0,["Slash of Duality"]=0,["Dribble"]=0,
    ["Dragon Spirit"]=0,["Pull"]=0,["Guardian Angel"]=0,["Reaper"]=0,
    ["Slashes of Fury"]=0,["Forcefield"]=0,["Bunny Leap"]=0,
    ["Calming Deflection"]=0,["Singularity"]=0,["Invisibility"]=0,
    ["Titan Blade"]=0,["Scopophobia"]=0,["Blade Trap"]=0,["Golden Ball"]=0,
    ["Continuity Zero"]=0,["Death Slash"]=0,["Phantom"]=0,["Swap"]=0,
    ["Doppelganger"]=0,["Necromancer"]=0,["Serpent Shadow Clone"]=0,
    ["Phase Bypass"]=0,["Hell Hook"]=0,["Blink"]=0,["Qi-Charge"]=0,
    ["Telekinesis"]=0,["Aerodynamic Slash"]=0,["Rapture"]=0,
    ["Thunder Dash"]=0,["Flash Counter"]=0,["Absolute Confidence"]=0,
    ["Pulse"]=0,["Bounty"]=0,["Luck"]=0,["Decrypted Clone"]=0,
    ["Raging Deflection"]=0,["Tact"]=0,["Waypoint"]=0,["Misfortune"]=0,
    ["Freeze"]=0,["Shadow Step"]=0,["Quantum Arena"]=0,["Force"]=0,
    ["Dash"]=0,["Wind Cloak"]=0,["Quasar"]=0,["Chieftain's Totem"]=0,
    ["Freeze Trap"]=0,["Martyrdom"]=0,["Platform"]=0,
    ["Super Jump"]=0,["Quad Jump"]=0,
}

-- Auto-scan the game for ability icon images at runtime
task.spawn(function()
    task.wait(3)
    local function scan(root)
        pcall(function()
            for _, obj in ipairs(root:GetDescendants()) do
                if (obj:IsA("ImageLabel") or obj:IsA("ImageButton")) and obj.Image ~= "" then
                    local id = tonumber(obj.Image:match("rbxassetid://(%d+)"))
                    if id and id > 0 then
                        local p = obj.Parent
                        while p and p ~= root do
                            if ABILITY_IMAGES[p.Name] == 0 then
                                ABILITY_IMAGES[p.Name] = id
                            end
                            p = p.Parent
                        end
                    end
                end
            end
        end)
    end
    scan(game:GetService("ReplicatedStorage"))
    scan(workspace)
    pcall(function() scan(game:GetService("StarterGui")) end)
end)

local function PremBuildBB(plr)
    if not (plr and plr.Character) then return end
    local head = plr.Character:FindFirstChild("Head"); if not head then return end
    local old = PremAbilityBBs[plr]
    if old and old.bb and old.bb.Parent then pcall(function() old.bb:Destroy() end) end

    local bb = Instance.new("BillboardGui")
    bb.Name="RyuuPremAbilESP"; bb.AlwaysOnTop=true
    bb.Size=UDim2.new(0,148,0,44); bb.StudsOffset=Vector3.new(0,3.2,0)
    bb.MaxDistance=80; bb.ClipsDescendants=false; bb.Parent=head

    local bg=Instance.new("Frame",bb)
    bg.Size=UDim2.new(1,0,1,0); bg.BackgroundColor3=Color3.fromRGB(6,4,14)
    bg.BackgroundTransparency=0.08; bg.BorderSizePixel=0
    Instance.new("UICorner",bg).CornerRadius=UDim.new(0,10)
    local stroke=Instance.new("UIStroke",bg)
    stroke.Name="S"; stroke.Thickness=1.2
    stroke.Color=Color3.fromRGB(60,30,110); stroke.Transparency=0

    local nL=Instance.new("TextLabel",bg); nL.Name="N"
    nL.Size=UDim2.new(1,-52,0,16); nL.Position=UDim2.new(0,4,0,3)
    nL.BackgroundTransparency=1; nL.Text=plr.Name
    nL.TextColor3=Color3.fromRGB(130,120,160); nL.Font=Enum.Font.Gotham
    nL.TextSize=10; nL.TextXAlignment=Enum.TextXAlignment.Left
    nL.TextTruncate=Enum.TextTruncate.AtEnd

    local aL=Instance.new("TextLabel",bg); aL.Name="A"
    aL.Size=UDim2.new(1,-52,0,18); aL.Position=UDim2.new(0,4,0,21)
    aL.BackgroundTransparency=1; aL.Text="—"
    aL.TextColor3=Color3.fromRGB(180,180,200); aL.Font=Enum.Font.GothamBold
    aL.TextSize=12; aL.TextXAlignment=Enum.TextXAlignment.Left
    aL.TextTruncate=Enum.TextTruncate.AtEnd

    -- Icon badge: 36x36 square, shows ability image or tier letter fallback
    local ib=Instance.new("Frame",bg); ib.Name="IB"
    ib.Size=UDim2.new(0,36,0,36); ib.Position=UDim2.new(1,-40,0.5,-18)
    ib.BackgroundColor3=Color3.fromRGB(18,10,36); ib.BorderSizePixel=0
    Instance.new("UICorner",ib).CornerRadius=UDim.new(0,7)
    local iStroke=Instance.new("UIStroke",ib)
    iStroke.Name="IS"; iStroke.Thickness=1.2; iStroke.Color=Color3.fromRGB(80,40,160)

    local img=Instance.new("ImageLabel",ib); img.Name="IMG"
    img.Size=UDim2.new(1,-4,1,-4); img.Position=UDim2.new(0,2,0,2)
    img.BackgroundTransparency=1; img.Image=""
    img.ScaleType=Enum.ScaleType.Fit; img.ImageTransparency=1
    Instance.new("UICorner",img).CornerRadius=UDim.new(0,5)

    local tf=Instance.new("TextLabel",ib); tf.Name="TF"
    tf.Size=UDim2.new(1,0,1,0); tf.BackgroundTransparency=1
    tf.Text="?"; tf.TextColor3=Color3.fromRGB(160,90,255)
    tf.Font=Enum.Font.GothamBlack; tf.TextSize=14

    PremAbilityBBs[plr]={bb=bb, lastAbility="__NONE__"}
end

local function PremRefreshBB(plr)
    if not (plr and plr.Character) then return end
    local entry=PremAbilityBBs[plr]; if not entry then return end
    local head=plr.Character:FindFirstChild("Head"); if not head then return end
    if not entry.bb or not entry.bb.Parent or entry.bb.Parent~=head then
        PremBuildBB(plr); entry=PremAbilityBBs[plr]; if not entry then return end
    end
    local abilityName=ScanAbility(plr)
    local displayName=abilityName or "No Ability"
    if entry.lastAbility==displayName then return end
    entry.lastAbility=displayName

    local bg=entry.bb:FindFirstChildOfClass("Frame"); if not bg then return end
    local aL=bg:FindFirstChild("A"); if not aL then return end
    local sk=bg:FindFirstChild("S")
    local ib=bg:FindFirstChild("IB"); if not ib then return end
    local img=ib:FindFirstChild("IMG")
    local tf=ib:FindFirstChild("TF")
    local iSk=ib:FindFirstChild("IS")

    local data=abilityName and BB_ABILITIES[abilityName]
    if data then
        local tc=TIER_COLORS[data.tier] or Color3.fromRGB(160,90,255)
        local warn=data.warn
        aL.Text=(warn and "⚠ " or "")..abilityName
        aL.TextColor3=warn and Color3.fromRGB(255,70,70) or tc
        if sk then sk.Color=warn and Color3.fromRGB(200,40,40)
            or Color3.fromRGB(math.floor(tc.R*128),math.floor(tc.G*128),math.floor(tc.B*128)) end
        if iSk then iSk.Color=warn and Color3.fromRGB(200,40,40) or tc end
        -- Show image or tier letter
        local imgId=ABILITY_IMAGES[abilityName]
        if imgId and imgId>0 then
            if img then img.Image="rbxassetid://"..imgId; img.ImageTransparency=0
                img.ImageColor3=warn and Color3.fromRGB(255,180,180) or Color3.new(1,1,1) end
            if tf then tf.Visible=false end
        else
            if img then img.ImageTransparency=1 end
            if tf then tf.Visible=true; tf.Text=data.tier
                tf.TextColor3=warn and Color3.fromRGB(255,100,100) or tc end
        end
    else
        aL.Text=abilityName or "No Ability"; aL.TextColor3=Color3.fromRGB(80,75,100)
        if sk then sk.Color=Color3.fromRGB(30,18,55) end
        if img then img.ImageTransparency=1 end
        if iSk then iSk.Color=Color3.fromRGB(50,30,90) end
        if tf then tf.Visible=true; tf.Text="?"; tf.TextColor3=Color3.fromRGB(80,75,100) end
    end
end


local function StartPremAbilityESP()
    PremAbilityRunning = true
    -- Build boards for everyone already in
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr and p.Character then PremBuildBB(p) end
    end
    -- Single shared polling loop — much cheaper than per-player loops
    task.spawn(function()
        while PremAbilityRunning do
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= Plr then
                    PremRefreshBB(p)
                end
            end
            -- Prune players who left
            for p, entry in pairs(PremAbilityBBs) do
                if not p.Parent then
                    pcall(function() if entry.bb and entry.bb.Parent then entry.bb:Destroy() end end)
                    PremAbilityBBs[p] = nil
                end
            end
            task.wait(0.6)  -- lighter interval than free's 0.4
        end
    end)
    -- Hook new players / respawns
    Players.PlayerAdded:Connect(function(p)
        if not PremAbilityRunning then return end
        p.CharacterAdded:Connect(function()
            task.wait(0.4)
            if PremAbilityRunning then PremBuildBB(p) end
        end)
    end)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr then
            p.CharacterAdded:Connect(function()
                task.wait(0.4)
                if PremAbilityRunning then PremBuildBB(p) end
            end)
        end
    end
end

local function StopPremAbilityESP()
    PremAbilityRunning = false
    for _, entry in pairs(PremAbilityBBs) do
        pcall(function() if entry.bb and entry.bb.Parent then entry.bb:Destroy() end end)
    end
    PremAbilityBBs = {}
end

-- ── Premium tab toggle ────────────────────────────────────────────
PT:CreateSection("👁 Premium Ability ESP")
PT:CreateLabel("Cleaner cards · Dirty-flag updates · No frame drops")
PT:CreateLabel("⚠ Red border = dangerous ability targeting you")
PT:CreateToggle({Name="Premium Ability ESP", CurrentValue=false, Flag="PAESP",
    Callback=function(v)
        if not PremiumUnlocked then
            Rayfield:Notify({Title="🔒 Locked",Content="Enter your premium key first.",Duration=3,Image=4483362458})
            return
        end
        if v then
            StartPremAbilityESP()
            Rayfield:Notify({Title="👁 Ability ESP ON",Content="Clean cards active.",Duration=3,Image=4483362458})
        else
            StopPremAbilityESP()
            Rayfield:Notify({Title="👁 Ability ESP OFF",Content="Cards removed.",Duration=2,Image=4483362458})
        end
    end})

PT:CreateDivider()
PT:CreateSection("⚡ Premium Ability Detector")
PT:CreateLabel("10 ability signatures. Fires PremiumFire() the instant any activates.")
PT:CreateLabel("Slashes of Fury · Time Hole · Slash of Duality · Dragon Spirit")
PT:CreateLabel("Pull · Infinity · Phantom · Hell Hook · Thunder Dash · Doppelganger")
PT:CreateLabel("⚠ 0.4s cooldown per enemy to prevent spam.")
PT:CreateToggle({Name="Premium Ability Detector", CurrentValue=false, Flag="PADET",
    Callback=function(v)
        PremGuard(v, "PremAbilDetect", function(val)
            if val then
                StartPremAbilDetect()
                Rayfield:Notify({Title="⚡ Prem Detector ON",Content="10 abilities monitored.",Duration=3,Image=4483362458})
            else
                StopPremAbilDetect()
            end
        end)
    end})
PT:CreateSlider({Name="Detect Cooldown (per enemy)",Range={10,100},Increment=5,CurrentValue=40,Suffix="0ms",Flag="PDETCD",
    Callback=function(v)
        if not PremiumUnlocked then return end
        Det.cd = v / 100
    end})


do
-- ══════════════════════════════════════════════════════════════════
-- ABILITY DETECTOR ENGINE ◈ Free + Premium
-- ══════════════════════════════════════════════════════════════════
-- Watches ALL players for ability animations/particles/state changes.
-- When a dangerous ability activates on an enemy who has the ball,
-- it instantly fires a preemptive parry BEFORE the ball even moves.
--
-- HOW IT WORKS:
-- 1. Scans enemy characters every frame for known ability animation IDs,
--    particle emitter names, and attribute changes that signal activation.
-- 2. Checks if that enemy currently holds the ball (target == their name).
-- 3. Fires parry immediately — bypasses the normal distance/ping logic.
-- 4. Per-ability cooldown prevents double-firing.
--
-- Detected abilities and their signatures:
--   Slashes of Fury  — AnimationTrack name or particle burst "SlashFury"
--   Infinity         — "InfinityAura" particle or Attribute "AbilityActive"
--   Time Hole        — "TimeHole" BasePart spawning near player
--   Slash of Duality — "DualitySlash" animation or part
--   Dragon Spirit    — "DragonCharge" particle burst
--   Pull             — attribute "PullActive" or "AbilityActive"=Pull
--   Doppelganger     — "Clone" part spawning on enemy
--   Phantom          — enemy transparency drop (going invisible)
--   Hell Hook        — "HookProjectile" part near enemy
--   Thunder Dash     — "ThunderTrail" particle on enemy
-- ══════════════════════════════════════════════════════════════════

local Det = {conn=nil, pconn=nil, cds={}, cd=0.4}

-- Ability signature definitions
-- Each entry: name (for logging), check function(char, plr) -> bool
local FREE_SIGS = {
    -- Slashes of Fury: look for rapid animation or particle named SlashFury/FurySlash
    {
        name = "Slashes of Fury",
        check = function(char, plr)
            if not char then return false end
            -- Check for active animation track with fury/slash in name
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                local animator = hum:FindFirstChildOfClass("Animator")
                if animator then
                    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                        local n = track.Name:lower()
                        if n:find("fury") or n:find("slash") or n:find("barrage") then
                            return true
                        end
                    end
                end
            end
            -- Check for fury particles on character
            for _, v in ipairs(char:GetDescendants()) do
                if v:IsA("ParticleEmitter") then
                    local n = v.Name:lower()
                    if n:find("fury") or n:find("slash") then return true end
                end
                if v:IsA("BasePart") then
                    local n = v.Name:lower()
                    if n:find("slash") or n:find("fury") then return true end
                end
            end
            return false
        end,
    },
    -- Pull: watch for AbilityActive attribute or pull-named part
    {
        name = "Pull",
        check = function(char, plr)
            if not char then return false end
            local aa = plr:GetAttribute("AbilityActive")
            if aa and tostring(aa):lower():find("pull") then return true end
            for _, v in ipairs(char:GetDescendants()) do
                if v:IsA("BasePart") and v.Name:lower():find("pull") then return true end
            end
            return false
        end,
    },
    -- Infinity: InfinityAura particle or attribute
    {
        name = "Infinity",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                if (v:IsA("ParticleEmitter") or v:IsA("SelectionBox") or v:IsA("BasePart"))
                    and v.Name:lower():find("infinity") then
                    return true
                end
            end
            return false
        end,
    },
}

-- Premium has all free sigs + more precise + faster poll
local PREM_SIGS = {
    -- Slashes of Fury (enhanced — also checks attribute + hit parts)
    {
        name = "Slashes of Fury",
        check = function(char, plr)
            if not char then return false end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                local animator = hum:FindFirstChildOfClass("Animator")
                if animator then
                    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                        local n = track.Name:lower()
                        if n:find("fury") or n:find("slash") or n:find("barrage") or n:find("multi") then
                            return true
                        end
                    end
                end
            end
            for _, v in ipairs(char:GetDescendants()) do
                local n = v.Name:lower()
                if v:IsA("ParticleEmitter") and (n:find("fury") or n:find("slash")) then return true end
                if v:IsA("BasePart") and (n:find("slash") or n:find("fury") or n:find("hit")) then return true end
            end
            local aa = plr:GetAttribute("AbilityActive")
            if aa and tostring(aa):lower():find("fury") then return true end
            return false
        end,
    },
    -- Time Hole: circular/hole part spawning or attribute
    {
        name = "Time Hole",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                local n = v.Name:lower()
                if (n:find("hole") or n:find("time") or n:find("vortex")) and
                   (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
            end
            -- Also scan workspace near this player for time hole part
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                for _, v in ipairs(workspace:GetDescendants()) do
                    if v:IsA("BasePart") then
                        local n = v.Name:lower()
                        if (n:find("hole") or n:find("timewarp")) and
                           (v.Position - hrp.Position).Magnitude < 30 then
                            return true
                        end
                    end
                end
            end
            return false
        end,
    },
    -- Slash of Duality: dual slash parts or animation
    {
        name = "Slash of Duality",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                local n = v.Name:lower()
                if (n:find("dual") or n:find("duality")) and
                   (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
            end
            return false
        end,
    },
    -- Dragon Spirit: dragon charge particle/part
    {
        name = "Dragon Spirit",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                local n = v.Name:lower()
                if (n:find("dragon") or n:find("spirit") or n:find("charge")) and
                   (v:IsA("BasePart") or v:IsA("ParticleEmitter")) then return true end
            end
            return false
        end,
    },
    -- Pull
    {
        name = "Pull",
        check = function(char, plr)
            if not char then return false end
            local aa = plr:GetAttribute("AbilityActive")
            if aa and tostring(aa):lower():find("pull") then return true end
            for _, v in ipairs(char:GetDescendants()) do
                if v:IsA("BasePart") and v.Name:lower():find("pull") then return true end
            end
            return false
        end,
    },
    -- Infinity
    {
        name = "Infinity",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                if (v:IsA("ParticleEmitter") or v:IsA("BasePart"))
                    and v.Name:lower():find("infinity") then return true end
            end
            return false
        end,
    },
    -- Phantom: enemy goes semi-transparent (< 0.5 transparency)
    {
        name = "Phantom",
        check = function(char, plr)
            if not char then return false end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and hrp.Transparency > 0.4 then return true end
            return false
        end,
    },
    -- Hell Hook: hook projectile part near enemy
    {
        name = "Hell Hook",
        check = function(char, plr)
            if not char then return false end
            local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return false end
            for _, v in ipairs(workspace:GetDescendants()) do
                if v:IsA("BasePart") then
                    local n = v.Name:lower()
                    if (n:find("hook") or n:find("hell")) and
                       (v.Position - hrp.Position).Magnitude < 25 then return true end
                end
            end
            return false
        end,
    },
    -- Thunder Dash: thunder trail particle on enemy
    {
        name = "Thunder Dash",
        check = function(char, plr)
            if not char then return false end
            for _, v in ipairs(char:GetDescendants()) do
                local n = v.Name:lower()
                if v:IsA("ParticleEmitter") and (n:find("thunder") or n:find("dash") or n:find("lightning")) then
                    return true
                end
            end
            return false
        end,
    },
    -- Doppelganger: clone part near enemy
    {
        name = "Doppelganger",
        check = function(char, plr)
            if not char then return false end
            local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return false end
            for _, p2 in ipairs(Players:GetPlayers()) do
                if p2 ~= plr and p2 ~= Plr then
                    local c2 = p2.Character
                    if c2 then
                        local hrp2 = c2:FindFirstChild("HumanoidRootPart")
                        if hrp2 and (hrp2.Position - hrp.Position).Magnitude < 5 then
                            return true  -- two players occupying same space = clone
                        end
                    end
                end
            end
            return false
        end,
    },
}

local function RunDetector(sigList, fireFn, pollRate)
    local conn
    conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall(); if not ball then return end
            local tgt  = ball:GetAttribute("target"); if not tgt then return end
            -- Only care about enemies who currently have the ball targeting them
            -- AND who are not us (we only parry when they redirect back at us,
            -- but ability detects should fire when ball is heading to us from them)
            -- Actually: fire when the ball is targeting US — enemy already sent it
            -- OR when enemy has ability active and ball is near them about to redirect
            local myHRP = GetHRP(); if not myHRP then return end

            for _, p in ipairs(Players:GetPlayers()) do
                if p == Plr then continue end
                local char = p.Character; if not char then continue end
                local hrp  = char:FindFirstChild("HumanoidRootPart"); if not hrp then continue end
                local distToMe = (myHRP.Position - hrp.Position).Magnitude
                if distToMe > 100 then continue end  -- too far, skip

                -- Check if this enemy currently holds the ball (it's targeting them)
                -- OR the ball was recently redirected by them at us
                local ballNearEnemy = (hrp.Position - ball.Position).Magnitude < 20
                local ballTargetsMe = tgt == Plr.Name

                if not (ballNearEnemy or ballTargetsMe) then continue end

                local now = os.clock()
                local cdKey = p.Name
                if Det.cds[cdKey] and now - Det.cds[cdKey] < Det.cd then continue end

                -- Run ability signatures
                for _, sig in ipairs(sigList) do
                    if sig.check(char, p) then
                        -- Ability detected — fire parry NOW
                        Det.cds[cdKey] = now
                        task.spawn(function()
                            fireFn()
                            Rayfield:Notify({
                                Title = "⚡ ABILITY DETECTED",
                                Content = sig.name .. " — parry fired!",
                                Duration = 1.2,
                                Image = 4483362458,
                            })
                        end)
                        break
                    end
                end
            end
        end)
    end)
    return conn
end

local AbilDetect = {active=false, premActive=false}

local function StartAbilDetect()
    if Det.conn then Det.conn:Disconnect() end
    Det.cds = {}
    AbilDetect.active = true
    Det.conn = RunDetector(FREE_SIGS, FireParry, 0)  -- heartbeat = every frame
end
local function StopAbilDetect()
    AbilDetect.active = false
    if Det.conn then Det.conn:Disconnect(); Det.conn = nil end
end
local function StartPremAbilDetect()
    if PDet.conn then PDet.conn:Disconnect() end
    Det.cds = {}
    AbilDetect.premActive = true
    PDet.conn = RunDetector(PREM_SIGS, PremiumFire, 0)
end
local function StopPremAbilDetect()
    AbilDetect.premActive = false
    if PDet.conn then PDet.conn:Disconnect(); PDet.conn = nil end
end

end -- ability detector do..end

do
-- ══════════════════════════════════════════════════════════════════
-- PREMIUM EXCLUSIVE FEATURES ◈ Backend systems
-- ══════════════════════════════════════════════════════════════════

-- ── 1. BALL TRAJECTORY LINE ──────────────────────────────────────
-- Draws a neon line from the ball toward YOU showing exactly where
-- it will land. Updates every frame. Premium only.
local Traj = {conn=nil, beam=nil}
local function StartTrajectory()
    if Traj.conn then Traj.conn:Disconnect() end
    Traj.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall(); if not ball then return end
            local hrp = GetHRP(); if not hrp then return end
            local isTgt = ball:GetAttribute("target") == Plr.Name
            if not isTgt then
                if Traj.beam then Traj.beam.Enabled = false end
                return
            end
            -- Draw/update attachment beam
            if not Traj.beam then
                local a0 = Instance.new("Attachment", ball)
                a0.Name = "RyuuTA0"
                local a1 = Instance.new("Attachment", hrp)
                a1.Name = "RyuuTA1"
                local beam = Instance.new("Beam", workspace.Terrain)
                beam.Name = "RyuuTraj.beam"
                beam.Attachment0 = a0
                beam.Attachment1 = a1
                beam.Width0 = 0.08
                beam.Width1 = 0.08
                beam.FaceCamera = true
                beam.Segments = 6
                beam.Transparency = NumberSequence.new{
                    NumberSequenceKeypoint.new(0, 0.2),
                    NumberSequenceKeypoint.new(1, 0.7),
                }
                beam.Color = ColorSequence.new(Color3.fromRGB(255, 60, 60))
                beam.LightEmission = 1
                beam.LightInfluence = 0
                beam.Enabled = true
                Traj.beam = beam
            else
                Traj.beam.Enabled = true
            end
        end)
    end)
end
local function StopTrajectory()
    if Traj.conn then Traj.conn:Disconnect(); Traj.conn = nil end
    pcall(function()
        if Traj.beam then Traj.beam:Destroy(); Traj.beam = nil end
        local hrp = GetHRP()
        if hrp then
            local a = hrp:FindFirstChild("RyuuTA1")
            if a then a:Destroy() end
        end
        local ball = GetBall()
        if ball then
            local a = ball:FindFirstChild("RyuuTA0")
            if a then a:Destroy() end
        end
        local b = workspace.Terrain:FindFirstChild("RyuuTraj.beam")
        if b then b:Destroy() end
    end)
end

-- ── 2. ENEMY DISTANCE ESP ────────────────────────────────────────
-- Floating tag above each enemy showing their stud distance from you.
-- Updates every 0.2s. Color: green=far, yellow=close, red=danger.
local DistESP = {bbs={}, conn=nil}
local function StartDistESP()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr and p.Character then
            local head = p.Character:FindFirstChild("Head")
            if head then
                local bb = Instance.new("BillboardGui", head)
                bb.Name = "RyuuDistESP"
                bb.AlwaysOnTop = true
                bb.Size = UDim2.new(0, 60, 0, 18)
                bb.StudsOffset = Vector3.new(0, 2.0, 0)
                bb.MaxDistance = 120
                local lbl = Instance.new("TextLabel", bb)
                lbl.Size = UDim2.new(1,0,1,0)
                lbl.BackgroundTransparency = 1
                lbl.Font = Enum.Font.GothamBold
                lbl.TextSize = 11
                lbl.Text = "-- st"
                lbl.TextColor3 = Color3.fromRGB(100,255,100)
                DistESP.bbs[p] = bb
            end
        end
    end
    DistESP.conn = RunService.Heartbeat:Connect(function()
        local hrp = GetHRP(); if not hrp then return end
        for p, bb in pairs(DistESP.bbs) do
            pcall(function()
                if not p.Parent or not p.Character then return end
                local ehr = p.Character:FindFirstChild("HumanoidRootPart")
                if not ehr or not bb.Parent then return end
                local d = math.floor((hrp.Position - ehr.Position).Magnitude)
                local lbl = bb:FindFirstChildOfClass("TextLabel")
                if lbl then
                    lbl.Text = d.."st"
                    lbl.TextColor3 = d <= 15 and Color3.fromRGB(255,60,60)
                                  or d <= 35 and Color3.fromRGB(255,200,50)
                                  or Color3.fromRGB(60,220,100)
                end
            end)
        end
    end)
    Players.PlayerAdded:Connect(function(p)
        p.CharacterAdded:Connect(function(char)
            task.wait(0.5)
            local head = char:FindFirstChild("Head"); if not head then return end
            local bb = Instance.new("BillboardGui", head)
            bb.Name = "RyuuDistESP"; bb.AlwaysOnTop = true
            bb.Size = UDim2.new(0,60,0,18); bb.StudsOffset = Vector3.new(0,2.0,0)
            bb.MaxDistance = 120
            local lbl = Instance.new("TextLabel", bb)
            lbl.Size = UDim2.new(1,0,1,0); lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 11
            lbl.Text = "-- st"; lbl.TextColor3 = Color3.fromRGB(100,255,100)
            DistESP.bbs[p] = bb
        end)
    end)
end
local function StopDistESP()
    if DistESP.conn then DistESP.conn:Disconnect(); DistESP.conn = nil end
    for _, bb in pairs(DistESP.bbs) do
        pcall(function() if bb and bb.Parent then bb:Destroy() end end)
    end
    DistESP.bbs = {}
end

-- ── 3. BALL SPEED METER ──────────────────────────────────────────
-- Floating HUD showing current ball speed in studs/s.
-- Also shows if a speed boost is active.
local BSM = {gui=nil, conn=nil}
local function StartBallSpeedMeter()
    if BSM.gui then pcall(function() BSM.gui:Destroy() end) end
    local sg = Instance.new("ScreenGui")
    sg.Name = "RyuuBSM"; sg.ResetOnSpawn = false
    sg.IgnoreGuiInset = true; sg.DisplayOrder = 9995
    pcall(function() sg.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not sg.Parent then pcall(function() sg.Parent = Plr:WaitForChild("PlayerGui") end) end
    BSM.gui = sg
    local card = Instance.new("Frame", sg)
    card.AnchorPoint = Vector2.new(1,1)
    card.Position = UDim2.new(1,-12,1,-12)
    card.Size = UDim2.new(0,160,0,46)
    card.BackgroundColor3 = Color3.fromRGB(8,4,18)
    card.BackgroundTransparency = 0.2
    card.BorderSizePixel = 0
    Instance.new("UICorner", card).CornerRadius = UDim.new(0,8)
    local stroke = Instance.new("UIStroke", card)
    stroke.Thickness = 1; stroke.Color = Color3.fromRGB(90,40,180)
    local titleLbl = Instance.new("TextLabel", card)
    titleLbl.Size = UDim2.new(1,-8,0,16)
    titleLbl.Position = UDim2.new(0,4,0,4)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = "⚽ BALL SPEED"
    titleLbl.Font = Enum.Font.GothamBold; titleLbl.TextSize = 10
    titleLbl.TextColor3 = Color3.fromRGB(120,80,200)
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    local speedLbl = Instance.new("TextLabel", card)
    speedLbl.Size = UDim2.new(1,-8,0,22)
    speedLbl.Position = UDim2.new(0,4,0,20)
    speedLbl.BackgroundTransparency = 1
    speedLbl.Text = "-- st/s"
    speedLbl.Font = Enum.Font.GothamBlack; speedLbl.TextSize = 16
    speedLbl.TextColor3 = Color3.fromRGB(200,200,200)
    speedLbl.TextXAlignment = Enum.TextXAlignment.Left
    BSM.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall()
            if not ball then speedLbl.Text = "No Ball"; speedLbl.TextColor3 = Color3.fromRGB(80,75,100); return end
            local spd = math.floor(GetVel(ball).Magnitude)
            local col = spd >= 150 and Color3.fromRGB(255,60,60)
                     or spd >= 80  and Color3.fromRGB(255,200,50)
                     or Color3.fromRGB(60,220,100)
            speedLbl.Text = spd.." st/s"
            speedLbl.TextColor3 = col
            stroke.Color = col
        end)
    end)
end
local function StopBallSpeedMeter()
    if BSM.conn then BSM.conn:Disconnect(); BSM.conn = nil end
    if BSM.gui then pcall(function() BSM.gui:Destroy() end); BSM.gui = nil end
end

-- ── 4. CHAMS (Enemy highlight through walls) ─────────────────────
-- Makes all enemies glow a solid highlight color visible through walls.
local Chams = {active=false, orig={}}
local function ApplyChams(col)
    Chams.orig = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Plr and p.Character then
            for _, part in ipairs(p.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    table.insert(Chams.orig, {
                        part = part,
                        color = part.Color,
                        mat = part.Material,
                        cast = part.CastShadow,
                    })
                    pcall(function()
                        part.Color = col
                        part.Material = Enum.Material.Neon
                        part.CastShadow = false
                    end)
                end
            end
        end
    end
    Chams.active = true
end
local function RemoveChams()
    for _, v in ipairs(Chams.orig) do
        pcall(function()
            v.part.Color = v.color
            v.part.Material = v.mat
            v.part.CastShadow = v.cast
        end)
    end
    Chams.orig = {}
    Chams.active = false
end

-- ── 5. SAFE ZONE INDICATOR ───────────────────────────────────────
-- Pulses screen edge red when ball is targeting YOU and close.
-- Warning kicks in when ball is within 25 studs.
local SZ = {gui=nil, conn=nil}
local function StartSafeZone()
    if SZ.gui then pcall(function() SZ.gui:Destroy() end) end
    local sg = Instance.new("ScreenGui")
    sg.Name = "RyuuSZ"; sg.ResetOnSpawn = false
    sg.IgnoreGuiInset = true; sg.DisplayOrder = 9994
    pcall(function() sg.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not sg.Parent then pcall(function() sg.Parent = Plr:WaitForChild("PlayerGui") end) end
    SZ.gui = sg
    local edge = Instance.new("Frame", sg)
    edge.Size = UDim2.new(1,0,1,0)
    edge.BackgroundTransparency = 1
    edge.BorderSizePixel = 0
    local stroke = Instance.new("UIStroke", edge)
    stroke.Thickness = 14
    stroke.Color = Color3.fromRGB(255,40,40)
    stroke.Transparency = 1
    SZ.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall(); if not ball then stroke.Transparency=1; return end
            local hrp = GetHRP(); if not hrp then stroke.Transparency=1; return end
            local isTgt = ball:GetAttribute("target") == Plr.Name
            if not isTgt then stroke.Transparency=1; return end
            local dist = (hrp.Position - ball.Position).Magnitude
            if dist <= 25 then
                local intensity = 1 - (dist / 25)
                stroke.Transparency = 1 - (intensity * 0.85)
                stroke.Color = dist <= 10
                    and Color3.fromRGB(255,40,40)
                    or  Color3.fromRGB(255,140,40)
            else
                stroke.Transparency = 1
            end
        end)
    end)
end
local function StopSafeZone()
    if SZ.conn then SZ.conn:Disconnect(); SZ.conn = nil end
    if SZ.gui then pcall(function() SZ.gui:Destroy() end); SZ.gui = nil end
end

-- ── 6. PREMIUM FPS BOOSTER ───────────────────────────────────────
-- More aggressive than the old free one. Removes ALL particles,
-- disables ALL post effects, nukes shadows, lowers render distance.
local PremFPS = {active=false, saved={effects={},casters={},fidelity={},parts={}}}
local function ApplyPremFPS()
    if PremFPS.active then return end; PremFPS.active = true
    PremFPS.saved = {effects={},casters={},fidelity={},parts={}}
    -- Nuke lighting
    pcall(function()
        PremFPS.saved.tech    = Lighting.Technology
        PremFPS.saved.shadow  = Lighting.GlobalShadows
        PremFPS.saved.envD    = Lighting.EnvironmentDiffuseScale
        PremFPS.saved.envS    = Lighting.EnvironmentSpecularScale
        PremFPS.saved.bright  = Lighting.Brightness
        Lighting.Technology               = Enum.Technology.Compatibility
        Lighting.GlobalShadows            = false
        Lighting.EnvironmentDiffuseScale  = 0
        Lighting.EnvironmentSpecularScale = 0
        for _, e in ipairs(Lighting:GetChildren()) do
            if e:IsA("PostEffect") or e:IsA("Sky") or e:IsA("Atmosphere") then
                table.insert(PremFPS.saved.effects, {o=e, was=e.Enabled})
                pcall(function() e.Enabled = false end)
            end
        end
    end)
    -- Nuke all workspace parts
    pcall(function()
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") then
                if v.CastShadow then
                    table.insert(PremFPS.saved.casters, v); v.CastShadow = false
                end
                if v.RenderFidelity ~= Enum.RenderFidelity.Performance then
                    table.insert(PremFPS.saved.fidelity, {o=v,was=v.RenderFidelity})
                    pcall(function() v.RenderFidelity = Enum.RenderFidelity.Performance end)
                end
            end
            -- Kill all particles
            if v:IsA("ParticleEmitter") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
                table.insert(PremFPS.saved.parts, {o=v,was=v.Enabled})
                pcall(function() v.Enabled = false end)
            end
        end
    end)
    -- Watch new parts
    if Conn.PremFPSWatch then Conn.PremFPSWatch:Disconnect() end
    Conn.PremFPSWatch = workspace.DescendantAdded:Connect(function(v)
        if not PremFPS.active then return end
        pcall(function()
            if v:IsA("BasePart") then
                v.CastShadow = false
                v.RenderFidelity = Enum.RenderFidelity.Performance
            end
            if v:IsA("ParticleEmitter") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
                v.Enabled = false
            end
        end)
    end)
end
local function RemovePremFPS()
    if not PremFPS.active then return end; PremFPS.active = false
    if Conn.PremFPSWatch then Conn.PremFPSWatch:Disconnect(); Conn.PremFPSWatch=nil end
    pcall(function()
        Lighting.Technology               = PremFPS.saved.tech
        Lighting.GlobalShadows            = PremFPS.saved.shadow
        Lighting.EnvironmentDiffuseScale  = PremFPS.saved.envD
        Lighting.EnvironmentSpecularScale = PremFPS.saved.envS
        if PremFPS.saved.bright then Lighting.Brightness = PremFPS.saved.bright end
    end)
    for _, e in ipairs(PremFPS.saved.effects)   do pcall(function() e.o.Enabled = e.was end) end
    for _, v in ipairs(PremFPS.saved.casters)   do pcall(function() v.CastShadow = true end) end
    for _, e in ipairs(PremFPS.saved.fidelity)  do pcall(function() e.o.RenderFidelity = e.was end) end
    for _, e in ipairs(PremFPS.saved.parts)     do pcall(function() e.o.Enabled = e.was end) end
end

-- ── 7. GHOST MODE (Server-side invisible) ────────────────────────
-- Makes your character transparent locally. Very useful for
-- dodging + confusing enemies visually.
local Ghost = {active=false, orig={}}
local function SetGhost(on)
    Ghost.active = on
    local char = Plr.Character; if not char then return end
    if on then
        Ghost.orig = {}
        for _, v in ipairs(char:GetDescendants()) do
            if v:IsA("BasePart") or v:IsA("Decal") then
                table.insert(Ghost.orig, {o=v, t=v.Transparency})
                pcall(function() v.Transparency = 0.75 end)
            end
        end
    else
        for _, v in ipairs(Ghost.orig) do
            pcall(function() v.o.Transparency = v.t end)
        end
        Ghost.orig = {}
    end
end

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM TAB ◈ New features added
-- ══════════════════════════════════════════════════════════════════
PT:CreateDivider()
PT:CreateSection("🎯 Ball Trajectory")
PT:CreateLabel("Red beam from ball to YOU — see exactly where it's going.")
PT:CreateToggle({Name="Ball Trajectory Line", CurrentValue=false, Flag="PTRAJ",
    Callback=function(v) PremGuard(v,"Trajectory",function(val)
        if val then StartTrajectory()
            Rayfield:Notify({Title="🎯 Trajectory ON",Content="Red line shows ball path.",Duration=3,Image=4483362458})
        else StopTrajectory() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("📏 Enemy Distance ESP")
PT:CreateLabel("Shows stud distance above every enemy. Red = danger zone.")
PT:CreateToggle({Name="Distance ESP", CurrentValue=false, Flag="PDESP",
    Callback=function(v) PremGuard(v,"DistESP",function(val)
        if val then StartDistESP()
            Rayfield:Notify({Title="📏 Distance ESP ON",Content="Stud tags active.",Duration=3,Image=4483362458})
        else StopDistESP() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("⚽ Ball Speed Meter")
PT:CreateLabel("Live ball speed in studs/s. Red = dangerous speed.")
PT:CreateToggle({Name="Ball Speed Meter", CurrentValue=false, Flag="PBSM",
    Callback=function(v) PremGuard(v,"BSM",function(val)
        if val then StartBallSpeedMeter()
            Rayfield:Notify({Title="⚽ Speed Meter ON",Content="Bottom-right corner.",Duration=3,Image=4483362458})
        else StopBallSpeedMeter() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🌟 Enemy Chams")
PT:CreateLabel("Enemies glow through walls. Neon highlight color.")
PT:CreateDropdown({Name="Chams Color",
    Options={"Red","Purple","Cyan","Yellow","White","Orange"},
    CurrentOption={"Red"}, MultipleOptions=false, Flag="PCHAMSCOL",
    Callback=function(v)
        if not PremiumUnlocked then return end
        local s = type(v)=="table" and v[1] or tostring(v)
        local cols = {
            Red=Color3.fromRGB(255,60,60), Purple=Color3.fromRGB(180,40,255),
            Cyan=Color3.fromRGB(0,220,220), Yellow=Color3.fromRGB(255,220,40),
            White=Color3.fromRGB(240,240,255), Orange=Color3.fromRGB(255,120,20),
        }
        if Chams.active then RemoveChams(); ApplyChams(cols[s] or cols.Red) end
    end})
PT:CreateToggle({Name="Enemy Chams", CurrentValue=false, Flag="PCHAMS",
    Callback=function(v) PremGuard(v,"Chams",function(val)
        if val then
            local colMap = {Red=Color3.fromRGB(255,60,60),Purple=Color3.fromRGB(180,40,255),
                Cyan=Color3.fromRGB(0,220,220),Yellow=Color3.fromRGB(255,220,40),
                White=Color3.fromRGB(240,240,255),Orange=Color3.fromRGB(255,120,20)}
            local ok,sel = pcall(function()
                return Rayfield.Flags and Rayfield.Flags.PCHAMSCOL and Rayfield.Flags.PCHAMSCOL.Value or "Red"
            end)
            local selName = (ok and sel) or "Red"
            ApplyChams(colMap[selName] or colMap.Red)
            Rayfield:Notify({Title="🌟 Chams ON",Content="Enemies highlighted.",Duration=3,Image=4483362458})
        else RemoveChams() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🔴 Danger Screen Pulse")
PT:CreateLabel("Screen edge pulses RED when ball is flying at you.")
PT:CreateLabel("Brighter = closer. Impossible to miss incoming ball.")
PT:CreateToggle({Name="Danger Pulse", CurrentValue=false, Flag="PSZ",
    Callback=function(v) PremGuard(v,"SafeZone",function(val)
        if val then StartSafeZone()
            Rayfield:Notify({Title="🔴 Danger Pulse ON",Content="Screen edge warns you.",Duration=3,Image=4483362458})
        else StopSafeZone() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("⚡ Premium FPS Booster")
PT:CreateLabel("Kills particles, shadows, atmosphere, post FX — everything.")
PT:CreateLabel("Way more aggressive than the old free version.")
PT:CreateToggle({Name="Premium FPS Boost", CurrentValue=false, Flag="PFPSB",
    Callback=function(v) PremGuard(v,"PremFPS",function(val)
        if val then ApplyPremFPS()
            Rayfield:Notify({Title="⚡ Premium FPS ON",Content="Max performance mode.",Duration=3,Image=4483362458})
        else RemovePremFPS()
            Rayfield:Notify({Title="⚡ Premium FPS OFF",Content="Restored.",Duration=2,Image=4483362458})
        end
    end)
end})

PT:CreateDivider()
PT:CreateSection("👻 Ghost Mode")
PT:CreateLabel("Makes your character 75% transparent locally.")
PT:CreateLabel("Confuses enemies — they can barely see you moving.")
PT:CreateToggle({Name="Ghost Mode", CurrentValue=false, Flag="PGHOST",
    Callback=function(v) PremGuard(v,"Ghost",function(val)
        SetGhost(val)
        Rayfield:Notify({
            Title = val and "👻 Ghost Mode ON" or "👻 Ghost Mode OFF",
            Content = val and "You are nearly invisible." or "Restored.",
            Duration = 3, Image = 4483362458,
        })
    end)
end})

PT:CreateDivider()
PT:CreateSection("📊 Premium Status")
PT:CreateButton({Name="Check Premium Status", Callback=function()
    if PremiumUnlocked then
        Rayfield:Notify({Title="✨ Premium ACTIVE",Content="All premium features unlocked.",Duration=4,Image=4483362458})
    else
        Rayfield:Notify({Title="🔒 Not Unlocked",Content="Enter your key above.",Duration=4,Image=4483362458})
    end
end})

end -- premium backend 1 do..end

do
-- ══════════════════════════════════════════════════════════════════
-- PREMIUM FEATURES BATCH 2 ◈ More exclusives
-- ══════════════════════════════════════════════════════════════════

-- ── 8. AUTO DODGE ────────────────────────────────────────────────
-- When the ball is targeting YOU and within a threshold,
-- fires a random strafe direction to break the ball's path.
-- Uses BodyVelocity for a clean instant burst.
local AutoDodge = {conn=nil, cd=0}
local function StartAutoDodge()
    if AutoDodge.conn then AutoDodge.conn:Disconnect() end
    AutoDodge.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            if not PremiumUnlocked then return end
            local ball = GetBall(); if not ball then return end
            local hrp = GetHRP(); if not hrp then return end
            if ball:GetAttribute("target") ~= Plr.Name then return end
            local dist = (hrp.Position - ball.Position).Magnitude
            local now = os.clock()
            if dist <= 18 and now - AutoDodge.cd > 1.2 then
                AutoDodge.cd = now
                local dirs = {
                    hrp.CFrame.RightVector,
                    -hrp.CFrame.RightVector,
                    hrp.CFrame.RightVector + hrp.CFrame.LookVector,
                    -hrp.CFrame.RightVector - hrp.CFrame.LookVector,
                }
                local dir = dirs[math.random(1, #dirs)]
                local bv = Instance.new("BodyVelocity", hrp)
                bv.Velocity = dir * 55
                bv.MaxForce = Vector3.new(1e5, 0, 1e5)
                game:GetService("Debris"):AddItem(bv, 0.12)
            end
        end)
    end)
end
local function StopAutoDodge()
    if AutoDodge.conn then AutoDodge.conn:Disconnect(); AutoDodge.conn = nil end
end

-- ── 9. COMBO TRACKER ─────────────────────────────────────────────
-- Counts consecutive successful parries this round.
-- Shows on a floating HUD with streak record.
local Combo = {gui=nil, count=0, record=0, conn=nil, lastPCount=0}
local function StartComboTracker()
    if Combo.gui then pcall(function() Combo.gui:Destroy() end) end
    local sg = Instance.new("ScreenGui")
    sg.Name = "RyuuCombo"; sg.ResetOnSpawn = false
    sg.IgnoreGuiInset = true; sg.DisplayOrder = 9993
    pcall(function() sg.Parent = (typeof(gethui)=="function" and gethui()) or CoreGui end)
    if not sg.Parent then pcall(function() sg.Parent = Plr:WaitForChild("PlayerGui") end) end
    Combo.gui = sg
    local card = Instance.new("Frame", sg)
    card.AnchorPoint = Vector2.new(0, 0.5)
    card.Position = UDim2.new(0, 14, 0.5, 0)
    card.Size = UDim2.new(0, 120, 0, 60)
    card.BackgroundColor3 = Color3.fromRGB(6, 4, 14)
    card.BackgroundTransparency = 0.15
    card.BorderSizePixel = 0
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 10)
    local stroke = Instance.new("UIStroke", card)
    stroke.Thickness = 1.2; stroke.Color = Color3.fromRGB(90, 40, 180)
    local titleL = Instance.new("TextLabel", card)
    titleL.Size = UDim2.new(1,-8,0,16); titleL.Position = UDim2.new(0,4,0,4)
    titleL.BackgroundTransparency = 1; titleL.Text = "⚔ COMBO"
    titleL.Font = Enum.Font.GothamBold; titleL.TextSize = 10
    titleL.TextColor3 = Color3.fromRGB(120,80,200); titleL.TextXAlignment = Enum.TextXAlignment.Left
    local comboL = Instance.new("TextLabel", card)
    comboL.Name = "CL"
    comboL.Size = UDim2.new(1,-8,0,22); comboL.Position = UDim2.new(0,4,0,18)
    comboL.BackgroundTransparency = 1; comboL.Text = "x0"
    comboL.Font = Enum.Font.GothamBlack; comboL.TextSize = 20
    comboL.TextColor3 = Color3.fromRGB(200,200,220); comboL.TextXAlignment = Enum.TextXAlignment.Left
    local recL = Instance.new("TextLabel", card)
    recL.Name = "RL"
    recL.Size = UDim2.new(1,-8,0,14); recL.Position = UDim2.new(0,4,0,42)
    recL.BackgroundTransparency = 1; recL.Text = "Best: 0"
    recL.Font = Enum.Font.Gotham; recL.TextSize = 10
    recL.TextColor3 = Color3.fromRGB(100,80,150); recL.TextXAlignment = Enum.TextXAlignment.Left
    -- Watch parry counter
    Combo.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local cl = card:FindFirstChild("CL"); if not cl then return end
            local rl = card:FindFirstChild("RL"); if not rl then return end
            if S.ParryCount ~= Combo.lastPCount then
                Combo.count = Combo.count + (S.ParryCount - Combo.lastPCount)
                if Combo.count > Combo.record then Combo.record = Combo.count end
                Combo.lastPCount = S.ParryCount
                cl.Text = "x"..Combo.count
                rl.Text = "Best: "..Combo.record
                -- Color tier
                local col = Combo.count >= 20 and Color3.fromRGB(255,60,220)
                         or Combo.count >= 10 and Color3.fromRGB(255,130,30)
                         or Combo.count >=  5 and Color3.fromRGB(80,220,120)
                         or Color3.fromRGB(200,200,220)
                cl.TextColor3 = col
                stroke.Color = col
            end
        end)
    end)
    -- Reset combo on death
    Plr.CharacterAdded:Connect(function()
        Combo.count = 0
        pcall(function()
            local cl = card:FindFirstChild("CL"); if cl then cl.Text="x0"; cl.TextColor3=Color3.fromRGB(200,200,220) end
        end)
    end)
end
local function StopComboTracker()
    if Combo.conn then Combo.conn:Disconnect(); Combo.conn = nil end
    if Combo.gui then pcall(function() Combo.gui:Destroy() end); Combo.gui = nil end
end

-- ── 10. SILENT BALL HIGHLIGHT ────────────────────────────────────
-- Makes the ball glow bright neon when targeting YOU specifically.
-- Normal color otherwise. Impossible to lose track of.
local SBH = {conn=nil, color=nil, mat=nil}
local TA = {conn=nil, last=""}
local AB = {conn=nil, last=0}
local PS  = {sound=nil, conn=nil, last=0}
local function StartSilentBallHighlight()
    if SBH.conn then SBH.conn:Disconnect() end
    SBH.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall(); if not ball then return end
            local bp = ball:FindFirstChildOfClass("BasePart") or ball
            if not bp:IsA("BasePart") then return end
            if ball:GetAttribute("target") == Plr.Name then
                if SBH.color == nil then
                    SBH.color = bp.Color
                    SBH.mat   = bp.Material
                end
                bp.Color    = Color3.fromRGB(255, 50, 50)
                bp.Material = Enum.Material.Neon
            else
                if SBH.color then
                    bp.Color    = SBH.color
                    bp.Material = SBH.mat
                    SBH.color = nil; SBH.mat = nil
                end
            end
        end)
    end)
end
local function StopSilentBallHighlight()
    if SBH.conn then SBH.conn:Disconnect(); SBH.conn = nil end
    pcall(function()
        local ball = GetBall(); if not ball then return end
        local bp = ball:FindFirstChildOfClass("BasePart") or ball
        if SBH.color and bp:IsA("BasePart") then
            bp.Color = SBH.color; bp.Material = SBH.mat
        end
        SBH.color = nil; SBH.mat = nil
    end)
end

-- ── 11. TARGET ALERT ─────────────────────────────────────────────
-- Pops a screen notification the instant the ball switches to target YOU.
-- Shows who passed it and the ball speed at time of targeting.
local function StartTargetAlert()
    if TA.conn then TA.conn:Disconnect() end
    TA.last = ""
    TA.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local ball = GetBall(); if not ball then TA.last=""; return end
            local tgt = ball:GetAttribute("target") or ""
            if tgt == Plr.Name and TA.last ~= Plr.Name then
                -- Ball just switched to us
                local spd = math.floor(GetVel(ball).Magnitude)
                Rayfield:Notify({
                    Title = "🎯 BALL INCOMING",
                    Content = "Speed: "..spd.." st/s — GET READY",
                    Duration = 1.5,
                    Image = 4483362458,
                })
            end
            TA.last = tgt
        end)
    end)
end
local function StopTargetAlert()
    if TA.conn then TA.conn:Disconnect(); TA.conn = nil end
    TA.last = ""
end

-- ── 12. ANTI-BLOCK (spam prevention bypass) ──────────────────────
-- Auto-detects when your parry key is eaten (e.g. clicked too fast)
-- and re-fires once with a 0.05s delay to push it through.
local function StartAntiBlock()
    if AB.conn then AB.conn:Disconnect() end
    local prevCount = S.ParryCount
    AB.conn = RunService.Heartbeat:Connect(function()
        pcall(function()
            local now = os.clock()
            -- If we fired but parry count didn't go up in 0.15s, re-fire
            if S.DidParry and now - S.LastParry > 0.1 and now - AB.last > 0.3 then
                if S.ParryCount == prevCount then
                    task.delay(0.05, function()
                        if not S.DidParry then return end
                        VIM:SendMouseButtonEvent(0,0,0,true,game,0)
                        task.wait(0.02)
                        VIM:SendMouseButtonEvent(0,0,0,false,game,0)
                        AB.last = os.clock()
                    end)
                end
            end
            prevCount = S.ParryCount
        end)
    end)
end
local function StopAntiBlock()
    if AB.conn then AB.conn:Disconnect(); AB.conn = nil end
end

-- ── 13. PARRY SOUND ALERT ────────────────────────────────────────
-- Plays a local beep sound on successful parry so you can hear
-- your own parry rate without watching the counter.
local function SetupParrySound()
    PS.sound = Instance.new("Sound")
    PS.sound.SoundId = "rbxassetid://6042053626"
    PS.sound.Volume = 0.4
    PS.sound.RollOffMaxDistance = 0
    PS.sound.Parent = workspace
end
local function StartParrySound()
    SetupParrySound()
    local lastCount = S.ParryCount
    PS.soundConn = RunService.Heartbeat:Connect(function()
        pcall(function()
            if S.ParryCount ~= lastCount then
                lastCount = S.ParryCount
                local now = os.clock()
                if now - PS.soundLast > 0.1 then
                    PS.soundLast = now
                    PS.sound:Play()
                end
            end
        end)
    end)
end
local function StopParrySound()
    if PS.soundConn then PS.soundConn:Disconnect(); PS.soundConn = nil end
    if PS.sound then pcall(function() PS.sound:Destroy() end); PS.sound = nil end
end

-- ══════════════════════════════════════════════════════════════════
-- PREMIUM TAB ◈ Batch 2 UI additions
-- ══════════════════════════════════════════════════════════════════
PT:CreateDivider()
end -- premium backend 2 do..end

PT:CreateSection("💨 Auto Dodge")
PT:CreateLabel("Strafes randomly when ball is ~18 studs away targeting you.")
PT:CreateLabel("1.2s cooldown so it doesn't spam. Works with any parry engine.")
PT:CreateToggle({Name="Auto Dodge", CurrentValue=false, Flag="PADODGE",
    Callback=function(v) PremGuard(v,"AutoDodge",function(val)
        if val then StartAutoDodge()
            Rayfield:Notify({Title="💨 Auto Dodge ON",Content="Will strafe when ball closes in.",Duration=3,Image=4483362458})
        else StopAutoDodge() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("⚔ Combo Tracker")
PT:CreateLabel("Counts consecutive parries. Left side of screen. Tracks your best.")
PT:CreateToggle({Name="Combo Tracker", CurrentValue=false, Flag="PCOMBO",
    Callback=function(v) PremGuard(v,"Combo",function(val)
        if val then StartComboTracker()
            Rayfield:Notify({Title="⚔ Combo ON",Content="Left side of screen.",Duration=3,Image=4483362458})
        else StopComboTracker() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🔴 Ball Highlight")
PT:CreateLabel("Ball turns RED neon when it's targeting you. Never lose it.")
PT:CreateToggle({Name="Ball Highlight (Targeting You)", CurrentValue=false, Flag="PSBH",
    Callback=function(v) PremGuard(v,"BallHL",function(val)
        if val then StartSilentBallHighlight()
            Rayfield:Notify({Title="🔴 Ball Highlight ON",Content="Ball glows red when on you.",Duration=3,Image=4483362458})
        else StopSilentBallHighlight() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🎯 Target Alert")
PT:CreateLabel("Instant notification the moment the ball switches to target YOU.")
PT:CreateLabel("Shows ball speed so you know how fast to react.")
PT:CreateToggle({Name="Target Alert", CurrentValue=false, Flag="PTGTALERT",
    Callback=function(v) PremGuard(v,"TargetAlert",function(val)
        if val then StartTargetAlert()
            Rayfield:Notify({Title="🎯 Target Alert ON",Content="You'll be notified instantly.",Duration=3,Image=4483362458})
        else StopTargetAlert() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🛡 Anti-Block")
PT:CreateLabel("Re-fires parry if it gets eaten by the game's click filter.")
PT:CreateLabel("Fixes missed parries caused by rapid clicking.")
PT:CreateToggle({Name="Anti-Block", CurrentValue=false, Flag="PANTIBLOCK",
    Callback=function(v) PremGuard(v,"AntiBlock",function(val)
        if val then StartAntiBlock()
            Rayfield:Notify({Title="🛡 Anti-Block ON",Content="Missed parries auto-retry.",Duration=3,Image=4483362458})
        else StopAntiBlock() end
    end)
end})

PT:CreateDivider()
PT:CreateSection("🔊 Parry Sound")
PT:CreateLabel("Plays a click sound on every successful parry.")
PT:CreateLabel("Lets you hear your own parry rate without watching the HUD.")
PT:CreateToggle({Name="Parry Sound", CurrentValue=false, Flag="PPSOUND",
    Callback=function(v) PremGuard(v,"ParrySound",function(val)
        if val then StartParrySound()
            Rayfield:Notify({Title="🔊 Parry Sound ON",Content="Click = confirmed parry.",Duration=3,Image=4483362458})
        else StopParrySound() end
    end)
end})
PT:CreateSlider({Name="Sound Volume", Range={0,100}, Increment=5, CurrentValue=40, Suffix="%", Flag="PVOL",
    Callback=function(v)
        if not PremiumUnlocked then return end
        if PS.sound then pcall(function() PS.sound.Volume = v/100 end) end
    end})
